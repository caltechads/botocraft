---
Parameter:
  operations:
    create:
      boto3_name: put_parameter
      return_type: int
      response_attr: Version
      args:
        Overwrite:
          default: "False"
        Description:
          explicit: true
        AllowedPattern:
          explicit: true
        Tags:
          explicit: true
        Tier:
          explicit: true
        Policies:
          explicit: true
        KeyId:
          explicit: true
    update:
      boto3_name: put_parameter
      return_type: int
      response_attr: Version
      defaults:
        Overwrite:
          default: "True"
        Description:
          explicit: true
        AllowedPattern:
          explicit: true
        Tags:
          explicit: true
        Tier:
          explicit: true
        Policies:
          explicit: true
        KeyId:
          explicit: true
    get:
      boto3_name: get_parameters
      response_attr: Parameters[0]
      args:
        WithDecryption:
          explicit: true
    list:
      boto3_name: describe_parameters
      return_type: List["ParameterMetadata"]
      response_attr: Parameters
    delete:
      boto3_name: delete_parameter
      return_type: None

