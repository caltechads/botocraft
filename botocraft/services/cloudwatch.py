# This file is automatically generated by botocraft.  Do not edit directly.
# mypy: disable-error-code="index, override, assignment, union-attr, misc"
import builtins
from botocraft.mixins.cloudwatch import CloudWatchMetricUnit
from botocraft.mixins.cloudwatch import CloudWatchGetDataPeriod
from botocraft.mixins.cloudwatch import CloudWatchInsightRuleManagerMixin
from botocraft.mixins.cloudwatch import (
    CloudWatchGetDataStat,
)
from botocraft.mixins.tags import TagsDictMixin
from typing import ClassVar, Literal, Any, Type as ModelType, cast
from botocraft.mixins.cloudwatch import CloudWatchMetricModelMixin
from pydantic import Field
from .abstract import PrimaryBoto3ModelQuerySet
from datetime import datetime
from .abstract import (
    Boto3Model,
    ReadonlyBoto3Model,
    PrimaryBoto3Model,
    ReadonlyPrimaryBoto3Model,
    Boto3ModelManager,
    ReadonlyBoto3ModelManager,
)
from botocraft.mixins.cloudwatch import CloudWatchAnomalyDetectorManagerMixin
from botocraft.mixins.cloudwatch import CloudWatchMetricManagerMixin

# ===============
# Managers
# ===============


class MetricAlarmManager(Boto3ModelManager):
    service_name: str = "cloudwatch"

    def create(
        self, model: "MetricAlarm", Tags: "builtins.list[CloudWatchTag] | None" = None
    ) -> None:
        """
        Creates or updates an alarm and associates it with the specified metric, metric math expression, anomaly detection
        model, Metrics Insights query, or PromQL query. For more information about using a Metrics Insights query for an alarm,
        see `Create alarms on Metrics Insights
        queries <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Create_Metrics_Insights_Alarm.html>`_.

        Args:
            model: The :py:class:`MetricAlarm` to create.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm. You can associate as many as 50 tags with an alarm. To
                be able to associate tags with the alarm when you create the alarm, you must have the ``cloudwatch:TagResource``
                permission.

        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            AlarmName=data.get("AlarmName"),
            AlarmDescription=data.get("AlarmDescription"),
            ActionsEnabled=data.get("ActionsEnabled"),
            OKActions=data.get("OKActions"),
            AlarmActions=data.get("AlarmActions"),
            InsufficientDataActions=data.get("InsufficientDataActions"),
            MetricName=data.get("MetricName"),
            Namespace=data.get("Namespace"),
            Statistic=data.get("Statistic"),
            ExtendedStatistic=data.get("ExtendedStatistic"),
            Dimensions=data.get("Dimensions"),
            Period=data.get("Period"),
            Unit=data.get("Unit"),
            EvaluationPeriods=data.get("EvaluationPeriods"),
            DatapointsToAlarm=data.get("DatapointsToAlarm"),
            Threshold=data.get("Threshold"),
            ComparisonOperator=data.get("ComparisonOperator"),
            TreatMissingData=data.get("TreatMissingData"),
            EvaluateLowSampleCountPercentile=data.get(
                "EvaluateLowSampleCountPercentile"
            ),
            Metrics=data.get("Metrics"),
            Tags=self.serialize(Tags),
            ThresholdMetricId=data.get("ThresholdMetricId"),
            EvaluationCriteria=data.get("EvaluationCriteria"),
            EvaluationInterval=data.get("EvaluationInterval"),
        )
        self.client.put_metric_alarm(**{k: v for k, v in args.items() if v is not None})

    def update(
        self, model: "MetricAlarm", Tags: "builtins.list[CloudWatchTag] | None" = None
    ) -> None:
        """
        Creates or updates an alarm and associates it with the specified metric, metric math expression, anomaly detection
        model, Metrics Insights query, or PromQL query. For more information about using a Metrics Insights query for an alarm,
        see `Create alarms on Metrics Insights
        queries <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Create_Metrics_Insights_Alarm.html>`_.

        Args:
            model: The :py:class:`MetricAlarm` to update.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm. You can associate as many as 50 tags with an alarm. To
                be able to associate tags with the alarm when you create the alarm, you must have the ``cloudwatch:TagResource``
                permission.

        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            AlarmName=data.get("AlarmName"),
            AlarmDescription=data.get("AlarmDescription"),
            ActionsEnabled=data.get("ActionsEnabled"),
            OKActions=data.get("OKActions"),
            AlarmActions=data.get("AlarmActions"),
            InsufficientDataActions=data.get("InsufficientDataActions"),
            MetricName=data.get("MetricName"),
            Namespace=data.get("Namespace"),
            Statistic=data.get("Statistic"),
            ExtendedStatistic=data.get("ExtendedStatistic"),
            Dimensions=data.get("Dimensions"),
            Period=data.get("Period"),
            Unit=data.get("Unit"),
            EvaluationPeriods=data.get("EvaluationPeriods"),
            DatapointsToAlarm=data.get("DatapointsToAlarm"),
            Threshold=data.get("Threshold"),
            ComparisonOperator=data.get("ComparisonOperator"),
            TreatMissingData=data.get("TreatMissingData"),
            EvaluateLowSampleCountPercentile=data.get(
                "EvaluateLowSampleCountPercentile"
            ),
            Metrics=data.get("Metrics"),
            Tags=self.serialize(Tags),
            ThresholdMetricId=data.get("ThresholdMetricId"),
            EvaluationCriteria=data.get("EvaluationCriteria"),
            EvaluationInterval=data.get("EvaluationInterval"),
        )
        self.client.put_metric_alarm(**{k: v for k, v in args.items() if v is not None})

    def get(
        self,
        AlarmName: str,
        *,
        AlarmNamePrefix: "str | None" = None,
        ChildrenOfAlarmName: "str | None" = None,
        ParentsOfAlarmName: "str | None" = None,
        StateValue: "Literal['OK', 'ALARM', 'INSUFFICIENT_DATA'] | None" = None,
        ActionPrefix: "str | None" = None,
    ) -> "MetricAlarm | None":
        """
        Retrieves the specified alarms. You can filter the results by specifying a
        prefix for the alarm name, the alarm state, or a prefix for any action.

        Args:
            AlarmName: The names of the alarms to retrieve information about.

        Keyword Args:
            AlarmNamePrefix: An alarm name prefix. If you specify this parameter, you receive information about all alarms that
                have names that start with this prefix.
            ChildrenOfAlarmName: If you use this parameter and specify the name of a composite alarm, the operation returns
                information about the "children" alarms of the alarm you specify. These are the metric alarms and composite alarms
                referenced in the ``AlarmRule`` field of the composite alarm that you specify in ``ChildrenOfAlarmName``.
                Information about the composite alarm that you name in ``ChildrenOfAlarmName`` is not returned.
            ParentsOfAlarmName: If you use this parameter and specify the name of a metric or composite alarm, the operation
                returns information about the "parent" alarms of the alarm you specify. These are the composite alarms that have
                ``AlarmRule`` parameters that reference the alarm named in ``ParentsOfAlarmName``. Information about the alarm that
                you specify in ``ParentsOfAlarmName`` is not returned.
            StateValue: Specify this parameter to receive information only about alarms that are currently in the state that you
                specify.
            ActionPrefix: Use this parameter to filter the results of the operation to only those alarms that use a certain
                alarm action. For example, you could specify the ARN of an SNS topic to find all alarms that send notifications to
                that topic.
        """
        args: dict[str, Any] = dict(
            AlarmNames=self.serialize([AlarmName]),
            AlarmNamePrefix=self.serialize(AlarmNamePrefix),
            AlarmTypes=["MetricAlarm"],
            ChildrenOfAlarmName=self.serialize(ChildrenOfAlarmName),
            ParentsOfAlarmName=self.serialize(ParentsOfAlarmName),
            StateValue=self.serialize(StateValue),
            ActionPrefix=self.serialize(ActionPrefix),
        )
        _response = self.client.describe_alarms(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DescribeAlarmsOutput(**_response)

        if response and response.MetricAlarms:
            self.sessionize(response.MetricAlarms[0])
            return response.MetricAlarms[0]
        return None

    def list(
        self,
        *,
        AlarmNames: "builtins.list[str] | None" = None,
        AlarmNamePrefix: "str | None" = None,
        ChildrenOfAlarmName: "str | None" = None,
        ParentsOfAlarmName: "str | None" = None,
        StateValue: "Literal['OK', 'ALARM', 'INSUFFICIENT_DATA'] | None" = None,
        ActionPrefix: "str | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Retrieves the specified alarms. You can filter the results by specifying a
        prefix for the alarm name, the alarm state, or a prefix for any action.

        Keyword Args:
            AlarmNames: The names of the alarms to retrieve information about.
            AlarmNamePrefix: An alarm name prefix. If you specify this parameter, you receive information about all alarms that
                have names that start with this prefix.
            ChildrenOfAlarmName: If you use this parameter and specify the name of a composite alarm, the operation returns
                information about the "children" alarms of the alarm you specify. These are the metric alarms and composite alarms
                referenced in the ``AlarmRule`` field of the composite alarm that you specify in ``ChildrenOfAlarmName``.
                Information about the composite alarm that you name in ``ChildrenOfAlarmName`` is not returned.
            ParentsOfAlarmName: If you use this parameter and specify the name of a metric or composite alarm, the operation
                returns information about the "parent" alarms of the alarm you specify. These are the composite alarms that have
                ``AlarmRule`` parameters that reference the alarm named in ``ParentsOfAlarmName``. Information about the alarm that
                you specify in ``ParentsOfAlarmName`` is not returned.
            StateValue: Specify this parameter to receive information only about alarms that are currently in the state that you
                specify.
            ActionPrefix: Use this parameter to filter the results of the operation to only those alarms that use a certain
                alarm action. For example, you could specify the ARN of an SNS topic to find all alarms that send notifications to
                that topic.
        """
        paginator = self.client.get_paginator("describe_alarms")
        args: dict[str, Any] = dict(
            AlarmNames=self.serialize(AlarmNames),
            AlarmNamePrefix=self.serialize(AlarmNamePrefix),
            AlarmTypes=["MetricAlarm"],
            ChildrenOfAlarmName=self.serialize(ChildrenOfAlarmName),
            ParentsOfAlarmName=self.serialize(ParentsOfAlarmName),
            StateValue=self.serialize(StateValue),
            ActionPrefix=self.serialize(ActionPrefix),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = DescribeAlarmsOutput(**_response)
            if response.MetricAlarms:
                results.extend(response.MetricAlarms)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, AlarmName: str) -> None:
        """
        Deletes the specified alarms. You can delete up to 100 alarms in one operation.
        However, this total can include no more than one composite alarm. For example,
        you could delete 99 metric alarms and one composite alarms with one operation,
        but you can't delete two composite alarms with one operation.

        Args:
            AlarmName: The alarms to be deleted. Do not enclose the alarm names in quote marks.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.delete_alarms(**{k: v for k, v in args.items() if v is not None})

    def enable_actions(self, AlarmName: str) -> None:
        """
        Enables the actions for the specified alarms.

        Args:
            AlarmName: The names of the alarms.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.enable_alarm_actions(
            **{k: v for k, v in args.items() if v is not None}
        )

    def disable_actions(self, AlarmName: str) -> None:
        """
        Disables the actions for the specified alarms. When an alarm's actions are
        disabled, the alarm actions do not execute when the alarm state changes.

        Args:
            AlarmName: The names of the alarms.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.disable_alarm_actions(
            **{k: v for k, v in args.items() if v is not None}
        )

    def set_state(
        self,
        AlarmName: str,
        StateValue: Literal["OK", "ALARM", "INSUFFICIENT_DATA"],
        StateReason: str,
        *,
        StateReasonData: "str | None" = None,
    ) -> None:
        """
        Temporarily sets the state of an alarm for testing purposes. When the updated
        state differs from the previous value, the action configured for the appropriate
        state is invoked. For example, if your alarm is configured to send an Amazon SNS
        message when an alarm is triggered, temporarily changing the alarm state to
        ``ALARM`` sends an SNS message.

        Args:
            AlarmName: The name of the alarm.
            StateValue: The value of the state.
            StateReason: The reason that this alarm is set to this specific state, in text format.

        Keyword Args:
            StateReasonData: The reason that this alarm is set to this specific state, in JSON format.
        """
        args: dict[str, Any] = dict(
            AlarmName=self.serialize(AlarmName),
            StateValue=self.serialize(StateValue),
            StateReason=self.serialize(StateReason),
            StateReasonData=self.serialize(StateReasonData),
        )
        self.client.set_alarm_state(**{k: v for k, v in args.items() if v is not None})

    def describe_history(
        self,
        AlarmName: str,
        *,
        AlarmContributorId: "str | None" = None,
        HistoryItemType: "Literal['ConfigurationUpdate', 'StateUpdate', 'Action', 'AlarmContributorStateUpdate', 'AlarmContributorAction'] | None" = None,
        StartDate: "datetime | None" = None,
        EndDate: "datetime | None" = None,
        ScanBy: "Literal['TimestampDescending', 'TimestampAscending'] | None" = None,
    ) -> "builtins.list[AlarmHistoryItem]":
        """
        Retrieves the history for the specified alarm. You can filter the results by
        date range or item type. If an alarm name is not specified, the histories for
        either all metric alarms or all composite alarms are returned.

        Args:
            AlarmName: The name of the alarm.

        Keyword Args:
            AlarmContributorId: The unique identifier of a specific alarm contributor to filter the alarm history results.
            HistoryItemType: The type of alarm histories to retrieve.
            StartDate: The starting date to retrieve alarm history.
            EndDate: The ending date to retrieve alarm history.
            ScanBy: Specified whether to return the newest or oldest alarm history first. Specify ``TimestampDescending`` to
                have the newest event history returned first, and specify ``TimestampAscending`` to have the oldest history returned
                first.
        """
        paginator = self.client.get_paginator("describe_alarm_history")
        args: dict[str, Any] = dict(
            AlarmName=self.serialize(AlarmName),
            AlarmContributorId=self.serialize(AlarmContributorId),
            AlarmTypes=["MetricAlarm"],
            HistoryItemType=self.serialize(HistoryItemType),
            StartDate=self.serialize(StartDate),
            EndDate=self.serialize(EndDate),
            ScanBy=self.serialize(ScanBy),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )

        results: "builtins.list[AlarmHistoryItem]" = []

        for _response in response_iterator:
            response = DescribeAlarmHistoryOutput(**_response)
            if response.AlarmHistoryItems is not None:
                results.extend(response.AlarmHistoryItems)
            else:
                break

        self.sessionize(results)
        return cast("builtins.list[AlarmHistoryItem]", results)


class CompositeAlarmManager(Boto3ModelManager):
    service_name: str = "cloudwatch"

    def create(
        self,
        model: "CompositeAlarm",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates or updates a *composite alarm*. When you create a composite alarm, you
        specify a rule expression for the alarm that takes into account the alarm states
        of other alarms that you have created. The composite alarm goes into ALARM state
        only if all conditions of the rule are met.

        Args:
            model: The :py:class:`CompositeAlarm` to create.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm. You can associate as many as 50 tags with an alarm. To
                be able to associate tags with the alarm when you create the alarm, you must have the ``cloudwatch:TagResource``
                permission.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            AlarmName=data.get("AlarmName"),
            AlarmRule=data.get("AlarmRule"),
            ActionsEnabled=data.get("ActionsEnabled"),
            AlarmActions=data.get("AlarmActions"),
            AlarmDescription=data.get("AlarmDescription"),
            InsufficientDataActions=data.get("InsufficientDataActions"),
            OKActions=data.get("OKActions"),
            Tags=self.serialize(Tags),
            ActionsSuppressor=data.get("ActionsSuppressor"),
            ActionsSuppressorWaitPeriod=data.get("ActionsSuppressorWaitPeriod"),
            ActionsSuppressorExtensionPeriod=data.get(
                "ActionsSuppressorExtensionPeriod"
            ),
        )
        self.client.put_composite_alarm(
            **{k: v for k, v in args.items() if v is not None}
        )

    def update(
        self,
        model: "CompositeAlarm",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates or updates a *composite alarm*. When you create a composite alarm, you
        specify a rule expression for the alarm that takes into account the alarm states
        of other alarms that you have created. The composite alarm goes into ALARM state
        only if all conditions of the rule are met.

        Args:
            model: The :py:class:`CompositeAlarm` to update.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm. You can associate as many as 50 tags with an alarm. To
                be able to associate tags with the alarm when you create the alarm, you must have the ``cloudwatch:TagResource``
                permission.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            AlarmName=data.get("AlarmName"),
            AlarmRule=data.get("AlarmRule"),
            ActionsEnabled=data.get("ActionsEnabled"),
            AlarmActions=data.get("AlarmActions"),
            AlarmDescription=data.get("AlarmDescription"),
            InsufficientDataActions=data.get("InsufficientDataActions"),
            OKActions=data.get("OKActions"),
            Tags=self.serialize(Tags),
            ActionsSuppressor=data.get("ActionsSuppressor"),
            ActionsSuppressorWaitPeriod=data.get("ActionsSuppressorWaitPeriod"),
            ActionsSuppressorExtensionPeriod=data.get(
                "ActionsSuppressorExtensionPeriod"
            ),
        )
        self.client.put_composite_alarm(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(
        self,
        AlarmName: str,
        *,
        AlarmNamePrefix: "str | None" = None,
        ChildrenOfAlarmName: "str | None" = None,
        ParentsOfAlarmName: "str | None" = None,
        StateValue: "Literal['OK', 'ALARM', 'INSUFFICIENT_DATA'] | None" = None,
        ActionPrefix: "str | None" = None,
    ) -> "CompositeAlarm | None":
        """
        Retrieves the specified alarms. You can filter the results by specifying a
        prefix for the alarm name, the alarm state, or a prefix for any action.

        Args:
            AlarmName: The names of the alarms to retrieve information about.

        Keyword Args:
            AlarmNamePrefix: An alarm name prefix. If you specify this parameter, you receive information about all alarms that
                have names that start with this prefix.
            ChildrenOfAlarmName: If you use this parameter and specify the name of a composite alarm, the operation returns
                information about the "children" alarms of the alarm you specify. These are the metric alarms and composite alarms
                referenced in the ``AlarmRule`` field of the composite alarm that you specify in ``ChildrenOfAlarmName``.
                Information about the composite alarm that you name in ``ChildrenOfAlarmName`` is not returned.
            ParentsOfAlarmName: If you use this parameter and specify the name of a metric or composite alarm, the operation
                returns information about the "parent" alarms of the alarm you specify. These are the composite alarms that have
                ``AlarmRule`` parameters that reference the alarm named in ``ParentsOfAlarmName``. Information about the alarm that
                you specify in ``ParentsOfAlarmName`` is not returned.
            StateValue: Specify this parameter to receive information only about alarms that are currently in the state that you
                specify.
            ActionPrefix: Use this parameter to filter the results of the operation to only those alarms that use a certain
                alarm action. For example, you could specify the ARN of an SNS topic to find all alarms that send notifications to
                that topic.
        """
        args: dict[str, Any] = dict(
            AlarmNames=self.serialize([AlarmName]),
            AlarmNamePrefix=self.serialize(AlarmNamePrefix),
            AlarmTypes=["CompositeAlarm"],
            ChildrenOfAlarmName=self.serialize(ChildrenOfAlarmName),
            ParentsOfAlarmName=self.serialize(ParentsOfAlarmName),
            StateValue=self.serialize(StateValue),
            ActionPrefix=self.serialize(ActionPrefix),
        )
        _response = self.client.describe_alarms(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DescribeAlarmsOutput(**_response)

        if response and response.CompositeAlarms:
            self.sessionize(response.CompositeAlarms[0])
            return response.CompositeAlarms[0]
        return None

    def list(
        self,
        *,
        AlarmNames: "builtins.list[str] | None" = None,
        AlarmNamePrefix: "str | None" = None,
        ChildrenOfAlarmName: "str | None" = None,
        ParentsOfAlarmName: "str | None" = None,
        StateValue: "Literal['OK', 'ALARM', 'INSUFFICIENT_DATA'] | None" = None,
        ActionPrefix: "str | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Retrieves the specified alarms. You can filter the results by specifying a
        prefix for the alarm name, the alarm state, or a prefix for any action.

        Keyword Args:
            AlarmNames: The names of the alarms to retrieve information about.
            AlarmNamePrefix: An alarm name prefix. If you specify this parameter, you receive information about all alarms that
                have names that start with this prefix.
            ChildrenOfAlarmName: If you use this parameter and specify the name of a composite alarm, the operation returns
                information about the "children" alarms of the alarm you specify. These are the metric alarms and composite alarms
                referenced in the ``AlarmRule`` field of the composite alarm that you specify in ``ChildrenOfAlarmName``.
                Information about the composite alarm that you name in ``ChildrenOfAlarmName`` is not returned.
            ParentsOfAlarmName: If you use this parameter and specify the name of a metric or composite alarm, the operation
                returns information about the "parent" alarms of the alarm you specify. These are the composite alarms that have
                ``AlarmRule`` parameters that reference the alarm named in ``ParentsOfAlarmName``. Information about the alarm that
                you specify in ``ParentsOfAlarmName`` is not returned.
            StateValue: Specify this parameter to receive information only about alarms that are currently in the state that you
                specify.
            ActionPrefix: Use this parameter to filter the results of the operation to only those alarms that use a certain
                alarm action. For example, you could specify the ARN of an SNS topic to find all alarms that send notifications to
                that topic.
        """
        paginator = self.client.get_paginator("describe_alarms")
        args: dict[str, Any] = dict(
            AlarmNames=self.serialize(AlarmNames),
            AlarmNamePrefix=self.serialize(AlarmNamePrefix),
            AlarmTypes=["CompositeAlarm"],
            ChildrenOfAlarmName=self.serialize(ChildrenOfAlarmName),
            ParentsOfAlarmName=self.serialize(ParentsOfAlarmName),
            StateValue=self.serialize(StateValue),
            ActionPrefix=self.serialize(ActionPrefix),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = DescribeAlarmsOutput(**_response)
            if response.CompositeAlarms:
                results.extend(response.CompositeAlarms)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, AlarmName: str) -> None:
        """
        Deletes the specified alarms. You can delete up to 100 alarms in one operation.
        However, this total can include no more than one composite alarm. For example,
        you could delete 99 metric alarms and one composite alarms with one operation,
        but you can't delete two composite alarms with one operation.

        Args:
            AlarmName: The alarms to be deleted. Do not enclose the alarm names in quote marks.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.delete_alarms(**{k: v for k, v in args.items() if v is not None})

    def enable_actions(self, AlarmName: str) -> None:
        """
        Enables the actions for the specified alarms.

        Args:
            AlarmName: The names of the alarms.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.enable_alarm_actions(
            **{k: v for k, v in args.items() if v is not None}
        )

    def disable_actions(self, AlarmName: str) -> None:
        """
        Disables the actions for the specified alarms. When an alarm's actions are
        disabled, the alarm actions do not execute when the alarm state changes.

        Args:
            AlarmName: The names of the alarms.
        """
        args: dict[str, Any] = dict(AlarmNames=self.serialize([AlarmName]))
        self.client.disable_alarm_actions(
            **{k: v for k, v in args.items() if v is not None}
        )

    def set_state(
        self,
        AlarmName: str,
        StateValue: Literal["OK", "ALARM", "INSUFFICIENT_DATA"],
        StateReason: str,
        *,
        StateReasonData: "str | None" = None,
    ) -> None:
        """
        Temporarily sets the state of an alarm for testing purposes. When the updated
        state differs from the previous value, the action configured for the appropriate
        state is invoked. For example, if your alarm is configured to send an Amazon SNS
        message when an alarm is triggered, temporarily changing the alarm state to
        ``ALARM`` sends an SNS message.

        Args:
            AlarmName: The name of the alarm.
            StateValue: The value of the state.
            StateReason: The reason that this alarm is set to this specific state, in text format.

        Keyword Args:
            StateReasonData: The reason that this alarm is set to this specific state, in JSON format.
        """
        args: dict[str, Any] = dict(
            AlarmName=self.serialize(AlarmName),
            StateValue=self.serialize(StateValue),
            StateReason=self.serialize(StateReason),
            StateReasonData=self.serialize(StateReasonData),
        )
        self.client.set_alarm_state(**{k: v for k, v in args.items() if v is not None})

    def describe_history(
        self,
        AlarmName: str,
        *,
        AlarmContributorId: "str | None" = None,
        HistoryItemType: "Literal['ConfigurationUpdate', 'StateUpdate', 'Action', 'AlarmContributorStateUpdate', 'AlarmContributorAction'] | None" = None,
        StartDate: "datetime | None" = None,
        EndDate: "datetime | None" = None,
        ScanBy: "Literal['TimestampDescending', 'TimestampAscending'] | None" = None,
    ) -> "builtins.list[AlarmHistoryItem]":
        """
        Retrieves the history for the specified alarm. You can filter the results by
        date range or item type. If an alarm name is not specified, the histories for
        either all metric alarms or all composite alarms are returned.

        Args:
            AlarmName: The name of the alarm.

        Keyword Args:
            AlarmContributorId: The unique identifier of a specific alarm contributor to filter the alarm history results.
            HistoryItemType: The type of alarm histories to retrieve.
            StartDate: The starting date to retrieve alarm history.
            EndDate: The ending date to retrieve alarm history.
            ScanBy: Specified whether to return the newest or oldest alarm history first. Specify ``TimestampDescending`` to
                have the newest event history returned first, and specify ``TimestampAscending`` to have the oldest history returned
                first.
        """
        paginator = self.client.get_paginator("describe_alarm_history")
        args: dict[str, Any] = dict(
            AlarmName=self.serialize(AlarmName),
            AlarmContributorId=self.serialize(AlarmContributorId),
            AlarmTypes=["CompositeAlarm"],
            HistoryItemType=self.serialize(HistoryItemType),
            StartDate=self.serialize(StartDate),
            EndDate=self.serialize(EndDate),
            ScanBy=self.serialize(ScanBy),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )

        results: "builtins.list[AlarmHistoryItem]" = []

        for _response in response_iterator:
            response = DescribeAlarmHistoryOutput(**_response)
            if response.AlarmHistoryItems is not None:
                results.extend(response.AlarmHistoryItems)
            else:
                break

        self.sessionize(results)
        return cast("builtins.list[AlarmHistoryItem]", results)


class CloudWatchMetricStreamManager(Boto3ModelManager):
    service_name: str = "cloudwatch"

    def create(
        self,
        model: "CloudWatchMetricStream",
        RoleArn: str,
        IncludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = None,
        ExcludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = None,
        Tags: "builtins.list[CloudWatchTag] | None" = None,
        StatisticsConfigurations: "builtins.list[CloudWatchMetricStreamStatisticsConfiguration] | None" = None,
        IncludeLinkedAccountsMetrics: "bool | None" = None,
    ) -> None:
        """
        Creates or updates a metric stream. Metric streams can automatically stream
        CloudWatch metrics to Amazon Web Services destinations, including Amazon S3, and
        to many third-party solutions.

        Args:
            model: The :py:class:`MetricStreamEntry` to create.
            RoleArn: The ARN of an IAM role that this metric stream will use to access Amazon Kinesis Data Firehose resources.
                This IAM role must already exist and must be in the same account as the metric stream. This IAM role must include
                the following permissions:

        Keyword Args:
            IncludeFilters: If you specify this parameter, the stream sends only the metrics from the metric namespaces that you
                specify here.
            ExcludeFilters: If you specify this parameter, the stream sends metrics from all metric namespaces except for the
                namespaces that you specify here.
            Tags: A list of key-value pairs to associate with the metric stream. You can associate as many as 50 tags with a
                metric stream.
            StatisticsConfigurations: By default, a metric stream always sends the ``MAX``, ``MIN``, ``SUM``, and
                ``SAMPLECOUNT`` statistics for each metric that is streamed. You can use this parameter to have the metric stream
                also send additional statistics in the stream. This array can have up to 100 members.
            IncludeLinkedAccountsMetrics: If you are creating a metric stream in a monitoring account, specify ``true`` to
                include metrics from source accounts in the metric stream.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Name=data.get("Name"),
            FirehoseArn=data.get("FirehoseArn"),
            RoleArn=self.serialize(RoleArn),
            OutputFormat=data.get("OutputFormat"),
            IncludeFilters=self.serialize(IncludeFilters),
            ExcludeFilters=self.serialize(ExcludeFilters),
            Tags=self.serialize(Tags),
            StatisticsConfigurations=self.serialize(StatisticsConfigurations),
            IncludeLinkedAccountsMetrics=self.serialize(IncludeLinkedAccountsMetrics),
        )
        self.client.put_metric_stream(
            **{k: v for k, v in args.items() if v is not None}
        )

    def update(
        self,
        model: "CloudWatchMetricStream",
        RoleArn: str,
        IncludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = None,
        ExcludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = None,
        Tags: "builtins.list[CloudWatchTag] | None" = None,
        StatisticsConfigurations: "builtins.list[CloudWatchMetricStreamStatisticsConfiguration] | None" = None,
        IncludeLinkedAccountsMetrics: "bool | None" = None,
    ) -> None:
        """
        Creates or updates a metric stream. Metric streams can automatically stream
        CloudWatch metrics to Amazon Web Services destinations, including Amazon S3, and
        to many third-party solutions.

        Args:
            model: The :py:class:`MetricStreamEntry` to update.
            RoleArn: The ARN of an IAM role that this metric stream will use to access Amazon Kinesis Data Firehose resources.
                This IAM role must already exist and must be in the same account as the metric stream. This IAM role must include
                the following permissions:

        Keyword Args:
            IncludeFilters: If you specify this parameter, the stream sends only the metrics from the metric namespaces that you
                specify here.
            ExcludeFilters: If you specify this parameter, the stream sends metrics from all metric namespaces except for the
                namespaces that you specify here.
            Tags: A list of key-value pairs to associate with the metric stream. You can associate as many as 50 tags with a
                metric stream.
            StatisticsConfigurations: By default, a metric stream always sends the ``MAX``, ``MIN``, ``SUM``, and
                ``SAMPLECOUNT`` statistics for each metric that is streamed. You can use this parameter to have the metric stream
                also send additional statistics in the stream. This array can have up to 100 members.
            IncludeLinkedAccountsMetrics: If you are creating a metric stream in a monitoring account, specify ``true`` to
                include metrics from source accounts in the metric stream.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Name=data.get("Name"),
            FirehoseArn=data.get("FirehoseArn"),
            RoleArn=self.serialize(RoleArn),
            OutputFormat=data.get("OutputFormat"),
            IncludeFilters=self.serialize(IncludeFilters),
            ExcludeFilters=self.serialize(ExcludeFilters),
            Tags=self.serialize(Tags),
            StatisticsConfigurations=self.serialize(StatisticsConfigurations),
            IncludeLinkedAccountsMetrics=self.serialize(IncludeLinkedAccountsMetrics),
        )
        self.client.put_metric_stream(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, Name: str) -> "CloudWatchMetricStream | None":
        """
        Returns information about the metric stream that you specify.

        Args:
            Name: The name of the metric stream to retrieve information about.
        """
        args: dict[str, Any] = dict(Name=self.serialize(Name))
        _response = self.client.get_metric_stream(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CloudWatchMetricStream(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self, *, NextToken: "str | None" = None, MaxResults: "int | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of metric streams in this account.

        Keyword Args:
            NextToken: Include this value, if it was returned by the previous call, to get the next set of metric streams.
            MaxResults: The maximum number of results to return in one operation.
        """
        args: dict[str, Any] = dict(
            NextToken=self.serialize(NextToken), MaxResults=self.serialize(MaxResults)
        )
        _response = self.client.list_metric_streams(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListMetricStreamsOutput(**_response)
        if response and response.Entries:
            self.sessionize(response.Entries)
            return PrimaryBoto3ModelQuerySet(response.Entries)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, Name: str) -> None:
        """
        Permanently deletes the metric stream that you specify.

        Args:
            Name: The name of the metric stream to delete.
        """
        args: dict[str, Any] = dict(Name=self.serialize(Name))
        self.client.delete_metric_stream(
            **{k: v for k, v in args.items() if v is not None}
        )

    def start(self, Name: str) -> None:
        """
        Starts the streaming of metrics for one or more of your metric streams.

        Args:
            Name: The array of the names of metric streams to start streaming.
        """
        args: dict[str, Any] = dict(Names=self.serialize([Name]))
        self.client.start_metric_streams(
            **{k: v for k, v in args.items() if v is not None}
        )

    def stop(self, Name: str) -> None:
        """
        Stops the streaming of metrics for one or more of your metric streams.

        Args:
            Name: The array of the names of metric streams to stop streaming.
        """
        args: dict[str, Any] = dict(Names=self.serialize([Name]))
        self.client.stop_metric_streams(
            **{k: v for k, v in args.items() if v is not None}
        )


class CloudWatchDashboardManager(Boto3ModelManager):
    service_name: str = "cloudwatch"

    def create(
        self,
        model: "CloudWatchDashboard",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates a dashboard if it does not already exist, or updates an existing
        dashboard. If you update a dashboard, the entire contents are replaced with what
        you specify here.

        Args:
            model: The :py:class:`DashboardEntry` to create.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the dashboard. You can associate as many as 50 tags with a
                dashboard.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            DashboardName=data.get("DashboardName"),
            DashboardBody=data.get("DashboardBody"),
            Tags=self.serialize(Tags),
        )
        self.client.put_dashboard(**{k: v for k, v in args.items() if v is not None})

    def update(
        self,
        model: "CloudWatchDashboard",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates a dashboard if it does not already exist, or updates an existing
        dashboard. If you update a dashboard, the entire contents are replaced with what
        you specify here.

        Args:
            model: The :py:class:`DashboardEntry` to update.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the dashboard. You can associate as many as 50 tags with a
                dashboard.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            DashboardName=data.get("DashboardName"),
            DashboardBody=data.get("DashboardBody"),
            Tags=self.serialize(Tags),
        )
        self.client.put_dashboard(**{k: v for k, v in args.items() if v is not None})

    def get(self, DashboardName: str) -> "CloudWatchDashboard | None":
        """
        Displays the details of the dashboard that you specify.

        Args:
            DashboardName: The name of the dashboard to be described.
        """
        args: dict[str, Any] = dict(DashboardName=self.serialize(DashboardName))
        _response = self.client.get_dashboard(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CloudWatchDashboard(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self, *, DashboardNamePrefix: "str | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of the dashboards for your account. If you include
        ``DashboardNamePrefix``, only those dashboards with names starting with the
        prefix are listed. Otherwise, all dashboards in your account are listed.

        Keyword Args:
            DashboardNamePrefix: If you specify this parameter, only the dashboards with names starting with the specified
                string are listed. The maximum length is 255, and valid characters are A-Z, a-z, 0-9, ".", "-", and "_".
        """
        paginator = self.client.get_paginator("list_dashboards")
        args: dict[str, Any] = dict(
            DashboardNamePrefix=self.serialize(DashboardNamePrefix)
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListDashboardsOutput(**_response)
            if response.DashboardEntries:
                results.extend(response.DashboardEntries)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, DashboardName: str) -> None:
        """
        Deletes all dashboards that you specify. You can specify up to 100 dashboards to
        delete. If there is an error during this call, the operation attempts to delete
        as many dashboards as possible.

        Args:
            DashboardName: The dashboards to be deleted. This parameter is required.
        """
        args: dict[str, Any] = dict(DashboardNames=self.serialize([DashboardName]))
        self.client.delete_dashboards(
            **{k: v for k, v in args.items() if v is not None}
        )


class CloudWatchInsightRuleManager(
    CloudWatchInsightRuleManagerMixin, Boto3ModelManager
):
    service_name: str = "cloudwatch"

    def create(
        self,
        model: "CloudWatchInsightRule",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates a Contributor Insights rule. Rules evaluate log events in a CloudWatch Logs log group, enabling you to find
        contributor data for the log events in that log group. For more information, see `Using Contributor Insights to Analyze
        High-Cardinality Data <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/ContributorInsights.html>`_.

        Args:
            model: The :py:class:`InsightRule` to create.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the Contributor Insights rule. You can associate as many as 50
                tags with a rule.

        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            RuleName=data.get("Name"),
            RuleDefinition=data.get("Definition"),
            RuleState=data.get("State"),
            Tags=self.serialize(Tags),
            ApplyOnTransformedLogs=data.get("ApplyOnTransformedLogs"),
        )
        self.client.put_insight_rule(**{k: v for k, v in args.items() if v is not None})

    def update(
        self,
        model: "CloudWatchInsightRule",
        Tags: "builtins.list[CloudWatchTag] | None" = None,
    ) -> None:
        """
        Creates a Contributor Insights rule. Rules evaluate log events in a CloudWatch Logs log group, enabling you to find
        contributor data for the log events in that log group. For more information, see `Using Contributor Insights to Analyze
        High-Cardinality Data <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/ContributorInsights.html>`_.

        Args:
            model: The :py:class:`InsightRule` to update.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the Contributor Insights rule. You can associate as many as 50
                tags with a rule.

        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            RuleName=data.get("Name"),
            RuleDefinition=data.get("Definition"),
            RuleState=data.get("State"),
            Tags=self.serialize(Tags),
            ApplyOnTransformedLogs=data.get("ApplyOnTransformedLogs"),
        )
        self.client.put_insight_rule(**{k: v for k, v in args.items() if v is not None})

    def delete(self, Name: str) -> None:
        """
        Permanently deletes the specified Contributor Insights rules.

        Args:
            Name: An array of the rule names to delete. If you need to find out the names of your rules, use
                `DescribeInsightRules
                <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_DescribeInsightRules.html>`_.
        """
        args: dict[str, Any] = dict(RuleNames=self.serialize([Name]))
        self.client.delete_insight_rules(
            **{k: v for k, v in args.items() if v is not None}
        )

    def enable(self, Name: str) -> None:
        """
        Enables the specified Contributor Insights rules. When rules are enabled, they
        immediately begin analyzing log data.

        Args:
            Name: An array of the rule names to enable. If you need to find out the names of your rules, use
                `DescribeInsightRules
                <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_DescribeInsightRules.html>`_.
        """
        args: dict[str, Any] = dict(RuleNames=self.serialize([Name]))
        self.client.enable_insight_rules(
            **{k: v for k, v in args.items() if v is not None}
        )

    def disable(self, Name: str) -> None:
        """
        Disables the specified Contributor Insights rules. When rules are disabled, they
        do not analyze log groups and do not incur costs.

        Args:
            Name: An array of the rule names to disable. If you need to find out the names of your rules, use
                `DescribeInsightRules
                <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_DescribeInsightRules.html>`_.
        """
        args: dict[str, Any] = dict(RuleNames=self.serialize([Name]))
        self.client.disable_insight_rules(
            **{k: v for k, v in args.items() if v is not None}
        )


class CloudWatchAnomalyDetectorManager(
    CloudWatchAnomalyDetectorManagerMixin, Boto3ModelManager
):
    service_name: str = "cloudwatch"

    def create(self, model: "CloudWatchAnomalyDetector") -> None:
        """
        Creates an anomaly detection model for a CloudWatch metric. You can use the
        model to display a band of expected normal values when the metric is graphed.

        Args:
            model: The :py:class:`AnomalyDetector` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Namespace=data.get("Namespace"),
            MetricName=data.get("MetricName"),
            Dimensions=data.get("Dimensions"),
            Stat=data.get("Stat"),
            Configuration=data.get("Configuration"),
            MetricCharacteristics=data.get("MetricCharacteristics"),
            SingleMetricAnomalyDetector=data.get("SingleMetricAnomalyDetector"),
            MetricMathAnomalyDetector=data.get("MetricMathAnomalyDetector"),
        )
        self.client.put_anomaly_detector(
            **{k: v for k, v in args.items() if v is not None}
        )

    def update(self, model: "CloudWatchAnomalyDetector") -> None:
        """
        Creates an anomaly detection model for a CloudWatch metric. You can use the
        model to display a band of expected normal values when the metric is graphed.

        Args:
            model: The :py:class:`AnomalyDetector` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Namespace=data.get("Namespace"),
            MetricName=data.get("MetricName"),
            Dimensions=data.get("Dimensions"),
            Stat=data.get("Stat"),
            Configuration=data.get("Configuration"),
            MetricCharacteristics=data.get("MetricCharacteristics"),
            SingleMetricAnomalyDetector=data.get("SingleMetricAnomalyDetector"),
            MetricMathAnomalyDetector=data.get("MetricMathAnomalyDetector"),
        )
        self.client.put_anomaly_detector(
            **{k: v for k, v in args.items() if v is not None}
        )

    def list(
        self,
        *,
        Namespace: "str | None" = None,
        MetricName: "str | None" = None,
        Dimensions: "builtins.list[CloudWatchDimension] | None" = None,
        AnomalyDetectorTypes: "builtins.list[Literal['SINGLE_METRIC', 'METRIC_MATH']] | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Lists the anomaly detection models that you have created in your account. For
        single metric anomaly detectors, you can list all of the models in your account
        or filter the results to only the models that are related to a certain
        namespace, metric name, or metric dimension. For metric math anomaly detectors,
        you can list them by adding ``METRIC_MATH`` to the ``AnomalyDetectorTypes``
        array. This will return all metric math anomaly detectors in your account.

        Keyword Args:
            Namespace: Limits the results to only the anomaly detection models that are associated with the specified namespace.
            MetricName: Limits the results to only the anomaly detection models that are associated with the specified metric
                name. If there are multiple metrics with this name in different namespaces that have anomaly detection models,
                they're all returned.
            Dimensions: Limits the results to only the anomaly detection models that are associated with the specified metric
                dimensions. If there are multiple metrics that have these dimensions and have anomaly detection models associated,
                they're all returned.
            AnomalyDetectorTypes: The anomaly detector types to request when using ``DescribeAnomalyDetectorsInput``. If empty,
                defaults to ``SINGLE_METRIC``.
        """
        paginator = self.client.get_paginator("describe_anomaly_detectors")
        args: dict[str, Any] = dict(
            Namespace=self.serialize(Namespace),
            MetricName=self.serialize(MetricName),
            Dimensions=self.serialize(Dimensions),
            AnomalyDetectorTypes=self.serialize(AnomalyDetectorTypes),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = DescribeAnomalyDetectorsOutput(**_response)
            if response.AnomalyDetectors:
                results.extend(response.AnomalyDetectors)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results


class CloudWatchMetricManager(CloudWatchMetricManagerMixin, ReadonlyBoto3ModelManager):
    service_name: str = "cloudwatch"

    def list(
        self,
        *,
        Namespace: "str | None" = None,
        MetricName: "str | None" = None,
        Dimensions: "builtins.list[DimensionFilter] | None" = None,
        RecentlyActive: "Literal['PT3H'] | None" = None,
        IncludeLinkedAccounts: "bool | None" = None,
        OwningAccount: "str | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List the specified metrics. You can use the returned metrics with
        `GetMetricData <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_GetMetricData.html>`_ or
        `GetMetricStatistics <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_GetMetricStatistics.html>`_ to
        get statistical data.

        Keyword Args:
            Namespace: The metric namespace to filter against. Only the namespace that matches exactly will be returned.
            MetricName: The name of the metric to filter against. Only the metrics with names that match exactly will be
                returned.
            Dimensions: The dimensions to filter against. Only the dimension with names that match exactly will be returned. If
                you specify one dimension name and a metric has that dimension and also other dimensions, it will be returned.
            RecentlyActive: To filter the results to show only metrics that have had data points published in the past three
                hours, specify this parameter with a value of ``PT3H``. This is the only valid value for this parameter.
            IncludeLinkedAccounts: If you are using this operation in a monitoring account, specify ``true`` to include metrics
                from source accounts in the returned data.
            OwningAccount: When you use this operation in a monitoring account, use this field to return metrics only from one
                source account. To do so, specify that source account ID in this field, and also specify ``true`` for
                ``IncludeLinkedAccounts``.

        """
        paginator = self.client.get_paginator("list_metrics")
        args: dict[str, Any] = dict(
            Namespace=self.serialize(Namespace),
            MetricName=self.serialize(MetricName),
            Dimensions=self.serialize(Dimensions),
            RecentlyActive=self.serialize(RecentlyActive),
            IncludeLinkedAccounts=self.serialize(IncludeLinkedAccounts),
            OwningAccount=self.serialize(OwningAccount),
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListMetricsOutput(**_response)
            if response.Metrics:
                results.extend(response.Metrics)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results


class AlarmMuteRuleManager(Boto3ModelManager):
    service_name: str = "cloudwatch"

    def create(
        self, model: "AlarmMuteRule", Tags: "builtins.list[CloudWatchTag] | None" = None
    ) -> None:
        """
        Creates or updates an alarm mute rule.

        Args:
            model: The :py:class:`AlarmMuteRuleSummary` to create.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm mute rule. You can use tags to categorize and manage
                your mute rules.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Name=data.get("Name"),
            Rule=data.get("Rule"),
            Description=data.get("Description"),
            MuteTargets=data.get("MuteTargets"),
            Tags=self.serialize(Tags),
            StartDate=data.get("StartDate"),
            ExpireDate=data.get("ExpireDate"),
        )
        self.client.put_alarm_mute_rule(
            **{k: v for k, v in args.items() if v is not None}
        )

    def update(
        self, model: "AlarmMuteRule", Tags: "builtins.list[CloudWatchTag] | None" = None
    ) -> None:
        """
        Creates or updates an alarm mute rule.

        Args:
            model: The :py:class:`AlarmMuteRuleSummary` to update.

        Keyword Args:
            Tags: A list of key-value pairs to associate with the alarm mute rule. You can use tags to categorize and manage
                your mute rules.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Name=data.get("Name"),
            Rule=data.get("Rule"),
            Description=data.get("Description"),
            MuteTargets=data.get("MuteTargets"),
            Tags=self.serialize(Tags),
            StartDate=data.get("StartDate"),
            ExpireDate=data.get("ExpireDate"),
        )
        self.client.put_alarm_mute_rule(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, Name: str) -> "AlarmMuteRule | None":
        """
        Retrieves details for a specific alarm mute rule.

        Args:
            Name: The name of the alarm mute rule to retrieve.
        """
        args: dict[str, Any] = dict(AlarmMuteRuleName=self.serialize(Name))
        _response = self.client.get_alarm_mute_rule(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = AlarmMuteRule(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self,
        *,
        AlarmName: "str | None" = None,
        Statuses: "builtins.list[Literal['SCHEDULED', 'ACTIVE', 'EXPIRED']] | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Lists alarm mute rules in your Amazon Web Services account and region.

        Keyword Args:
            AlarmName: Filter results to show only mute rules that target the specified alarm name.
            Statuses: Filter results to show only mute rules with the specified statuses. Valid values are ``SCHEDULED``,
                ``ACTIVE``, or ``EXPIRED``.
        """
        paginator = self.client.get_paginator("list_alarm_mute_rules")
        args: dict[str, Any] = dict(
            AlarmName=self.serialize(AlarmName), Statuses=self.serialize(Statuses)
        )
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListAlarmMuteRulesOutput(**_response)
            if response.AlarmMuteRuleSummaries:
                results.extend(response.AlarmMuteRuleSummaries)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, Name: str) -> None:
        """
        Deletes a specific alarm mute rule.

        Args:
            Name: The name of the alarm mute rule to delete.
        """
        args: dict[str, Any] = dict(AlarmMuteRuleName=self.serialize(Name))
        self.client.delete_alarm_mute_rule(
            **{k: v for k, v in args.items() if v is not None}
        )


class OTelEnrichmentStatusManager(ReadonlyBoto3ModelManager):
    service_name: str = "cloudwatch"

    def get(
        self,
    ) -> "OTelEnrichmentStatus | None":
        """
        Returns the current status of vended metric enrichment for the account,
        including whether CloudWatch vended metrics are enriched with resource ARN and
        resource tag labels and queryable using PromQL.

        For the list of supported resources, see
        `Supported Amazon Web Services infrastructure metrics <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/UsingResourceTagsForTelemetry.html>`_.
        """
        args: dict[str, Any] = dict()
        _response = self.client.get_otel_enrichment(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = OTelEnrichmentStatus(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def start(
        self,
    ) -> None:
        """
        Enables enrichment and PromQL access for CloudWatch vended metrics for `supported Amazon Web Services
        resources <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/UsingResourceTagsForTelemetry.html>`_ in the
        account. Once enabled, metrics that contain a resource identifier dimension (for example, EC2 ``CPUUtilization`` with an
        ``InstanceId`` dimension) are enriched with resource ARN and resource tag labels and become queryable using PromQL.

        """
        args: dict[str, Any] = dict()
        self.client.start_otel_enrichment(
            **{k: v for k, v in args.items() if v is not None}
        )

    def stop(
        self,
    ) -> None:
        """
        Disables enrichment and PromQL access for CloudWatch vended metrics for `supported Amazon Web Services
        resources <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/UsingResourceTagsForTelemetry.html>`_ in the
        account. After disabling, these metrics are no longer enriched with resource ARN and resource tag labels, and cannot be
        queried using PromQL.

        """
        args: dict[str, Any] = dict()
        self.client.stop_otel_enrichment(
            **{k: v for k, v in args.items() if v is not None}
        )


# ==============
# Service Models
# ==============


class CloudWatchDimension(Boto3Model):
    """
    A dimension is a name/value pair that is part of the identity of a metric. Because
    dimensions are part of the unique identifier for a metric, whenever you add a unique
    name/value pair to one of your metrics, you are creating a new variation of that
    metric. For example, many Amazon EC2 metrics publish ``InstanceId`` as a dimension
    name, and the actual instance ID as the value for that dimension.

    You can assign up to 30 dimensions to a metric.
    """

    Name: str
    """
    The name of the dimension.

    Dimension names must contain only ASCII characters, must include at least one non-
    whitespace character, and cannot start with a colon (``:``). ASCII control
    characters are not supported as part of dimension names.
    """
    Value: str
    """
    The value of the dimension.

    Dimension values must contain only ASCII characters and must include at least one
    non- whitespace character. ASCII control characters are not supported as part of
    dimension values.
    """


class CloudWatchMetricStat(Boto3Model):
    """
    This structure defines the metric to be returned, along with the statistics, period,
    and units.
    """

    Metric: "CloudWatchMetric | None" = None
    """
    The metric to return, including the metric name, namespace, and dimensions.
    """
    Period: int
    """
    The granularity, in seconds, of the returned data points.

    For metrics with regular resolution, a period can be as short
    as one minute (60 seconds) and must be a multiple of 60. For high-resolution metrics that are collected at intervals of
    less than one minute, the period can be 1, 5, 10, 20, 30, 60, or any multiple of 60. High-resolution metrics are those
    metrics stored by a ``PutMetricData`` call that includes a ``StorageResolution`` of 1 second.
    """
    Stat: str
    """
    The statistic to return.

    It can include any CloudWatch statistic or extended statistic.
    """
    Unit: "Literal['Seconds', 'Microseconds', 'Milliseconds', 'Bytes', 'Kilobytes', 'Megabytes', 'Gigabytes', 'Terabytes', 'Bits', 'Kilobits', 'Megabits', 'Gigabits', 'Terabits', 'Percent', 'Count', 'Bytes/Second', 'Kilobytes/Second', 'Megabytes/Second', 'Gigabytes/Second', 'Terabytes/Second', 'Bits/Second', 'Kilobits/Second', 'Megabits/Second', 'Gigabits/Second', 'Terabits/Second', 'Count/Second', 'None'] | None" = None
    """
    When you are using a ``Put`` operation, this defines what unit you want to use when
    storing the metric.
    """


class CloudWatchMetricDataQuery(Boto3Model):
    """
    This structure is used in both ``GetMetricData`` and ``PutMetricAlarm``. The
    supported use of this structure is different for those two operations.

    When used in ``GetMetricData``, it indicates the metric data to return, and whether this call is just retrieving a batch
    set of data for one metric, or is performing a Metrics Insights query or a math expression. A single ``GetMetricData``
    call can include up to 500 ``MetricDataQuery`` structures.

    When used in ``PutMetricAlarm``, it enables you to create an alarm based on a metric math expression. Each
    ``MetricDataQuery`` in the array specifies either a metric to retrieve, or a math expression to be performed on
    retrieved metrics. A single ``PutMetricAlarm`` call can include up to 20 ``MetricDataQuery`` structures in the array.
    The 20 structures can include as many as 10 structures that contain a ``MetricStat`` parameter to retrieve a metric, and
    as many as 10 structures that contain the ``Expression`` parameter to perform a math expression. Of those ``Expression``
    structures, one must have ``true`` as the value for ``ReturnData``. The result of this expression is the value the alarm
    watches.

    Any expression used in a ``PutMetricAlarm`` operation must return a single time series. For more information, see
    `Metric Math Syntax and Functions <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/using-metric-
    math.html#metric-math-syntax>`_ in the *Amazon CloudWatch User Guide*.

    Some of the parameters of this structure also have different uses whether you are using this structure in a
    ``GetMetricData`` operation or a ``PutMetricAlarm`` operation. These differences are explained in the following
    parameter list.
    """

    Id: str
    """
    A short name used to tie this object to the results in the response.

    This name must be unique within a single call to
    ``GetMetricData``. If you are performing math expressions on this set of data, this name represents that data and can
    serve as a variable in the mathematical expression. The valid characters are letters, numbers, and underscore. The first
    character must be a lowercase letter.
    """
    MetricStat: "CloudWatchMetricStat | None" = None
    """
    The metric to be returned, along with statistics, period, and units.

    Use this parameter only if this object is retrieving a metric and not performing a
    math expression on returned data.
    """
    Expression: "str | None" = None
    """
    This field can contain either a Metrics Insights query, or a metric math expression
    to be performed on the returned data.

    For more information about Metrics Insights queries, see
    `Metrics Insights query components and syntax <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/cloudwatch-metrics-insights-querylanguage>`_
    in the
    *Amazon CloudWatch User Guide*.
    """
    Label: "str | None" = None
    """
    A human-readable label for this metric or expression.

    This is especially useful if this is an expression, so that you know what the value
    represents. If the metric or expression is shown in a CloudWatch dashboard widget,
    the label is shown. If Label is omitted, CloudWatch generates a default.
    """
    ReturnData: "bool | None" = None
    """
    When used in ``GetMetricData``, this option indicates whether to return the
    timestamps and raw data values of this metric.

    If you are performing this call just to do math expressions and do not also need the raw data returned, you can
    specify ``false``. If you omit this, the default of ``true`` is used.
    """
    Period: "int | None" = None
    """
    The granularity, in seconds, of the returned data points.

    For metrics with regular resolution, a period can be as short
    as one minute (60 seconds) and must be a multiple of 60. For high-resolution metrics that are collected at intervals of
    less than one minute, the period can be 1, 5, 10, 20, 30, 60, or any multiple of 60. High-resolution metrics are those
    metrics stored by a ``PutMetricData`` operation that includes a ``StorageResolution of 1 second``.
    """
    AccountId: "str | None" = None
    """
    The ID of the account where the metrics are located.
    """


class CloudWatchAlarmPromQLCriteria(Boto3Model):
    """
    Contains the configuration that determines how a PromQL alarm evaluates its
    contributors, including the query to run and the durations that define when
    contributors transition between states.
    """

    Query: str
    """
    The PromQL query that the alarm evaluates.

    The query must return a result of vector type. Each entry in the vector result
    represents an alarm contributor.
    """
    PendingPeriod: "int | None" = None
    """
    The duration, in seconds, that a contributor must be continuously breaching before
    it transitions to the ``ALARM`` state.
    """
    RecoveryPeriod: "int | None" = None
    """
    The duration, in seconds, that a contributor must continuously not be breaching
    before it transitions back to the ``OK`` state.
    """


class CloudWatchEvaluationCriteria(Boto3Model):
    """
    The evaluation criteria for an alarm.

    This is a union type that currently supports ``PromQLCriteria``.
    """

    PromQLCriteria: "CloudWatchAlarmPromQLCriteria | None" = None
    """
    The PromQL criteria for the alarm evaluation.
    """


class MetricAlarm(PrimaryBoto3Model):
    """
    The details about a metric alarm.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = MetricAlarmManager

    AlarmName: "str | None" = None
    """
    The name of the alarm.
    """
    AlarmArn: str = Field(default=None, frozen=True)
    """
    The Amazon Resource Name (ARN) of the alarm.
    """
    AlarmDescription: "str | None" = None
    """
    The description of the alarm.
    """
    AlarmConfigurationUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The time stamp of the last update to the alarm configuration.
    """
    ActionsEnabled: "bool | None" = None
    """
    Indicates whether actions should be executed during any changes to the alarm state.
    """
    OKActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the ``OK`` state from any
    other state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    AlarmActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the ``ALARM`` state from any
    other state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    InsufficientDataActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the ``INSUFFICIENT_DATA``
    state from any other state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    StateValue: Literal["OK", "ALARM", "INSUFFICIENT_DATA"] = Field(
        default=None, frozen=True
    )
    """
    The state value for the alarm.
    """
    StateReason: str = Field(default=None, frozen=True)
    """
    An explanation for the alarm state, in text format.
    """
    StateReasonData: str = Field(default=None, frozen=True)
    """
    An explanation for the alarm state, in JSON format.
    """
    StateUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The time stamp of the last update to the value of either the ``StateValue`` or
    ``EvaluationState`` parameters.
    """
    MetricName: "str | None" = None
    """
    The name of the metric associated with the alarm, if this is an alarm based on a
    single metric.
    """
    Namespace: "str | None" = None
    """
    The namespace of the metric associated with the alarm.
    """
    Statistic: "Literal['SampleCount', 'Average', 'Sum', 'Minimum', 'Maximum'] | None" = None
    """
    The statistic for the metric associated with the alarm, other than percentile.

    For percentile statistics, use
    ``ExtendedStatistic``.
    """
    ExtendedStatistic: "str | None" = None
    """
    The percentile statistic for the metric associated with the alarm.

    Specify a value between p0.0 and p100.
    """
    Dimensions: "builtins.list[CloudWatchDimension] | None" = Field(
        default_factory=list
    )
    """
    The dimensions for the metric associated with the alarm.
    """
    Period: "int | None" = None
    """
    The period, in seconds, over which the statistic is applied.
    """
    Unit: "Literal['Seconds', 'Microseconds', 'Milliseconds', 'Bytes', 'Kilobytes', 'Megabytes', 'Gigabytes', 'Terabytes', 'Bits', 'Kilobits', 'Megabits', 'Gigabits', 'Terabits', 'Percent', 'Count', 'Bytes/Second', 'Kilobytes/Second', 'Megabytes/Second', 'Gigabytes/Second', 'Terabytes/Second', 'Bits/Second', 'Kilobits/Second', 'Megabits/Second', 'Gigabits/Second', 'Terabits/Second', 'Count/Second', 'None'] | None" = None
    """
    The unit of the metric associated with the alarm.
    """
    EvaluationPeriods: "int | None" = None
    """
    The number of periods over which data is compared to the specified threshold.
    """
    DatapointsToAlarm: "int | None" = None
    """
    The number of data points that must be breaching to trigger the alarm.
    """
    Threshold: "float | None" = None
    """
    The value to compare with the specified statistic.
    """
    ComparisonOperator: "Literal['GreaterThanOrEqualToThreshold', 'GreaterThanThreshold', 'LessThanThreshold', 'LessThanOrEqualToThreshold', 'LessThanLowerOrGreaterThanUpperThreshold', 'LessThanLowerThreshold', 'GreaterThanUpperThreshold'] | None" = None
    """
    The arithmetic operation to use when comparing the specified statistic and
    threshold.

    The specified statistic value is used as the first operand.
    """
    TreatMissingData: "str | None" = None
    """
    Sets how this alarm is to handle missing data points.

    The valid values are ``breaching``, ``notBreaching``, ``ignore``,
    and ``missing``. For more information, see `Configuring how CloudWatch alarms treat missing
    data <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/AlarmThatSendsEmail.html#alarms-and-missing-data>`_.
    """
    EvaluateLowSampleCountPercentile: "str | None" = None
    """
    Used only for alarms based on percentiles.

    If ``ignore``, the alarm state does not change during periods with too few
    data points to be statistically significant. If ``evaluate`` or this parameter is not used, the alarm is always
    evaluated and possibly changes state no matter how many data points are available.
    """
    Metrics: "builtins.list[CloudWatchMetricDataQuery] | None" = Field(
        default_factory=list
    )
    """
    An array of MetricDataQuery structures, used in an alarm based on a metric math
    expression.

    Each structure either
    retrieves a metric or performs a math expression. One item in the Metrics array is the math expression that the alarm
    watches. This expression by designated by having ``ReturnData`` set to true.
    """
    ThresholdMetricId: "str | None" = None
    """
    In an alarm based on an anomaly detection model, this is the ID of the
    ``ANOMALY_DETECTION_BAND`` function used as the threshold for the alarm.
    """
    EvaluationState: Literal[
        "PARTIAL_DATA", "EVALUATION_FAILURE", "EVALUATION_ERROR"
    ] = Field(default=None, frozen=True)
    """
    If the value of this field is ``PARTIAL_DATA``, it indicates that not all the
    available data was able to be retrieved due to quota limitations.

    For more information, see
    `Create alarms on Metrics Insights queries <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Create_Metrics_Insights_Alarm.html>`_.
    """
    StateTransitionedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The date and time that the alarm's ``StateValue`` most recently changed.
    """
    EvaluationCriteria: "CloudWatchEvaluationCriteria | None" = None
    """
    The evaluation criteria for the alarm.
    """
    EvaluationInterval: "int | None" = None
    """
    The frequency, in seconds, at which the alarm is evaluated.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the
        :py:attr:`AlarmName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.AlarmName

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.   This is the value of the :py:attr:`AlarmArn`
        attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.AlarmArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the :py:attr:`AlarmName`
        attribute.

        Returns:
            The name of the model instance.
        """
        return self.AlarmName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`AlarmName` attribute.
        """
        return hash(self.AlarmName)

    def set_state(
        self,
        StateValue: Literal["OK", "ALARM", "INSUFFICIENT_DATA"],
        StateReason: str,
        StateReasonData: str | None = None,
    ) -> "None":
        """
        Temporarily set alarm state for testing.

        Args:
            StateValue: The value of the state.
            StateReason: The reason that this alarm is set to this specific state, in text format.

        Keyword Args:
            StateReasonData: The reason that this alarm is set to this specific state, in JSON format.
        """

        return (
            cast("MetricAlarmManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .set_state(
                cast("str", self.AlarmName),
                StateValue,
                StateReason,
                StateReasonData=StateReasonData,
            )
        )


class CompositeAlarm(PrimaryBoto3Model):
    """
    The details about a composite alarm.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = CompositeAlarmManager

    ActionsEnabled: "bool | None" = None
    """
    Indicates whether actions should be executed during any changes to the alarm state.
    """
    AlarmActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the ALARM state from any other
    state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    AlarmArn: str = Field(default=None, frozen=True)
    """
    The Amazon Resource Name (ARN) of the alarm.
    """
    AlarmConfigurationUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The time stamp of the last update to the alarm configuration.
    """
    AlarmDescription: "str | None" = None
    """
    The description of the alarm.
    """
    AlarmName: "str | None" = None
    """
    The name of the alarm.
    """
    AlarmRule: "str | None" = None
    """
    The rule that this alarm uses to evaluate its alarm state.
    """
    InsufficientDataActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the INSUFFICIENT_DATA state
    from any other state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    OKActions: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The actions to execute when this alarm transitions to the OK state from any other
    state.

    Each action is specified as an Amazon Resource Name (ARN).
    """
    StateReason: str = Field(default=None, frozen=True)
    """
    An explanation for the alarm state, in text format.
    """
    StateReasonData: str = Field(default=None, frozen=True)
    """
    An explanation for the alarm state, in JSON format.
    """
    StateUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    Tracks the timestamp of any state update, even if ``StateValue`` doesn't change.
    """
    StateValue: Literal["OK", "ALARM", "INSUFFICIENT_DATA"] = Field(
        default=None, frozen=True
    )
    """
    The state value for the alarm.
    """
    StateTransitionedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The timestamp of the last change to the alarm's ``StateValue``.
    """
    ActionsSuppressedBy: Literal["WaitPeriod", "ExtensionPeriod", "Alarm"] = Field(
        default=None, frozen=True
    )
    """
    When the value is ``ALARM``, it means that the actions are suppressed because the
    suppressor alarm is in ``ALARM`` When the value is ``WaitPeriod``, it means that the
    actions are suppressed because the composite alarm is waiting for the suppressor
    alarm to go into into the ``ALARM`` state.

    The maximum waiting time is as specified in
    ``ActionsSuppressorWaitPeriod``. After this time, the composite alarm performs its actions. When the value is
    ``ExtensionPeriod``, it means that the actions are suppressed because the composite alarm is waiting after the
    suppressor alarm went out of the ``ALARM`` state. The maximum waiting time is as specified in
    ``ActionsSuppressorExtensionPeriod``. After this time, the composite alarm performs its actions.
    """
    ActionsSuppressedReason: str = Field(default=None, frozen=True)
    """
    Captures the reason for action suppression.
    """
    ActionsSuppressor: "str | None" = None
    """
    Actions will be suppressed if the suppressor alarm is in the ``ALARM`` state.

    ``ActionsSuppressor`` can be an AlarmName
    or an Amazon Resource Name (ARN) from an existing alarm.
    """
    ActionsSuppressorWaitPeriod: "int | None" = None
    """
    The maximum time in seconds that the composite alarm waits for the suppressor alarm
    to go into the ``ALARM`` state.

    After this time, the composite alarm performs its actions.
    """
    ActionsSuppressorExtensionPeriod: "int | None" = None
    """
    The maximum time in seconds that the composite alarm waits after suppressor alarm
    goes out of the ``ALARM`` state.

    After this time, the composite alarm performs its actions.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the
        :py:attr:`AlarmName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.AlarmName

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.   This is the value of the :py:attr:`AlarmArn`
        attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.AlarmArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the :py:attr:`AlarmName`
        attribute.

        Returns:
            The name of the model instance.
        """
        return self.AlarmName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`AlarmName` attribute.
        """
        return hash(self.AlarmName)


class CloudWatchMetricStream(PrimaryBoto3Model):
    """
    This structure contains the configuration information about one metric stream.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        CloudWatchMetricStreamManager
    )

    Arn: str = Field(default=None, frozen=True)
    """
    The ARN of the metric stream.
    """
    CreationDate: datetime = Field(default=None, frozen=True)
    """
    The date that the metric stream was originally created.
    """
    LastUpdateDate: datetime = Field(default=None, frozen=True)
    """
    The date that the configuration of this metric stream was most recently updated.
    """
    Name: "str | None" = None
    """
    The name of the metric stream.
    """
    FirehoseArn: "str | None" = None
    """
    The ARN of the Kinesis Firehose devlivery stream that is used for this metric
    stream.
    """
    State: str = Field(default=None, frozen=True)
    """
    The current state of this stream.

    Valid values are ``running`` and ``stopped``.
    """
    OutputFormat: "Literal['json', 'opentelemetry0.7', 'opentelemetry1.0'] | None" = (
        None
    )
    """
    The output format of this metric stream.

    Valid values are ``json``, ``opentelemetry1.0``, and ``opentelemetry0.7``.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.Name

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.   This is the value of the :py:attr:`Arn`
        attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.Arn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The name of the model instance.
        """
        return self.Name

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`Name` attribute.
        """
        return hash(self.Name)


class CloudWatchDashboard(PrimaryBoto3Model):
    """
    Represents a specific dashboard.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = CloudWatchDashboardManager

    DashboardName: "str | None" = None
    """
    The name of the dashboard.
    """
    DashboardArn: str = Field(default=None, frozen=True)
    """
    The Amazon Resource Name (ARN) of the dashboard.
    """
    LastModified: datetime = Field(default=None, frozen=True)
    """
    The time stamp of when the dashboard was last modified, either by an API call or
    through the console.

    This number is expressed as the number of milliseconds since Jan 1, 1970 00:00:00
    UTC.
    """
    Size: int = Field(default=None, frozen=True)
    """
    The size of the dashboard, in bytes.
    """
    DashboardBody: str | None = None
    """
    The detail pane of the dashboard in JSON format.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the
        :py:attr:`DashboardName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.DashboardName

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.   This is the value of the :py:attr:`DashboardArn`
        attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.DashboardArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the
        :py:attr:`DashboardName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.DashboardName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`DashboardName` attribute.
        """
        return hash(self.DashboardName)


class CloudWatchInsightRule(PrimaryBoto3Model):
    """
    This structure contains the definition for a Contributor Insights rule.

    For more information about this rule, see `Using
    Constributor Insights to analyze high-cardinality
    data <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/ContributorInsights.html>`_ in the *Amazon CloudWatch
    User Guide*.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = CloudWatchInsightRuleManager

    Schema: "str | None" = None
    """
    For rules that you create, this is always ``{"Name": "CloudWatchLogRule", "Version":
    1}``.

    For managed rules, this is ``{"Name": "ServiceLogRule", "Version": 1}``
    """
    Name: str = Field(frozen=True)
    """
    The name of the rule.
    """
    State: str = Field(frozen=True)
    """
    Indicates whether the rule is enabled or disabled.
    """
    Definition: str = Field(frozen=True)
    """
    The definition of the rule, as a JSON object.

    The definition contains the keywords used to define contributors, the value to
    aggregate on if this rule returns a sum instead of a count, and the filters. For
    details on the valid syntax, see
    `Contributor Insights Rule Syntax <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/ContributorInsights-RuleSyntax.html>`_.
    """
    ManagedRule: bool = Field(default=None, frozen=True)
    """
    An optional built-in rule that Amazon Web Services manages.
    """
    ApplyOnTransformedLogs: "bool | None" = None
    """
Displays whether the rule is evaluated on the transformed versions of logs, for log groups that have `Log
transformation <https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/CloudWatch-Logs-Transformation.html>`_ enabled.
If this is ``false``, log events are evaluated before they are transformed.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.Name

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The name of the model instance.
        """
        return self.Name

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`Name` attribute.
        """
        return hash(self.Name)


class Range(Boto3Model):
    """
    Specifies one range of days or times to exclude from use for training an anomaly
    detection model.
    """

    StartTime: datetime
    """
    The start time of the range to exclude.

    The format is ``yyyy-MM-dd'T'HH:mm:ss``. For example, ``2019-07-01T23:59:59``.
    """
    EndTime: datetime
    """
    The end time of the range to exclude.

    The format is ``yyyy-MM-dd'T'HH:mm:ss``. For example, ``2019-07-01T23:59:59``.
    """


class CloudWatchAnomalyDetectorConfiguration(Boto3Model):
    """
    The configuration specifies details about how the anomaly detection model is to be
    trained, including time ranges to exclude from use for training the model and the
    time zone to use for the metric.
    """

    ExcludedTimeRanges: "builtins.list[Range] | None" = Field(default_factory=list)
    """
    An array of time ranges to exclude from use when the anomaly detection model is
    trained.

    Use this to make sure that events that could cause unusual values for the metric,
    such as deployments, aren't used when CloudWatch creates the model.
    """
    MetricTimezone: "str | None" = None
    """
    The time zone to use for the metric.

    This is useful to enable the model to automatically account for daylight savings
    time changes if the metric is sensitive to such time changes.
    """


class CloudWatchMetricCharacteristics(Boto3Model):
    """
    This object includes parameters that you can use to provide information to
    CloudWatch to help it build more accurate anomaly detection models.
    """

    PeriodicSpikes: "bool | None" = None
    """
    Set this parameter to ``true`` if values for this metric consistently include spikes
    that should not be considered to be anomalies.

    With this set to ``true``, CloudWatch will expect to see spikes that occurred consistently during the model
    training period, and won't flag future similar spikes as anomalies.
    """


class CloudWatchSingleMetricAnomalyDetector(Boto3Model):
    """
    Designates the CloudWatch metric and statistic that provides the time series the
    anomaly detector uses as input.

    If you have enabled unified cross-account observability, and this account is a
    monitoring account, the metric can be in the same account or a source account.
    """

    AccountId: "str | None" = None
    """
    If the CloudWatch metric that provides the time series that the anomaly detector
    uses as input is in another account, specify that account ID here.

    If you omit this parameter, the current account is used.
    """
    Namespace: "str | None" = None
    """
    The namespace of the metric to create the anomaly detection model for.
    """
    MetricName: "str | None" = None
    """
    The name of the metric to create the anomaly detection model for.
    """
    Dimensions: "builtins.list[CloudWatchDimension] | None" = Field(
        default_factory=list
    )
    """
    The metric dimensions to create the anomaly detection model for.
    """
    Stat: "str | None" = None
    """
    The statistic to use for the metric and anomaly detection model.
    """


class CloudWatchMetricMathAnomalyDetector(Boto3Model):
    """
    Indicates the CloudWatch math expression that provides the time series the anomaly
    detector uses as input.

    The designated math expression must return a single time series.
    """

    MetricDataQueries: "builtins.list[CloudWatchMetricDataQuery] | None" = Field(
        default_factory=list
    )
    """
    An array of metric data query structures that enables you to create an anomaly
    detector based on the result of a metric math expression.

    Each item in ``MetricDataQueries`` gets a metric or performs a math expression. One item in
    ``MetricDataQueries`` is the expression that provides the time series that the anomaly detector uses as input. Designate
    the expression by setting ``ReturnData`` to ``true`` for this object in the array. For all other expressions and
    metrics, set ``ReturnData`` to ``false``. The designated expression must return a single time series.
    """


class CloudWatchAnomalyDetector(PrimaryBoto3Model):
    """
    An anomaly detection model associated with a particular CloudWatch metric,
    statistic, or metric math expression. You can use the model to display a band of
    expected, normal values when the metric is graphed.

    If you have enabled unified cross-account observability, and this account is a
    monitoring account, the metric can be in the same account or a source account.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        CloudWatchAnomalyDetectorManager
    )

    Namespace: "str | None" = None
    """
    The namespace of the metric associated with the anomaly detection model.
    """
    MetricName: "str | None" = None
    """
    The name of the metric associated with the anomaly detection model.
    """
    Dimensions: "builtins.list[CloudWatchDimension] | None" = Field(
        default_factory=list
    )
    """
    The metric dimensions associated with the anomaly detection model.
    """
    Stat: "str | None" = None
    """
    The statistic associated with the anomaly detection model.
    """
    Configuration: "CloudWatchAnomalyDetectorConfiguration | None" = None
    """
    The configuration specifies details about how the anomaly detection model is to be
    trained, including time ranges to exclude from use for training the model, and the
    time zone to use for the metric.
    """
    StateValue: Literal["PENDING_TRAINING", "TRAINED_INSUFFICIENT_DATA", "TRAINED"] = (
        Field(default=None, frozen=True)
    )
    """
    The current status of the anomaly detector's training.
    """
    MetricCharacteristics: "CloudWatchMetricCharacteristics | None" = None
    """
    This object includes parameters that you can use to provide information about your
    metric to CloudWatch to help it build more accurate anomaly detection models.

    Currently, it includes the ``PeriodicSpikes`` parameter.
    """
    SingleMetricAnomalyDetector: "CloudWatchSingleMetricAnomalyDetector | None" = None
    """
    The CloudWatch metric and statistic for this anomaly detector.
    """
    MetricMathAnomalyDetector: "CloudWatchMetricMathAnomalyDetector | None" = None
    """
    The CloudWatch metric math expression for this anomaly detector.
    """

    @property
    def pk(self) -> dict[str, Any]:
        """
        The composite identity for an anomaly detector.
        """

        return (
            {
                "MetricMathAnomalyDetector": self.MetricMathAnomalyDetector.model_dump(
                    exclude_none=True
                )
            }
            if self.MetricMathAnomalyDetector
            else {
                "Namespace": self.Namespace,
                "MetricName": self.MetricName,
                "Dimensions": [
                    dimension.model_dump(exclude_none=True)
                    if hasattr(dimension, "model_dump")
                    else dimension
                    for dimension in (self.Dimensions or [])
                ],
                "Stat": self.Stat,
                "SingleMetricAnomalyDetector": self.SingleMetricAnomalyDetector.model_dump(
                    exclude_none=True
                )
                if self.SingleMetricAnomalyDetector
                else None,
            }
        )


class CloudWatchAlarmMuteSchedule(Boto3Model):
    """
    Specifies when and how long an alarm mute rule is active.

    The schedule uses either a cron expression for recurring mute windows or an at
    expression for one-time mute windows. When the schedule activates, the mute rule
    mutes alarm actions for the specified duration.
    """

    Expression: str
    """
    The schedule expression that defines when the mute rule activates.

    The expression must be between 1 and 256 characters in length.
    """
    Duration: str
    """
    The length of time that alarms remain muted when the schedule activates.

    The duration must be between 1 and 50 characters in length.
    """
    Timezone: "str | None" = None
    """
    The time zone to use when evaluating the schedule expression.

    The time zone must be between 1 and 50 characters in length.
    """


class CloudWatchAlarmMuteRule(Boto3Model):
    """
    Defines the schedule configuration for an alarm mute rule.

    The rule contains a schedule that specifies when and how long alarms should be
    muted. The schedule can be a recurring pattern using cron expressions or a one-time
    mute window using at expressions.
    """

    Schedule: CloudWatchAlarmMuteSchedule
    """
    The schedule configuration that defines when the mute rule activates and how long it
    remains active.
    """


class CloudWatchAlarmMuteTargets(Boto3Model):
    """
    Specifies which alarms an alarm mute rule applies to.

    You can target up to 100 specific alarms by name. When a mute rule is active, the
    targeted alarms continue to evaluate metrics and transition between states, but
    their configured actions are muted.
    """

    AlarmNames: "builtins.list[str]"
    """
    The list of alarm names that this mute rule targets.

    You can specify up to 100 alarm names.
    """


class AlarmMuteRule(PrimaryBoto3Model):
    """
    Summary information about an alarm mute rule, including its name, status, and
    configuration details.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = AlarmMuteRuleManager

    AlarmMuteRuleArn: str = Field(default=None, frozen=True)
    """
    The Amazon Resource Name (ARN) of the alarm mute rule.
    """
    ExpireDate: "datetime | None" = None
    """
    The date and time when the mute rule expires and is no longer evaluated.

    This field is only present if an expiration date was configured.
    """
    Status: Literal["SCHEDULED", "ACTIVE", "EXPIRED"] = Field(default=None, frozen=True)
    """
    The current status of the alarm mute rule.

    Valid values are ``SCHEDULED``, ``ACTIVE``, or ``EXPIRED``.
    """
    MuteType: str = Field(default=None, frozen=True)
    """
    Indicates whether the mute rule is one-time or recurring.

    Valid values are ``ONE_TIME`` or ``RECURRING``.
    """
    LastUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The date and time when the mute rule was last updated.
    """
    Name: str = Field(default=None, frozen=True)
    """
    The name of the alarm mute rule.
    """
    Description: str = Field(default=None, frozen=True)
    """
    The description of the alarm mute rule.
    """
    Rule: CloudWatchAlarmMuteRule = Field(default=None, frozen=True)
    """
    The configuration that defines when and how long alarms are muted.
    """
    MuteTargets: CloudWatchAlarmMuteTargets = Field(default=None, frozen=True)
    """
    Specifies which alarms this rule applies to.
    """
    StartDate: datetime = Field(default=None, frozen=True)
    """
    The date and time when the mute rule becomes active.

    If not set, the rule is active immediately.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.Name

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.   This is the value of the
        :py:attr:`AlarmMuteRuleArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.AlarmMuteRuleArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.   This is the value of the :py:attr:`Name`
        attribute.

        Returns:
            The name of the model instance.
        """
        return self.Name

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`Name` attribute.
        """
        return hash(self.Name)


class OTelEnrichmentStatus(ReadonlyPrimaryBoto3Model):
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = OTelEnrichmentStatusManager

    Status: Literal["Running", "Stopped"]
    """
    The status of OTel enrichment for the account.

    Valid values are ``Running`` (enrichment is enabled) and ``Stopped``
    (enrichment is disabled).
    """

    @property
    def pk(self) -> str:
        """
        Fixed singleton identity for account OTel enrichment.
        """

        return "otel-enrichment"


class StatisticSet(Boto3Model):
    """
    Represents a set of statistics that describes a specific metric.
    """

    SampleCount: float
    """
    The number of samples used for the statistic set.
    """
    Sum: float
    """
    The sum of values for the sample set.
    """
    Minimum: float
    """
    The minimum value of the sample set.
    """
    Maximum: float
    """
    The maximum value of the sample set.
    """


class MetricDatum(Boto3Model):
    """
    Encapsulates the information sent to either create a metric or add new values to be
    aggregated into an existing metric.
    """

    MetricName: str
    """
    The name of the metric.
    """
    Dimensions: "builtins.list[CloudWatchDimension] | None" = Field(
        default_factory=list
    )
    """
    The dimensions associated with the metric.
    """
    Timestamp: "datetime | None" = None
    """
    The time the metric data was received, expressed as the number of milliseconds since
    Jan 1, 1970 00:00:00 UTC.
    """
    Value: "float | None" = None
    """
    The value for the metric.
    """
    StatisticValues: "StatisticSet | None" = None
    """
    The statistical values for the metric.
    """
    Values: "builtins.list[float] | None" = Field(default_factory=list)
    """
    Array of numbers representing the values for the metric during the period.

    Each unique value is listed just once in this
    array, and the corresponding number in the ``Counts`` array specifies the number of times that value occurred during the
    period. You can include up to 150 unique values in each ``PutMetricData`` action that specifies a ``Values`` array.
    """
    Counts: "builtins.list[float] | None" = Field(default_factory=list)
    """
    Array of numbers that is used along with the ``Values`` array.

    Each number in the ``Count`` array is the number of times
    the corresponding value in the ``Values`` array occurred during the period.
    """
    Unit: "Literal['Seconds', 'Microseconds', 'Milliseconds', 'Bytes', 'Kilobytes', 'Megabytes', 'Gigabytes', 'Terabytes', 'Bits', 'Kilobits', 'Megabits', 'Gigabits', 'Terabits', 'Percent', 'Count', 'Bytes/Second', 'Kilobytes/Second', 'Megabytes/Second', 'Gigabytes/Second', 'Terabytes/Second', 'Bits/Second', 'Kilobits/Second', 'Megabits/Second', 'Gigabits/Second', 'Terabits/Second', 'Count/Second', 'None'] | None" = None
    """
    When you are using a ``Put`` operation, this defines what unit you want to use when
    storing the metric.
    """
    StorageResolution: "int | None" = None
    """
    Valid values are 1 and 60.

    Setting this to 1 specifies this metric as a high-resolution metric, so that CloudWatch
    stores the metric with sub-minute resolution down to one second. Setting this to 60 specifies this metric as a regular-
    resolution metric, which CloudWatch stores at 1-minute resolution. Currently, high resolution is available only for
    custom metrics. For more information about high-resolution metrics, see `High-Resolution
    Metrics <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/publishingMetrics.html#high-resolution-metrics>`_
    in the *Amazon CloudWatch User Guide*.
    """


class EntityMetricData(Boto3Model):
    """
    A set of metrics that are associated with an entity, such as a specific service or
    resource.

    Contains the entity and the list of metric data associated with it.
    """

    Entity: "CloudWatchEntity | None" = None
    """
    The entity associated with the metrics.
    """
    MetricData: "builtins.list[MetricDatum] | None" = Field(default_factory=list)
    """
    The metric data.
    """


class LabelOptions(Boto3Model):
    """
    This structure includes the ``Timezone`` parameter, which you can use to specify
    your time zone so that the labels that are associated with returned metrics display
    the correct time for your time zone.

    The ``Timezone`` value affects a label only if you have a time-based dynamic expression in the label. For more
    information about dynamic expressions in labels, see `Using Dynamic
    Labels <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/graph-dynamic-labels.html>`_.
    """

    Timezone: "str | None" = None
    """
    The time zone to use for metric data return in this operation.

    The format is ``+`` or ``-`` followed by four digits. The
    first two digits indicate the number of hours ahead or behind of UTC, and the final two digits are the number of
    minutes. For example, +0130 indicates a time zone that is 1 hour and 30 minutes ahead of UTC. The default is +0000.
    """


class CloudWatchMetric(CloudWatchMetricModelMixin, PrimaryBoto3Model):
    """
    Represents a specific metric.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = CloudWatchMetricManager

    Namespace: "str | None" = None
    """
    The namespace of the metric.
    """
    MetricName: "str | None" = None
    """
    The name of the metric.

    This is a required field.
    """
    Dimensions: "builtins.list[CloudWatchDimension] | None" = Field(
        default_factory=list
    )
    """
    The dimensions for the metric.
    """

    @property
    def pk(self) -> dict[str, Any]:
        """
        The composite identity for a CloudWatch metric time series.
        """

        return {
            "Namespace": self.Namespace,
            "MetricName": self.MetricName,
            "Dimensions": sorted(
                [
                    {"Name": dimension.Name, "Value": dimension.Value}
                    for dimension in (self.Dimensions or [])
                ],
                key=lambda item: item["Name"],
            ),
        }

    def publish(self, MetricData: list[Any]) -> "None":
        """
        Publish datapoints for this metric time series.

        Args:
            MetricData: The data for the metrics. Use this parameter if your metrics do not contain associated entities. The
                array can include no more than 1000 metrics per call.
        """

        return (
            cast("CloudWatchMetricManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .publish(
                self,
                MetricData,
            )
        )

    def get_data(
        self,
        StartTime: datetime,
        EndTime: datetime,
        Period: CloudWatchGetDataPeriod,
        Stat: CloudWatchGetDataStat,
        Unit: CloudWatchMetricUnit | None = None,
        ScanBy: str | None = None,
    ) -> "list[Any]":
        """
        Retrieve metric data via GetMetricData for this metric.

        Args:
            StartTime: The time stamp indicating the earliest data to be returned.
            EndTime: The time stamp indicating the latest data to be returned.
            Period: Aggregation period in seconds. Must be one of 1, 5, 10, 20, 30, 60, 120, 300, 600, 900, 1800, 3600, 10800,
                21600, 43200, or 86400.
            Stat: Statistic for the query. Standard values are SampleCount, Average, Sum, Minimum, and Maximum. Extended
                percentiles use a p prefix, for example p99 or p95.0 (p0 through p100 and common decimal forms).

        Keyword Args:
            Unit: Optional unit filter. When omitted, CloudWatch may return data stored under any unit. When set, must be a
                standard unit such as Seconds, Count, Percent, Bytes, or Bytes/Second.
            ScanBy: The order in which data points should be returned. ``TimestampDescending`` returns the newest data first and
                paginates when the ``MaxDatapoints`` limit is reached. ``TimestampAscending`` returns the oldest data first and
                paginates when the ``MaxDatapoints`` limit is reached.
        """

        return (
            cast("CloudWatchMetricManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .get_data(self, StartTime, EndTime, Period, Stat, Unit=Unit, ScanBy=ScanBy)
        )

    def get_statistics(
        self,
        StartTime: datetime,
        EndTime: datetime,
        Period: int,
        Statistics: list[str] | None = None,
        ExtendedStatistics: list[str] | None = None,
        Unit: CloudWatchMetricUnit | None = None,
    ) -> "list[Any]":
        """
        Retrieve aggregated datapoints via GetMetricStatistics.

        Args:
            StartTime: The time stamp that determines the first data point to return. Start times are evaluated relative to the
                time that CloudWatch receives the request.
            EndTime: The time stamp that determines the last data point to return.
            Period: The granularity, in seconds, of the returned data points. For metrics with regular resolution, a period can
                be as short as one minute (60 seconds) and must be a multiple of 60. For high-resolution metrics that are collected
                at intervals of less than one minute, the period can be 1, 5, 10, 20, 30, 60, or any multiple of 60. High-resolution
                metrics are those metrics stored by a ``PutMetricData`` call that includes a ``StorageResolution`` of 1 second.

        Keyword Args:
            Statistics: The metric statistics, other than percentile. For percentile statistics, use ``ExtendedStatistics``.
                When calling ``GetMetricStatistics``, you must specify either ``Statistics`` or ``ExtendedStatistics``, but not
                both.
            ExtendedStatistics: The percentile statistics. Specify values between p0.0 and p100. When calling
                ``GetMetricStatistics``, you must specify either ``Statistics`` or ``ExtendedStatistics``, but not both. Percentile
                statistics are not available for metrics when any of the metric values are negative numbers.
            Unit: The unit for a given metric. If you omit ``Unit``, all data that was collected with any unit is returned,
                along with the corresponding units that were specified when the data was reported to CloudWatch. If you specify a
                unit, the operation returns only data that was collected with that unit specified. If you specify a unit that does
                not match the data collected, the results of the operation are null. CloudWatch does not perform unit conversions.
        """

        return (
            cast("CloudWatchMetricManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .get_statistics(
                self,
                StartTime,
                EndTime,
                Period,
                Statistics=Statistics,
                ExtendedStatistics=ExtendedStatistics,
                Unit=Unit,
            )
        )

    def widget_image(self, MetricWidget: str) -> "bytes":
        """
        Render a metric widget image from JSON widget definition.

        Args:
            MetricWidget: A JSON string that defines the bitmap graph to be retrieved. The string includes the metrics to
                include in the graph, statistics, annotations, title, axis limits, and so on. You can include only one
                ``MetricWidget`` parameter in each ``GetMetricWidgetImage`` call.
        """

        return (
            cast("CloudWatchMetricManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .widget_image(
                MetricWidget,
            )
        )


class CloudWatchEntity(Boto3Model):
    """
    An entity associated with metrics, to allow for finding related telemetry.

    An entity is typically a resource or service within your system. For example,
    metrics from an Amazon EC2 instance could be associated with that instance as the
    entity. Similarly, metrics from a service that you own could be associated with that
    service as the entity.
    """

    KeyAttributes: "dict[str, str] | None" = Field(default_factory=dict)
    """
    The attributes of the entity which identify the specific entity, as a list of key-
    value pairs.

    Entities with the same
    ``KeyAttributes`` are considered to be the same entity. For an entity to be valid, the ``KeyAttributes`` must exist and
    be formatted correctly.
    """
    Attributes: "dict[str, str] | None" = Field(default_factory=dict)
    """
    Additional attributes of the entity that are not used to specify the identity of the
    entity.

    A list of key-value pairs.
    """


# =======================
# Request/Response Models
# =======================


class CloudWatchTag(Boto3Model):
    """
    A key-value pair associated with a CloudWatch resource.
    """

    Key: str
    """
    A string that you can use to assign a value.

    The combination of tag keys and values can help you organize and categorize your
    resources.
    """
    Value: str
    """
    The value for the specified tag key.
    """


class DescribeAlarmsOutput(Boto3Model):
    CompositeAlarms: "builtins.list[CompositeAlarm] | None" = Field(
        default_factory=list
    )
    """
    The information about any composite alarms returned by the operation.
    """
    MetricAlarms: "builtins.list[MetricAlarm] | None" = Field(default_factory=list)
    """
    The information about any metric alarms returned by the operation.
    """
    NextToken: "str | None" = None
    """
    The token that marks the start of the next batch of returned results.
    """


class AlarmHistoryItem(Boto3Model):
    """
    Represents the history of a specific alarm.
    """

    AlarmName: "str | None" = None
    """
    The descriptive name for the alarm.
    """
    AlarmContributorId: "str | None" = None
    """
    The unique identifier of the alarm contributor associated with this history item, if
    applicable.
    """
    AlarmType: "Literal['CompositeAlarm', 'MetricAlarm'] | None" = None
    """
    The type of alarm, either metric alarm or composite alarm.
    """
    Timestamp: "datetime | None" = None
    """
    The time stamp for the alarm history item.
    """
    HistoryItemType: "Literal['ConfigurationUpdate', 'StateUpdate', 'Action', 'AlarmContributorStateUpdate', 'AlarmContributorAction'] | None" = None
    """
    The type of alarm history item.
    """
    HistorySummary: "str | None" = None
    """
    A summary of the alarm history, in text format.
    """
    HistoryData: "str | None" = None
    """
    Data about the alarm, in JSON format.
    """
    AlarmContributorAttributes: "dict[str, str] | None" = Field(default_factory=dict)
    """
    A map of attributes that describe the alarm contributor associated with this history
    item, providing context about the contributor's characteristics at the time of the
    event.
    """


class DescribeAlarmHistoryOutput(Boto3Model):
    AlarmHistoryItems: "builtins.list[AlarmHistoryItem] | None" = Field(
        default_factory=list
    )
    """
    The alarm histories, in JSON format.
    """
    NextToken: "str | None" = None
    """
    The token that marks the start of the next batch of returned results.
    """


class CloudWatchMetricStreamFilter(Boto3Model):
    """
    This structure contains a metric namespace and optionally, a list of metric names,
    to either include in a metric stream or exclude from a metric stream.

    A metric stream's filters can include up to 1000 total names. This limit applies to
    the sum of namespace names and metric names in the filters. For example, this could
    include 10 metric namespace filters with 99 metrics each, or 20 namespace filters
    with 49 metrics specified in each filter.
    """

    Namespace: "str | None" = None
    """
    The name of the metric namespace for this filter.
    """
    MetricNames: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The names of the metrics to either include or exclude from the metric stream.
    """


class MetricStreamStatisticsMetric(Boto3Model):
    """
    This object contains the information for one metric that is to be streamed with
    additional statistics.
    """

    Namespace: str
    """
    The namespace of the metric.
    """
    MetricName: str
    """
    The name of the metric.
    """


class CloudWatchMetricStreamStatisticsConfiguration(Boto3Model):
    """
    By default, a metric stream always sends the ``MAX``, ``MIN``, ``SUM``, and
    ``SAMPLECOUNT`` statistics for each metric that is streamed.

    This structure contains information for one metric that includes additional
    statistics in the stream. For more information about statistics, see CloudWatch,
    listed in
    `CloudWatch statistics definitions <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Statistics-definitions.html.html>`_.
    """

    IncludeMetrics: "builtins.list[MetricStreamStatisticsMetric]"
    """
    An array of metric name and namespace pairs that stream the additional statistics
    listed in the value of the ``AdditionalStatistics`` parameter.

    There can be as many as 100 pairs in the array.
    """
    AdditionalStatistics: "builtins.list[str]"
    """
    The list of additional statistics that are to be streamed for the metrics listed in
    the ``IncludeMetrics`` array in this structure.

    This list can include as many as 20 statistics.
    """


class GetMetricStreamOutput(Boto3Model):
    Arn: "str | None" = None
    """
    The ARN of the metric stream.
    """
    Name: "str | None" = None
    """
    The name of the metric stream.
    """
    IncludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = Field(
        default_factory=list
    )
    """
    If this array of metric namespaces is present, then these namespaces are the only
    metric namespaces that are streamed by this metric stream.
    """
    ExcludeFilters: "builtins.list[CloudWatchMetricStreamFilter] | None" = Field(
        default_factory=list
    )
    """
    If this array of metric namespaces is present, then these namespaces are the only
    metric namespaces that are not streamed by this metric stream.

    In this case, all other metric namespaces in the account are streamed by this metric
    stream.
    """
    FirehoseArn: "str | None" = None
    """
    The ARN of the Amazon Kinesis Data Firehose delivery stream that is used by this
    metric stream.
    """
    RoleArn: "str | None" = None
    """
    The ARN of the IAM role that is used by this metric stream.
    """
    State: "str | None" = None
    """
    The state of the metric stream.

    The possible values are ``running`` and ``stopped``.
    """
    CreationDate: "datetime | None" = None
    """
    The date that the metric stream was created.
    """
    LastUpdateDate: "datetime | None" = None
    """
    The date of the most recent update to the metric stream's configuration.
    """
    OutputFormat: "Literal['json', 'opentelemetry0.7', 'opentelemetry1.0'] | None" = (
        None
    )
    """
    The output format for the stream.

    Valid values are ``json``, ``opentelemetry1.0``, and ``opentelemetry0.7``. For more
    information about metric stream output formats, see `Metric streams output
    formats <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-metric-streams-formats.html>`_.
    """
    StatisticsConfigurations: "builtins.list[CloudWatchMetricStreamStatisticsConfiguration] | None" = Field(
        default_factory=list
    )
    """
    Each entry in this array displays information about one or more metrics that include
    additional statistics in the metric stream.

    For more information about the additional statistics, see
    `CloudWatch statistics definitions <https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Statistics-definitions.html.html>`_.
    """
    IncludeLinkedAccountsMetrics: "bool | None" = None
    """
    If this is ``true`` and this metric stream is in a monitoring account, then the
    stream includes metrics from source accounts that the monitoring account is linked
    to.
    """


class ListMetricStreamsOutput(Boto3Model):
    NextToken: "str | None" = None
    """
    The token that marks the start of the next batch of returned results.

    You can use this token in a subsequent operation to get the next batch of results.
    """
    Entries: "builtins.list[CloudWatchMetricStream] | None" = Field(
        default_factory=list
    )
    """
    The array of metric stream information.
    """


class DeleteMetricStreamOutput(Boto3Model):
    pass


class StartMetricStreamsOutput(Boto3Model):
    pass


class StopMetricStreamsOutput(Boto3Model):
    pass


class GetDashboardOutput(Boto3Model):
    DashboardArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the dashboard.
    """
    DashboardBody: "str | None" = None
    """
    The detailed information about the dashboard, including what widgets are included
    and their location on the dashboard.

    For more information about the ``DashboardBody`` syntax, see `Dashboard Body Structure and
    Syntax <https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/CloudWatch-Dashboard-Body-Structure.html>`_.
    """
    DashboardName: "str | None" = None
    """
    The name of the dashboard.
    """


class ListDashboardsOutput(Boto3Model):
    DashboardEntries: "builtins.list[CloudWatchDashboard] | None" = Field(
        default_factory=list
    )
    """
    The list of matching dashboards.
    """
    NextToken: "str | None" = None
    """
    The token that marks the start of the next batch of returned results.
    """


class DeleteDashboardsOutput(Boto3Model):
    pass


class PartialFailure(Boto3Model):
    """
    This array is empty if the API operation was successful for all the rules specified
    in the request.

    If the operation could not process one of the rules, the following data is returned
    for each of those rules.
    """

    FailureResource: "str | None" = None
    """
    The specified rule that could not be deleted.
    """
    ExceptionType: "str | None" = None
    """
    The type of error.
    """
    FailureCode: "str | None" = None
    """
    The code of the error.
    """
    FailureDescription: "str | None" = None
    """
    A description of the error.
    """


class DeleteInsightRulesOutput(Boto3Model):
    Failures: "builtins.list[PartialFailure] | None" = Field(default_factory=list)
    """
    An array listing the rules that could not be deleted.

    You cannot delete built-in rules.
    """


class EnableInsightRulesOutput(Boto3Model):
    Failures: "builtins.list[PartialFailure] | None" = Field(default_factory=list)
    """
    An array listing the rules that could not be enabled.

    You cannot disable or enable built-in rules.
    """


class DisableInsightRulesOutput(Boto3Model):
    Failures: "builtins.list[PartialFailure] | None" = Field(default_factory=list)
    """
    An array listing the rules that could not be disabled.

    You cannot disable built-in rules.
    """


class DescribeAnomalyDetectorsOutput(Boto3Model):
    AnomalyDetectors: "builtins.list[CloudWatchAnomalyDetector] | None" = Field(
        default_factory=list
    )
    """
    The list of anomaly detection models returned by the operation.
    """
    NextToken: "str | None" = None
    """
    A token that you can use in a subsequent operation to retrieve the next set of
    results.
    """


class DimensionFilter(Boto3Model):
    """
    Represents filters for a dimension.
    """

    Name: str
    """
    The dimension name to be matched.
    """
    Value: "str | None" = None
    """
    The value of the dimension to be matched.
    """


class ListMetricsOutput(Boto3Model):
    Metrics: "builtins.list[CloudWatchMetric] | None" = Field(default_factory=list)
    """
    The metrics that match your request.
    """
    NextToken: "str | None" = None
    """
    The token that marks the start of the next batch of returned results.
    """
    OwningAccounts: "builtins.list[str] | None" = Field(default_factory=list)
    """
    If you are using this operation in a monitoring account, this array contains the
    account IDs of the source accounts where the metrics in the returned data are from.
    """


class GetAlarmMuteRuleOutput(Boto3Model):
    Name: "str | None" = None
    """
    The name of the alarm mute rule.
    """
    AlarmMuteRuleArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the alarm mute rule.
    """
    Description: "str | None" = None
    """
    The description of the alarm mute rule.
    """
    Rule: "CloudWatchAlarmMuteRule | None" = None
    """
    The configuration that defines when and how long alarms are muted.
    """
    MuteTargets: "CloudWatchAlarmMuteTargets | None" = None
    """
    Specifies which alarms this rule applies to.
    """
    StartDate: "datetime | None" = None
    """
    The date and time when the mute rule becomes active.

    If not set, the rule is active immediately.
    """
    ExpireDate: "datetime | None" = None
    """
    The date and time when the mute rule expires and is no longer evaluated.
    """
    Status: "Literal['SCHEDULED', 'ACTIVE', 'EXPIRED'] | None" = None
    """
    The current status of the alarm mute rule.

    Valid values are ``SCHEDULED``, ``ACTIVE``, or ``EXPIRED``.
    """
    LastUpdatedTimestamp: "datetime | None" = None
    """
    The date and time when the mute rule was last updated.
    """
    MuteType: "str | None" = None
    """
    Indicates whether the mute rule is one-time or recurring.

    Valid values are ``ONE_TIME`` or ``RECURRING``.
    """


class ListAlarmMuteRulesOutput(Boto3Model):
    AlarmMuteRuleSummaries: "builtins.list[AlarmMuteRule] | None" = Field(
        default_factory=list
    )
    """
    A list of alarm mute rule summaries.
    """
    NextToken: "str | None" = None
    """
    The token to use when requesting the next set of results.

    If this field is absent, there are no more results to retrieve.
    """


class StartOTelEnrichmentOutput(Boto3Model):
    pass


class StopOTelEnrichmentOutput(Boto3Model):
    pass
