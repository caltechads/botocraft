# This file is automatically generated by botocraft.  Do not edit directly.
# mypy: disable-error-code="index, override, assignment, union-attr, misc"
from pydantic import Field
from typing import cast
from botocraft.mixins.sesv2 import refresh_multi_region_endpoint_after_mutation
from botocraft.mixins.sesv2 import multi_region_endpoint_response_to_model
from botocraft.mixins.sesv2 import refresh_dedicated_ip_pool_after_mutation
from botocraft.mixins.sesv2 import SESV2EmailIdentityManagerMixin
from botocraft.mixins.sesv2 import tenant_response_to_model
import builtins
from .abstract import (
    Boto3Model,
    ReadonlyBoto3Model,
    PrimaryBoto3Model,
    ReadonlyPrimaryBoto3Model,
    Boto3ModelManager,
    ReadonlyBoto3ModelManager,
)
from botocraft.mixins.sesv2 import refresh_suppressed_destination_after_mutation
from botocraft.mixins.sesv2 import SESV2DeliverabilityTestReportManagerMixin
from .abstract import PrimaryBoto3ModelQuerySet
from typing import Any
from botocraft.mixins.tags import TagsDictMixin
from typing import ClassVar, Literal, Type as ModelType
from botocraft.mixins.sesv2 import refresh_contact_list_after_mutation
from botocraft.mixins.sesv2 import configuration_set_names_to_models
from botocraft.mixins.sesv2 import refresh_email_template_after_mutation
from datetime import datetime
from botocraft.mixins.sesv2 import dedicated_ip_pool_names_to_models
from botocraft.mixins.sesv2 import refresh_configuration_set_after_mutation

# ===============
# Managers
# ===============


class ConfigurationSetManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_configuration_set_after_mutation
    def create(self, model: "ConfigurationSet") -> None:
        """
        Create a configuration set.

        *Configuration sets* are groups of rules that you can apply to the emails that
        you send. You apply a configuration set to an email by specifying the name of
        the configuration set when you call the Amazon SES API
        v2. When you apply a configuration set to an email, all of the rules in that
        configuration set are applied to the email.

        Args:
            model: The :py:class:`GetConfigurationSetResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ConfigurationSetName=data.get("ConfigurationSetName"),
            TrackingOptions=data.get("TrackingOptions"),
            DeliveryOptions=data.get("DeliveryOptions"),
            ReputationOptions=data.get("ReputationOptions"),
            SendingOptions=data.get("SendingOptions"),
            Tags=data.get("Tags"),
            SuppressionOptions=data.get("SuppressionOptions"),
            VdmOptions=data.get("VdmOptions"),
            ArchivingOptions=data.get("ArchivingOptions"),
        )
        self.client.create_configuration_set(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, ConfigurationSetName: str) -> "ConfigurationSet | None":
        """
        Get information about an existing configuration set, including the dedicated IP
        pool that it's associated with, whether or not it's enabled for sending email,
        and more.

        Args:
            ConfigurationSetName: The name of the configuration set.
        """
        args: dict[str, Any] = dict(
            ConfigurationSetName=self.serialize(ConfigurationSetName)
        )
        _response = self.client.get_configuration_set(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ConfigurationSet(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @configuration_set_names_to_models
    def list(
        self, *, NextToken: "str | None" = None, PageSize: "int | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List all of the configuration sets associated with your account in the current
        region.

        Keyword Args:
            NextToken: A token returned from a previous call to
                ``ListConfigurationSets`` to indicate the position in the list
                of configuration sets.
            PageSize: The number of results to show in a single call to
                ``ListConfigurationSets``. If the number of results is
                larger than the number you specified in this parameter, then the
                response includes a ``NextToken`` element, which
                you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            NextToken=self.serialize(NextToken), PageSize=self.serialize(PageSize)
        )
        _response = self.client.list_configuration_sets(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListConfigurationSetsResponse(**_response)
        if response and response.ConfigurationSets:
            self.sessionize(response.ConfigurationSets)
            return PrimaryBoto3ModelQuerySet(response.ConfigurationSets)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, ConfigurationSetName: str) -> None:
        """
        Delete an existing configuration set.

        Args:
            ConfigurationSetName: The name of the configuration set.
        """
        args: dict[str, Any] = dict(
            ConfigurationSetName=self.serialize(ConfigurationSetName)
        )
        self.client.delete_configuration_set(
            **{k: v for k, v in args.items() if v is not None}
        )


class EmailIdentityManager(SESV2EmailIdentityManagerMixin, Boto3ModelManager):
    service_name: str = "sesv2"

    def send(
        self,
        FromEmailAddress: str,
        Content: "SESV2EmailContent",
        *,
        FromEmailAddressIdentityArn: "str | None" = None,
        Destination: "SESV2Destination | None" = None,
        ReplyToAddresses: "builtins.list[str] | None" = None,
        FeedbackForwardingEmailAddress: "str | None" = None,
        FeedbackForwardingEmailAddressIdentityArn: "str | None" = None,
        EmailTags: "builtins.list[SESV2MessageTag] | None" = None,
        ConfigurationSetName: "str | None" = None,
        EndpointId: "str | None" = None,
        TenantName: "str | None" = None,
        ListManagementOptions: "SESV2ListManagementOptions | None" = None,
    ) -> str:
        """
        Sends an email message.

        You can use the Amazon SES API v2 to send simple, raw, or templated messages.

        Args:
            FromEmailAddress: The email address to use as the "From" address for the
                email. The address that you specify has to be verified.
            Content: An object that contains the body of the message. You can send
                either a simple message, raw message, or templated message.

        Keyword Args:
            FromEmailAddressIdentityArn: This parameter is used only for sending
                authorization. It is the ARN of the identity that is associated with
                the sending authorization policy that permits you to use the email
                address specified in the ``FromEmailAddress`` parameter.
            Destination: An object that contains the recipients of the email message.
            ReplyToAddresses: The "Reply-to" email addresses for the message. When the
                recipient replies to the message, each reply-to address receives the
                reply.
            FeedbackForwardingEmailAddress: The address that you want bounce and
                complaint notifications to be sent to.
            FeedbackForwardingEmailAddressIdentityArn: This parameter is used only for
                sending authorization. It is the ARN of the identity that is associated
                with the sending authorization policy that permits you to use the email
                address specified in the
                ``FeedbackForwardingEmailAddress`` parameter.
            EmailTags: A list of tags, in the form of name/value pairs, to apply to an
                email that you send using the ``SendEmail`` operation. Tags correspond
                to characteristics of the email that you define, so that you can publish
                email sending events.
            ConfigurationSetName: The name of the configuration set to use when sending
                the email.
            ListManagementOptions: An object used to specify a list or topic to which
                an email belongs, which will be used when a contact chooses to
                unsubscribe.
            EndpointId: The ID of the multi-region endpoint (global-endpoint).
            TenantName: The name of the tenant through which this email will be sent.
                    ListManagementOptions: An object used to specify a list or topic to
                        which an email belongs, which will be used when
                        a contact chooses to unsubscribe.
        """
        args: dict[str, Any] = dict(
            Content=self.serialize(Content),
            FromEmailAddress=self.serialize(FromEmailAddress),
            FromEmailAddressIdentityArn=self.serialize(FromEmailAddressIdentityArn),
            Destination=self.serialize(Destination),
            ReplyToAddresses=self.serialize(ReplyToAddresses),
            FeedbackForwardingEmailAddress=self.serialize(
                FeedbackForwardingEmailAddress
            ),
            FeedbackForwardingEmailAddressIdentityArn=self.serialize(
                FeedbackForwardingEmailAddressIdentityArn
            ),
            EmailTags=self.serialize(EmailTags),
            ConfigurationSetName=self.serialize(ConfigurationSetName),
            EndpointId=self.serialize(EndpointId),
            TenantName=self.serialize(TenantName),
            ListManagementOptions=self.serialize(ListManagementOptions),
        )
        _response = self.client.send_email(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = SendEmailResponse(**_response)

        results: str = None
        if response is not None:
            results = response.MessageId

        self.sessionize(results)
        return cast("str", results)

    def send_bulk(
        self,
        FromEmailAddress: str,
        DefaultContent: "SESV2BulkEmailContent",
        BulkEmailEntries: "builtins.list[SESV2BulkEmailEntry]",
        *,
        FromEmailAddressIdentityArn: "str | None" = None,
        ReplyToAddresses: "builtins.list[str] | None" = None,
        FeedbackForwardingEmailAddress: "str | None" = None,
        FeedbackForwardingEmailAddressIdentityArn: "str | None" = None,
        DefaultEmailTags: "builtins.list[SESV2MessageTag] | None" = None,
        ConfigurationSetName: "str | None" = None,
        EndpointId: "str | None" = None,
        TenantName: "str | None" = None,
    ) -> "builtins.list[BulkEmailEntryResult]":
        """
        Composes an email message to multiple destinations.

        Args:
            FromEmailAddress: The email address to use as the "From" address for the
                email. The address that you specify has to
                be verified.
            DefaultContent: An object that contains the body of the message. You can
                specify a template message.
            BulkEmailEntries: The list of bulk email entry objects.

        Keyword Args:
            FromEmailAddressIdentityArn: This parameter is used only for sending
                authorization. It is the ARN of the identity
                that is associated with the sending authorization policy that permits
                you to use the email address specified in the
                ``FromEmailAddress`` parameter.
            ReplyToAddresses: The "Reply-to" email addresses for the message. When the
                recipient replies to the message, each
                Reply-to address receives the reply.
            FeedbackForwardingEmailAddress: The address that you want bounce and
                complaint notifications to be sent to.
            FeedbackForwardingEmailAddressIdentityArn: This parameter is used only for
                sending authorization. It is the ARN of
                the identity that is associated with the sending authorization policy
                that permits you to use the email address
                specified in the ``FeedbackForwardingEmailAddress`` parameter.
            DefaultEmailTags: A list of tags, in the form of name/value pairs, to apply
                to an email that you send using the
                ``SendEmail`` operation. Tags correspond to characteristics of the email
                that you define, so that you can publish
                email sending events.
            ConfigurationSetName: The name of the configuration set to use when sending
                the email.
            EndpointId: The ID of the multi-region endpoint (global-endpoint).
            TenantName: The name of the tenant through which this bulk email will be
                sent.
        """
        args: dict[str, Any] = dict(
            DefaultContent=self.serialize(DefaultContent),
            BulkEmailEntries=self.serialize(BulkEmailEntries),
            FromEmailAddress=self.serialize(FromEmailAddress),
            FromEmailAddressIdentityArn=self.serialize(FromEmailAddressIdentityArn),
            ReplyToAddresses=self.serialize(ReplyToAddresses),
            FeedbackForwardingEmailAddress=self.serialize(
                FeedbackForwardingEmailAddress
            ),
            FeedbackForwardingEmailAddressIdentityArn=self.serialize(
                FeedbackForwardingEmailAddressIdentityArn
            ),
            DefaultEmailTags=self.serialize(DefaultEmailTags),
            ConfigurationSetName=self.serialize(ConfigurationSetName),
            EndpointId=self.serialize(EndpointId),
            TenantName=self.serialize(TenantName),
        )
        _response = self.client.send_bulk_email(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = SendBulkEmailResponse(**_response)

        results: "builtins.list[BulkEmailEntryResult]" = None
        if response is not None:
            results = response.BulkEmailEntryResults

        self.sessionize(results)
        return cast("builtins.list[BulkEmailEntryResult]", results)


class ContactListManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_contact_list_after_mutation
    def create(self, model: "ContactList") -> None:
        """
        Creates a contact list.

        Args:
            model: The :py:class:`GetContactListResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ContactListName=data.get("ContactListName"),
            Topics=data.get("Topics"),
            Description=data.get("Description"),
            Tags=data.get("Tags"),
        )
        self.client.create_contact_list(
            **{k: v for k, v in args.items() if v is not None}
        )

    @refresh_contact_list_after_mutation
    def update(self, model: "ContactList") -> None:
        """
        Updates contact list metadata.

        This operation does a complete replacement.

        Args:
                    model: The :py:class:`GetContactListResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ContactListName=data.get("ContactListName"),
            Topics=data.get("Topics"),
            Description=data.get("Description"),
        )
        self.client.update_contact_list(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, ContactListName: str) -> "ContactList | None":
        """
        Returns contact list metadata.

        It does not return any information about the contacts present in the list.

        Args:
                    ContactListName: The name of the contact list.
        """
        args: dict[str, Any] = dict(ContactListName=self.serialize(ContactListName))
        _response = self.client.get_contact_list(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ContactList(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self, *, PageSize: "int | None" = None, NextToken: "str | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Lists all of the contact lists available.

        Keyword Args:
            PageSize: Maximum number of contact lists to return at once. Use this
                parameter to paginate results. If additional
                contact lists exist beyond the specified limit, the ``NextToken``
                element is sent in the response. Use the
                ``NextToken`` value in subsequent requests to retrieve additional lists.
            NextToken: A string token indicating that there might be additional contact
                lists available to be listed. Use the
                token provided in the Response to use in the subsequent call to
                ListContactLists with the same parameters to
                retrieve the next page of contact lists.
        """
        args: dict[str, Any] = dict(
            PageSize=self.serialize(PageSize), NextToken=self.serialize(NextToken)
        )
        _response = self.client.list_contact_lists(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListContactListsResponse(**_response)
        if response and response.ContactLists:
            self.sessionize(response.ContactLists)
            return PrimaryBoto3ModelQuerySet(response.ContactLists)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, ContactListName: str) -> None:
        """
        Deletes a contact list and all of the contacts on that list.

        Args:
            ContactListName: The name of the contact list.
        """
        args: dict[str, Any] = dict(ContactListName=self.serialize(ContactListName))
        self.client.delete_contact_list(
            **{k: v for k, v in args.items() if v is not None}
        )


class EmailTemplateManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_email_template_after_mutation
    def create(self, model: "EmailTemplate") -> None:
        """
        Creates an email template.

        Email templates enable you to send personalized email to one or more
        destinations in a single API operation. For more information, see the `Amazon
        SES Developer
        Guide <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/send-personalized-email-api.html>`_.

        Args:
            model: The :py:class:`GetEmailTemplateResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            TemplateName=data.get("TemplateName"),
            TemplateContent=data.get("TemplateContent"),
            Tags=data.get("Tags"),
        )
        self.client.create_email_template(
            **{k: v for k, v in args.items() if v is not None}
        )

    @refresh_email_template_after_mutation
    def update(self, model: "EmailTemplate") -> None:
        """
        Updates an email template.

        Email templates enable you to send personalized email to one or more
        destinations in a single API operation. For more information, see the `Amazon
        SES Developer
        Guide <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/send-personalized-email-api.html>`_.

        Args:
            model: The :py:class:`GetEmailTemplateResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            TemplateName=data.get("TemplateName"),
            TemplateContent=data.get("TemplateContent"),
        )
        self.client.update_email_template(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, TemplateName: str) -> "EmailTemplate | None":
        """
        Displays the template object (which includes the subject line, HTML part and
        text part) for the template you specify.

        Args:
            TemplateName: The name of the template.
        """
        args: dict[str, Any] = dict(TemplateName=self.serialize(TemplateName))
        _response = self.client.get_email_template(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = EmailTemplate(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self, *, NextToken: "str | None" = None, PageSize: "int | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Lists the email templates present in your Amazon SES account in the current
        Amazon Web Services Region.

        Keyword Args:
            NextToken: A token returned from a previous call to ``ListEmailTemplates``
                to indicate the position in the list of
                email templates.
            PageSize: The number of results to show in a single call to
                ``ListEmailTemplates``. If the number of results is
                larger than the number you specified in this parameter, then the
                response includes a ``NextToken`` element, which
                you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            NextToken=self.serialize(NextToken), PageSize=self.serialize(PageSize)
        )
        _response = self.client.list_email_templates(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListEmailTemplatesResponse(**_response)
        if response and response.TemplatesMetadata:
            self.sessionize(response.TemplatesMetadata)
            return PrimaryBoto3ModelQuerySet(response.TemplatesMetadata)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, TemplateName: str) -> None:
        """
        Deletes an email template.

        Args:
            TemplateName: The name of the template to be deleted.
        """
        args: dict[str, Any] = dict(TemplateName=self.serialize(TemplateName))
        self.client.delete_email_template(
            **{k: v for k, v in args.items() if v is not None}
        )


class SuppressedDestinationManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_suppressed_destination_after_mutation
    def create(self, model: "SuppressedDestination") -> None:
        """
        Adds an email address to the suppression list for your account.

        Args:
            model: The :py:class:`SuppressedDestination` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(EmailAddress=data.get("EmailAddress"), Reason=data.get("Reason"))
        self.client.put_suppressed_destination(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, EmailAddress: str) -> "SuppressedDestination | None":
        """
        Retrieves information about a specific email address that's on the suppression
        list for your account.

        Args:
            EmailAddress: The email address that's on the account suppression list.
        """
        args: dict[str, Any] = dict(EmailAddress=self.serialize(EmailAddress))
        _response = self.client.get_suppressed_destination(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = GetSuppressedDestinationResponse(**_response)

        if response and response.SuppressedDestinationInstance:
            self.sessionize(response.SuppressedDestinationInstance)
            return response.SuppressedDestinationInstance
        return None

    def list(
        self,
        *,
        Reasons: "builtins.list[Literal['BOUNCE', 'COMPLAINT']] | None" = None,
        StartDate: "datetime | None" = None,
        EndDate: "datetime | None" = None,
        NextToken: "str | None" = None,
        PageSize: "int | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Retrieves a list of email addresses that are on the suppression list for your
        account.

        Keyword Args:
            Reasons: The factors that caused the email address to be added to .
            StartDate: Used to filter the list of suppressed email destinations so that
                it only includes addresses that were
                added to the list after a specific date.
            EndDate: Used to filter the list of suppressed email destinations so that it
                only includes addresses that were added
                to the list before a specific date.
            NextToken: A token returned from a previous call to
                ``ListSuppressedDestinations`` to indicate the position in the
                list of suppressed email addresses.
            PageSize: The number of results to show in a single call to
                ``ListSuppressedDestinations``. If the number of results
                is larger than the number you specified in this parameter, then the
                response includes a ``NextToken`` element, which
                you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            Reasons=self.serialize(Reasons),
            StartDate=self.serialize(StartDate),
            EndDate=self.serialize(EndDate),
            NextToken=self.serialize(NextToken),
            PageSize=self.serialize(PageSize),
        )
        _response = self.client.list_suppressed_destinations(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListSuppressedDestinationsResponse(**_response)
        if response and response.SuppressedDestinationSummaries:
            self.sessionize(response.SuppressedDestinationSummaries)
            return PrimaryBoto3ModelQuerySet(response.SuppressedDestinationSummaries)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, EmailAddress: str) -> None:
        """
        Removes an email address from the suppression list for your account.

        Args:
            EmailAddress: The suppressed email destination to remove from the account
                suppression list.
        """
        args: dict[str, Any] = dict(EmailAddress=self.serialize(EmailAddress))
        self.client.delete_suppressed_destination(
            **{k: v for k, v in args.items() if v is not None}
        )


class DeliverabilityTestReportManager(
    SESV2DeliverabilityTestReportManagerMixin, Boto3ModelManager
):
    service_name: str = "sesv2"

    def list(
        self, *, NextToken: "str | None" = None, PageSize: "int | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Show a list of the predictive inbox placement tests that you've performed,
        regardless of their statuses.

        For predictive inbox placement tests that are complete, you can use the
        ``GetDeliverabilityTestReport`` operation to view the results.

        Keyword Args:
            NextToken: A token returned from a previous call to
                ``ListDeliverabilityTestReports`` to indicate the position in
                the list of predictive inbox placement tests.
            PageSize: The number of results to show in a single call to
                ``ListDeliverabilityTestReports``. If the number of
                results is larger than the number you specified in this parameter, then
                the response includes a ``NextToken``
                element, which you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            NextToken=self.serialize(NextToken), PageSize=self.serialize(PageSize)
        )
        _response = self.client.list_deliverability_test_reports(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListDeliverabilityTestReportsResponse(**_response)
        if response and response.DeliverabilityTestReports:
            self.sessionize(response.DeliverabilityTestReports)
            return PrimaryBoto3ModelQuerySet(response.DeliverabilityTestReports)
        return PrimaryBoto3ModelQuerySet([])


class MultiRegionEndpointManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_multi_region_endpoint_after_mutation
    def create(
        self,
        model: "MultiRegionEndpoint",
        Tags: "builtins.list[SESV2Tag] | None" = None,
    ) -> None:
        """
        Creates a multi-region endpoint (global-endpoint).

        Args:
            model: The :py:class:`MultiRegionEndpoint` to create.

        Keyword Args:
            Tags: An array of objects that define the tags (keys and values) to
                associate with the multi-region endpoint
                (global- endpoint).
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            EndpointName=data.get("EndpointName"),
            Details=data.get("Details"),
            Tags=self.serialize(Tags),
        )
        self.client.create_multi_region_endpoint(
            **{k: v for k, v in args.items() if v is not None}
        )

    @multi_region_endpoint_response_to_model
    def get(self, EndpointName: str) -> "MultiRegionEndpoint | None":
        """
        Displays the multi-region endpoint (global-endpoint) configuration.

        Args:
            EndpointName: The name of the multi-region endpoint (global-endpoint).
        """
        args: dict[str, Any] = dict(EndpointName=self.serialize(EndpointName))
        _response = self.client.get_multi_region_endpoint(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = MultiRegionEndpoint(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(
        self,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List the multi-region endpoints (global-endpoints).
        """
        paginator = self.client.get_paginator("list_multi_region_endpoints")
        args: dict[str, Any] = dict()
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListMultiRegionEndpointsResponse(**_response)
            if response.MultiRegionEndpoints:
                results.extend(response.MultiRegionEndpoints)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, EndpointName: str) -> None:
        """
        Deletes a multi-region endpoint (global-endpoint).

        Args:
            EndpointName: The name of the multi-region endpoint (global-endpoint) to be
                deleted.
        """
        args: dict[str, Any] = dict(EndpointName=self.serialize(EndpointName))
        self.client.delete_multi_region_endpoint(
            **{k: v for k, v in args.items() if v is not None}
        )


class TenantManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @tenant_response_to_model
    def create(self, model: "Tenant") -> "CreateTenantResponse":
        """
        Create a tenant.

        Args:
            model: The :py:class:`Tenant` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(TenantName=data.get("TenantName"), Tags=data.get("Tags"))
        _response = self.client.create_tenant(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateTenantResponse(**_response)

        self.sessionize(response)
        return cast("CreateTenantResponse", response)

    def get(self, TenantName: str) -> "Tenant | None":
        """
        Get information about a specific tenant, including the tenant's name, ID, ARN,
        creation timestamp, tags, and sending status.

        Args:
            TenantName: The name of the tenant to retrieve information about.
        """
        args: dict[str, Any] = dict(TenantName=self.serialize(TenantName))
        _response = self.client.get_tenant(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = GetTenantResponse(**_response)

        if response and response.TenantInstance:
            self.sessionize(response.TenantInstance)
            return response.TenantInstance
        return None

    def list(
        self,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List all tenants associated with your account in the current Amazon Web Services
        Region.
        """
        paginator = self.client.get_paginator("list_tenants")
        args: dict[str, Any] = dict()
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListTenantsResponse(**_response)
            if response.Tenants:
                results.extend(response.Tenants)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    def delete(self, TenantName: str) -> None:
        """
        Delete an existing tenant.

        Args:
            TenantName: The name of the tenant to delete.
        """
        args: dict[str, Any] = dict(TenantName=self.serialize(TenantName))
        self.client.delete_tenant(**{k: v for k, v in args.items() if v is not None})


class DedicatedIpPoolManager(Boto3ModelManager):
    service_name: str = "sesv2"

    @refresh_dedicated_ip_pool_after_mutation
    def create(
        self, model: "DedicatedIpPool", Tags: "builtins.list[SESV2Tag] | None" = None
    ) -> None:
        """
        Create a new pool of dedicated IP addresses.

        A pool can include one or more dedicated IP addresses that are associated with
        your Amazon Web Services account. You can associate a pool with a configuration
        set. When you send an email that
        uses that configuration set, the message is sent from one of the addresses in
        the associated pool.

        Args:
            model: The :py:class:`DedicatedIpPool` to create.

        Keyword Args:
            Tags: An object that defines the tags (keys and values) that you want to
                associate with the pool.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            PoolName=data.get("PoolName"),
            Tags=self.serialize(Tags),
            ScalingMode=data.get("ScalingMode"),
        )
        self.client.create_dedicated_ip_pool(
            **{k: v for k, v in args.items() if v is not None}
        )

    @refresh_dedicated_ip_pool_after_mutation
    def update(self, model: "DedicatedIpPool") -> None:
        """
        Used to convert a dedicated IP pool to a different scaling mode.

        Args:
            model: The :py:class:`DedicatedIpPool` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(PoolName=data.get("PoolName"), ScalingMode=data.get("ScalingMode"))
        self.client.put_dedicated_ip_pool_scaling_attributes(
            **{k: v for k, v in args.items() if v is not None}
        )

    def get(self, PoolName: str) -> "DedicatedIpPool | None":
        """
        Retrieve information about the dedicated pool.

        Args:
            PoolName: The name of the dedicated IP pool to retrieve.
        """
        args: dict[str, Any] = dict(PoolName=self.serialize(PoolName))
        _response = self.client.get_dedicated_ip_pool(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = GetDedicatedIpPoolResponse(**_response)

        if response and response.DedicatedIpPoolInstance:
            self.sessionize(response.DedicatedIpPoolInstance)
            return response.DedicatedIpPoolInstance
        return None

    @dedicated_ip_pool_names_to_models
    def list(
        self, *, NextToken: "str | None" = None, PageSize: "int | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List all of the dedicated IP pools that exist in your Amazon Web Services
        account in the current Region.

        Keyword Args:
            NextToken: A token returned from a previous call to ``ListDedicatedIpPools``
                to indicate the position in the list of
                dedicated IP pools.
            PageSize: The number of results to show in a single call to
                ``ListDedicatedIpPools``. If the number of results is
                larger than the number you specified in this parameter, then the
                response includes a ``NextToken`` element, which
                you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            NextToken=self.serialize(NextToken), PageSize=self.serialize(PageSize)
        )
        _response = self.client.list_dedicated_ip_pools(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = ListDedicatedIpPoolsResponse(**_response)
        if response and response.DedicatedIpPools:
            self.sessionize(response.DedicatedIpPools)
            return PrimaryBoto3ModelQuerySet(response.DedicatedIpPools)
        return PrimaryBoto3ModelQuerySet([])

    def delete(self, PoolName: str) -> None:
        """
        Delete a dedicated IP pool.

        Args:
            PoolName: The name of the dedicated IP pool that you want to delete.
        """
        args: dict[str, Any] = dict(PoolName=self.serialize(PoolName))
        self.client.delete_dedicated_ip_pool(
            **{k: v for k, v in args.items() if v is not None}
        )


class DedicatedIpManager(Boto3ModelManager):
    service_name: str = "sesv2"

    def get(self, Ip: str) -> "DedicatedIp | None":
        """
        Get information about a dedicated IP address, including the name of the
        dedicated IP pool that it's associated with, as well information about the
        automatic warm-up process for the address.

        Args:
            Ip: The IP address that you want to obtain more information about. The value
                you specify has to be a dedicated IP
                address that's assocaited with your Amazon Web Services account.
        """
        args: dict[str, Any] = dict(Ip=self.serialize(Ip))
        _response = self.client.get_dedicated_ip(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = GetDedicatedIpResponse(**_response)

        if response and response.DedicatedIpInstance:
            self.sessionize(response.DedicatedIpInstance)
            return response.DedicatedIpInstance
        return None

    def list(
        self,
        *,
        PoolName: "str | None" = None,
        NextToken: "str | None" = None,
        PageSize: "int | None" = None,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        List the dedicated IP addresses that are associated with your Amazon Web
        Services account.

        Keyword Args:
            PoolName: The name of the IP pool that the dedicated IP address is
                associated with.
            NextToken: A token returned from a previous call to ``GetDedicatedIps`` to
                indicate the position of the dedicated IP
                pool in the list of IP pools.
            PageSize: The number of results to show in a single call to
                ``GetDedicatedIpsRequest``. If the number of results is
                larger than the number you specified in this parameter, then the
                response includes a ``NextToken`` element, which
                you can use to obtain additional results.
        """
        args: dict[str, Any] = dict(
            PoolName=self.serialize(PoolName),
            NextToken=self.serialize(NextToken),
            PageSize=self.serialize(PageSize),
        )
        _response = self.client.get_dedicated_ips(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = GetDedicatedIpsResponse(**_response)
        if response and response.DedicatedIps:
            self.sessionize(response.DedicatedIps)
            return PrimaryBoto3ModelQuerySet(response.DedicatedIps)
        return PrimaryBoto3ModelQuerySet([])


# ==============
# Service Models
# ==============


class SESV2TrackingOptions(Boto3Model):
    """
    An object that defines the tracking options for a configuration set.

    When you use the Amazon SES API v2 to send an email, it contains an invisible image
    that's used to track when recipients open your email. If your email contains links,
    those links are changed slightly in order to track when recipients click them.

    These images and links include references to a domain operated by Amazon Web
    Services. You can optionally configure the Amazon SES to use a domain that you
    operate for these images and links.
    """

    CustomRedirectDomain: str
    """
    The domain to use for tracking open and click events.
    """

    HttpsPolicy: "Literal['REQUIRE', 'REQUIRE_OPEN_ONLY', 'OPTIONAL'] | None" = None
    """
    The https policy to use for tracking open and click events.
    """

class SESV2DeliveryOptions(Boto3Model):
    """
    Used to associate a configuration set with a dedicated IP pool.
    """

    TlsPolicy: "Literal['REQUIRE', 'OPTIONAL'] | None" = None
    """
    Specifies whether messages that use the configuration set are required to use
    Transport Layer Security (TLS).

    If the value is ``Require``, messages are only delivered if a TLS connection can be
    established. If the value is ``Optional``, messages can be delivered in plain text
    if a TLS connection can't be established.
    """

    SendingPoolName: "str | None" = None
    """
    The name of the dedicated IP pool to associate with the configuration set.
    """

    MaxDeliverySeconds: "int | None" = None
    """
    The maximum amount of time, in seconds, that Amazon SES API v2 will attempt delivery
    of email.

    If specified, the value must greater than or equal to 300 seconds (5 minutes) and
    less than or equal to 50400 seconds (840 minutes).
    """

class SESV2ReputationOptions(Boto3Model):
    """
    Enable or disable collection of reputation metrics for emails that you send using
    this configuration set in the current Amazon Web Services Region.
    """

    ReputationMetricsEnabled: "bool | None" = None
    """
    If ``true``, tracking of reputation metrics is enabled for the configuration set.

    If ``false``, tracking of reputation metrics is disabled for the configuration set.
    """

    LastFreshStart: "datetime | None" = None
    """
    The date and time (in Unix time) when the reputation metrics were last given a fresh
    start.

    When your account is given a fresh start, your reputation metrics are calculated
    starting from the date of the fresh start.
    """

class SESV2SendingOptions(Boto3Model):
    """
    Used to enable or disable email sending for messages that use this configuration set
    in the current Amazon Web Services Region.
    """

    SendingEnabled: "bool | None" = None
    """
    If ``true``, email sending is enabled for the configuration set.

    If ``false``, email sending is disabled for the configuration set.
    """

class SESV2Tag(Boto3Model):
    """An object that defines the tags that are associated with a resource. A *tag* is a label that you optionally define and
    associate with a resource. Tags can help you categorize and manage resources in
    different ways, such as by purpose,
    owner, environment, or other criteria. A resource can have as many as 50 tags.

    Each tag consists of a required *tag key* and an associated *tag value*, both of
    which you define. A tag key is a
    general label that acts as a category for a more specific tag value. A tag value
    acts as a descriptor within a tag key.
    A tag key can contain as many as 128 characters. A tag value can contain as many as
    256 characters. The characters can
    be Unicode letters, digits, white space, or one of the following symbols: _ . : / =
        + -. The following additional
    restrictions apply to tags:

    * Tag keys and values are case sensitive.
    * For each associated resource, each tag key must be unique and it can have only one
    value.
    * The ``aws:`` prefix is reserved for use by Amazon Web Services; you can’t use it
    in any tag keys or values that you
      define. In addition, you can't edit or remove tag keys or values that use this
      prefix. Tags that use this prefix don’t
      count against the limit of 50 tags per resource.
    * You can associate tags with public or shared resources, but the tags are available
    only for your Amazon Web Services
      account, not any other accounts that share the resource. In addition, the tags are
      available only for resources that are
      located in the specified Amazon Web Services Region for your Amazon Web Services
      account.
    """

    Key: str
    """
    One part of a key-value pair that defines a tag.

    The maximum length of a tag key is 128 characters. The minimum length is 1
    character.
    """

    Value: str
    """
    The optional part of a key-value pair that defines a tag.

    The maximum length of a tag value is 256 characters. The minimum length is 0
    characters. If you don't want a resource to have a specific tag value, don't specify
    a value for this parameter. If you don't specify a value, Amazon SES sets the value
    to an empty string.
    """


class SESV2SuppressionConfidenceThreshold(Boto3Model):
    """
    Contains the confidence threshold settings for Auto Validation.
    """

    ConfidenceVerdictThreshold: Literal["MEDIUM", "HIGH", "MANAGED"]
    """
    The confidence level threshold for suppression decisions.
    """


class SESV2SuppressionConditionThreshold(Boto3Model):
    """
    Contains Auto Validation settings, allowing you to suppress sending to specific
    destination(s) if they do not meet required threshold.

    For details on Auto Validation, see
    `Auto Validation <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/email-validation.html>`_.
    """

    ConditionThresholdEnabled: Literal["ENABLED", "DISABLED"]
    """
    Indicates whether Auto Validation is enabled for suppression.

    Set to ``ENABLED`` to enable the Auto Validation feature, or set to ``DISABLED`` to
    disable it.
    """

    OverallConfidenceThreshold: "SESV2SuppressionConfidenceThreshold | None" = None
    """
    The overall confidence threshold used to determine suppression decisions.
    """

class SESV2SuppressionValidationOptions(Boto3Model):
    """
    Contains validation options for email address suppression.
    """

    ConditionThreshold: SESV2SuppressionConditionThreshold
    """
    Specifies the condition threshold settings for suppression validation.
    """


class SESV2SuppressionOptions(Boto3Model):
    """
    An object that contains information about the suppression list preferences for your
    account.
    """

    SuppressedReasons: "builtins.list[Literal['BOUNCE', 'COMPLAINT']] | None" = Field(
        default_factory=list
    )
    """
    A list that contains the reasons that email addresses are automatically added to the
    suppression list for your account.

    This list can contain any or all of the following:
    """

    ValidationOptions: "SESV2SuppressionValidationOptions | None" = None
    """
    Contains validation options for email address suppression.
    """

class SESV2DashboardOptions(Boto3Model):
    """
    An object containing additional settings for your VDM configuration as applicable to
    the Dashboard.
    """

    EngagementMetrics: "Literal['ENABLED', 'DISABLED'] | None" = None
    """
    Specifies the status of your VDM engagement metrics collection.

    Can be one of the following:
    """

class SESV2GuardianOptions(Boto3Model):
    """
    An object containing additional settings for your VDM configuration as applicable to
    the Guardian.
    """

    OptimizedSharedDelivery: "Literal['ENABLED', 'DISABLED'] | None" = None
    """
    Specifies the status of your VDM optimized shared delivery.

    Can be one of the following:
    """

class SESV2VdmOptions(Boto3Model):
    """
    An object that defines the VDM settings that apply to emails that you send using the
    configuration set.
    """

    DashboardOptions: "SESV2DashboardOptions | None" = None
    """
    Specifies additional settings for your VDM configuration as applicable to the
    Dashboard.
    """

    GuardianOptions: "SESV2GuardianOptions | None" = None
    """
    Specifies additional settings for your VDM configuration as applicable to the
    Guardian.
    """

class SESV2ArchivingOptions(Boto3Model):
    """
    Used to associate a configuration set with a MailManager archive.
    """

    ArchiveArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the MailManager archive where the Amazon SES API
    v2 will archive sent emails.
    """

class ConfigurationSet(TagsDictMixin, PrimaryBoto3Model):
    """
    Information about a configuration set.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = SESV2Tag
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = ConfigurationSetManager

    ConfigurationSetName: "str | None" = None
    """
    The name of the configuration set.
    """

    TrackingOptions: "SESV2TrackingOptions | None" = None
    """
    An object that defines the open and click tracking options for emails that you send
    using the configuration set.
    """

    DeliveryOptions: "SESV2DeliveryOptions | None" = None
    """
    An object that defines the dedicated IP pool that is used to send emails that you
    send using the configuration set.
    """

    ReputationOptions: "SESV2ReputationOptions | None" = None
    """
    An object that defines whether or not Amazon SES collects reputation metrics for the
    emails that you send that use the configuration set.
    """

    SendingOptions: "SESV2SendingOptions | None" = None
    """
    An object that defines whether or not Amazon SES can send email that you send using
    the configuration set.
    """

    Tags: "builtins.list[SESV2Tag] | None" = Field(default_factory=list)
    """
    An array of objects that define the tags (keys and values) that are associated with
    the configuration set.
    """

    SuppressionOptions: "SESV2SuppressionOptions | None" = None
    """
    An object that contains information about the suppression list preferences for your
    account.
    """
    VdmOptions: "SESV2VdmOptions | None" = None
    """
    An object that contains information about the VDM preferences for your configuration
    set.
    """
    ArchivingOptions: "SESV2ArchivingOptions | None" = None
    """
    An object that defines the MailManager archive where sent emails are archived that
    you send using the configuration set.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`ConfigurationSetName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.ConfigurationSetName

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`ConfigurationSetName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.ConfigurationSetName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`ConfigurationSetName` attribute.
        """
        return hash(self.ConfigurationSetName)


class SESV2DkimAttributes(Boto3Model):
    """
    An object that contains information about the DKIM authentication status for an
    email identity.

    Amazon SES determines the authentication status by searching for specific records in
    the DNS configuration for the domain. If you used
    `Easy DKIM <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/easy-dkim.html>`_
    to set up DKIM
    authentication, Amazon SES tries to find three unique CNAME records in the DNS
    configuration for your domain. If you
    provided a public key to perform DKIM authentication, Amazon SES tries to find a TXT
    record that uses the selector that
    you specified. The value of the TXT record must be a public key that's paired with
    the private key that you specified in
    the process of creating the identity
    """

    SigningEnabled: "bool | None" = None
    """
    If the value is ``true``, then the messages that you send from the identity are
    signed using DKIM.

    If the value is
    ``false``, then the messages that you send from the identity aren't DKIM-signed.
    """

    Status: "Literal['PENDING', 'SUCCESS', 'FAILED', 'TEMPORARY_FAILURE', 'NOT_STARTED'] | None" = None
    """
    Describes whether or not Amazon SES has successfully located the DKIM records in the
    DNS records for the domain.

    The
    status can be one of the following:
    """

    Tokens: "builtins.list[str] | None" = Field(default_factory=list)
    """
If you used `Easy DKIM <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/easy-dkim.html>`_ to configure DKIM
authentication for the domain, then this object contains a set of unique strings that
you use to create a set of CNAME
records that you add to the DNS configuration for your domain. When Amazon SES detects
these records in the DNS
configuration for your domain, the DKIM authentication process is complete.
    """

    SigningHostedZone: "str | None" = None
    """
    The hosted zone where Amazon SES publishes the DKIM public key TXT records for this
    email identity.

    This value indicates the DNS zone that customers must reference when configuring
    their CNAME records for DKIM authentication.
    """

    SigningAttributesOrigin: "Literal['AWS_SES', 'EXTERNAL', 'AWS_SES_AF_SOUTH_1', 'AWS_SES_EU_NORTH_1', 'AWS_SES_AP_SOUTH_1', 'AWS_SES_EU_WEST_3', 'AWS_SES_EU_WEST_2', 'AWS_SES_EU_SOUTH_1', 'AWS_SES_EU_WEST_1', 'AWS_SES_AP_NORTHEAST_3', 'AWS_SES_AP_NORTHEAST_2', 'AWS_SES_ME_SOUTH_1', 'AWS_SES_AP_NORTHEAST_1', 'AWS_SES_IL_CENTRAL_1', 'AWS_SES_SA_EAST_1', 'AWS_SES_CA_CENTRAL_1', 'AWS_SES_AP_SOUTHEAST_1', 'AWS_SES_AP_SOUTHEAST_2', 'AWS_SES_AP_SOUTHEAST_3', 'AWS_SES_EU_CENTRAL_1', 'AWS_SES_US_EAST_1', 'AWS_SES_US_EAST_2', 'AWS_SES_US_WEST_1', 'AWS_SES_US_WEST_2', 'AWS_SES_ME_CENTRAL_1', 'AWS_SES_AP_SOUTH_2', 'AWS_SES_EU_CENTRAL_2', 'AWS_SES_AP_SOUTHEAST_5', 'AWS_SES_CA_WEST_1'] | None" = None
    """
    A string that indicates how DKIM was configured for the identity.

    These are the possible values:
    """

    NextSigningKeyLength: "Literal['RSA_1024_BIT', 'RSA_2048_BIT'] | None" = None
    """
    [Easy DKIM] The key length of the future DKIM key pair to be generated.

    This can be changed at most once per day.
    """

    CurrentSigningKeyLength: "Literal['RSA_1024_BIT', 'RSA_2048_BIT'] | None" = None
    """
    [Easy DKIM] The key length of the DKIM key pair in use.
    """

    LastKeyGenerationTimestamp: "datetime | None" = None
    """
    [Easy DKIM] The last time a key pair was generated for this identity.
    """

class SESV2MailFromAttributes(Boto3Model):
    """
    A list of attributes that are associated with a MAIL FROM domain.
    """

    MailFromDomain: str
    """
    The name of a domain that an email identity uses as a custom MAIL FROM domain.
    """
    MailFromDomainStatus: Literal["PENDING", "SUCCESS", "FAILED", "TEMPORARY_FAILURE"]
    """
    The status of the MAIL FROM domain.

    This status can have the following values:
    """

    BehaviorOnMxFailure: Literal["USE_DEFAULT_VALUE", "REJECT_MESSAGE"]
    """
    The action to take if the required MX record can't be found when you send an email.

    When you set this value to ``USE_DEFAULT_VALUE``, the mail is sent using
    *amazonses.com* as the MAIL FROM domain. When you set this value to
    ``REJECT_MESSAGE``, the Amazon SES API v2 returns a ``MailFromDomainNotVerified``
    error, and doesn't attempt to deliver the email.
    """


class SESV2SOARecord(Boto3Model):
    """
    An object that contains information about the start of authority (SOA) record
    associated with the identity.
    """

    PrimaryNameServer: "str | None" = None
    """
    Primary name server specified in the SOA record.
    """

    AdminEmail: "str | None" = None
    """
    Administrative contact email from the SOA record.
    """

    SerialNumber: "int | None" = None
    """
    Serial number from the SOA record.
    """

class SESV2VerificationInfo(Boto3Model):
    """
    An object that contains additional information about the verification status for the
    identity.
    """

    LastCheckedTimestamp: "datetime | None" = None
    """
    The last time a verification attempt was made for this identity.
    """

    LastSuccessTimestamp: "datetime | None" = None
    """
    The last time a successful verification was made for this identity.
    """

    ErrorType: "Literal['SERVICE_ERROR', 'DNS_SERVER_ERROR', 'HOST_NOT_FOUND', 'TYPE_NOT_FOUND', 'INVALID_VALUE', 'REPLICATION_ACCESS_DENIED', 'REPLICATION_PRIMARY_NOT_FOUND', 'REPLICATION_PRIMARY_BYO_DKIM_NOT_SUPPORTED', 'REPLICATION_REPLICA_AS_PRIMARY_NOT_SUPPORTED', 'REPLICATION_PRIMARY_INVALID_REGION'] | None" = None
    """
    Provides the reason for the failure describing why Amazon SES was not able to
    successfully verify the identity.

    Below
    are the possible values:
    """
    SOARecord: "SESV2SOARecord | None" = None
    """
    An object that contains information about the start of authority (SOA) record
    associated with the identity.
    """

class SESV2Destination(Boto3Model):
    """
    An object that describes the recipients for an email.

    Amazon SES does not support the SMTPUTF8 extension, as described in
    `RFC6531 <https://tools.ietf.org/html/rfc6531>`_.
    For    this reason, the *local part* of a destination email address (the part of the
    email address that precedes the @ sign)    may only contain `7-bit ASCII characters
    <https://en.wikipedia.org/wiki/Email_address#Local-part>`_. If the *domain part*
    of an address (the part after the @ sign) contains non-ASCII characters, they must
    be encoded using Punycode, as    described in
    `RFC3492 <https://tools.ietf.org/html/rfc3492.html>`_.
    """

    ToAddresses: "builtins.list[str] | None" = Field(default_factory=list)
    """
    An array that contains the email addresses of the "To" recipients for the email.
    """

    CcAddresses: "builtins.list[str] | None" = Field(default_factory=list)
    """
    An array that contains the email addresses of the "CC" (carbon copy) recipients for
    the email.
    """

    BccAddresses: "builtins.list[str] | None" = Field(default_factory=list)
    """
    An array that contains the email addresses of the "BCC" (blind carbon copy)
    recipients for the email.
    """

class SESV2Content(Boto3Model):
    """
    An object that represents the content of the email, and optionally a character set
    specification.
    """

    Data: str
    """
    The content of the message itself.
    """
    Charset: "str | None" = None
    """
    The character set for the content.

    Because of the constraints of the SMTP protocol, Amazon SES uses 7-bit ASCII by
    default. If the text includes characters outside of the ASCII range, you have to
    specify a character set. For example, you could specify ``UTF-8``, ``ISO-8859-1``,
    or ``Shift_JIS``.
    """
class SESV2Body(Boto3Model):
    """
    Represents the body of the email message.
    """

    Text: "SESV2Content | None" = None
    """
    An object that represents the version of the message that is displayed in email
    clients that don't support HTML, or clients where the recipient has disabled HTML
    rendering.
    """
    Html: "SESV2Content | None" = None
    """
    An object that represents the version of the message that is displayed in email
    clients that support HTML.

    HTML messages can include formatted text, hyperlinks, images, and more.
    """

class MessageHeader(Boto3Model):
    """
    Contains the name and value of a message header that you add to an email.
    """

    Name: str
    """
    The name of the message header.

    The message header name has to meet the following criteria:
    """

    Value: str
    """
    The value of the message header.

    The message header value has to meet the following criteria:
    """


class Attachment(Boto3Model):
    """
    Contains metadata and attachment raw content.
    """

    RawContent: bytes
    """
    The raw data of the attachment.

    It needs to be base64-encoded if you are accessing Amazon SES directly through the
    HTTPS interface. If you are accessing Amazon SES using an Amazon Web Services SDK,
    the SDK takes care of the base 64-encoding for you.
    """
    ContentDisposition: "Literal['ATTACHMENT', 'INLINE'] | None" = None
    """
    A standard descriptor indicating how the attachment should be rendered in the email.

    Supported values: ``ATTACHMENT`` or
    ``INLINE``.
    """

    FileName: str
    """
    The file name for the attachment as it will appear in the email.

    Amazon SES restricts certain file extensions. To ensure attachments are accepted,
    check the
    `Unsupported attachment types <https://docs.aws.amazon.com/ses/latest/dg/mime-
    types.html>`_ in the Amazon SES Developer Guide.
    """
    ContentDescription: "str | None" = None
    """
    A brief description of the attachment content.
    """

    ContentId: "str | None" = None
    """
    Unique identifier for the attachment, used for referencing attachments with INLINE
    disposition in HTML content.
    """

    ContentTransferEncoding: "Literal['BASE64', 'QUOTED_PRINTABLE', 'SEVEN_BIT'] | None" = None
    """
    Specifies how the attachment is encoded.

    Supported values: ``BASE64``, ``QUOTED_PRINTABLE``, ``SEVEN_BIT``.
    """
    ContentType: "str | None" = None
    """
    The MIME type of the attachment.
    """

class SESV2Message(Boto3Model):
    """
    Represents the email message that you're sending.

    The ``Message`` object consists of a subject line and a message body.
    """

    Subject: SESV2Content
    """
    The subject line of the email.

    The subject line can only contain 7-bit ASCII characters. However, you can specify
    non- ASCII characters in the subject line by using encoded-word syntax, as described
    in
    `RFC 2047 <https://tools.ietf.org/html/rfc2047>`_.
    """

    Body: SESV2Body
    """
    The body of the message.

    You can specify an HTML version of the message, a text-only version of the message,
    or both.
    """

    Headers: "builtins.list[MessageHeader] | None" = Field(default_factory=list)
    """
    The list of message headers that will be added to the email message.
    """

    Attachments: "builtins.list[Attachment] | None" = Field(default_factory=list)
    """
    The List of attachments to include in your email.

    All recipients will receive the same attachments.
    """

class RawMessage(Boto3Model):
    """
    Represents the raw content of an email message.
    """

    Data: bytes
    """
    The raw email message.

    The message has to meet the following criteria:
    """
class EmailTemplateContent(Boto3Model):
    """
    The content of the email, composed of a subject line, an HTML part, and a text-only
    part.
    """

    Subject: "str | None" = None
    """
    The subject line of the email.
    """
    Text: "str | None" = None
    """
    The email body that will be visible to recipients whose email clients do not display
    HTML.
    """
    Html: "str | None" = None
    """
    The HTML body of the email.
    """

class SESV2Template(Boto3Model):
    """
    An object that defines the email template to use for an email message, and the
    values to use for any message variables in that template.

    An *email template* is a type of message template that contains content that you
    want to reuse in email messages that you send. You can specifiy the email template
    by providing the name or ARN of an *email template* previously saved in your Amazon
    SES account or by providing the full template content.
    """

    TemplateName: "str | None" = None
    """
    The name of the template.

    You will refer to this name when you send email using the ``SendEmail`` or
    ``SendBulkEmail`` operations.
    """

    TemplateArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the template.
    """

    TemplateContent: "EmailTemplateContent | None" = None
    """
    The content of the template.
    """

    TemplateData: "str | None" = None
    """
    An object that defines the values to use for message variables in the template.

    This object is a set of key-value pairs. Each key defines a message variable in the
    template. The corresponding value defines the value to use for that variable.
    """

    Headers: "builtins.list[MessageHeader] | None" = Field(default_factory=list)
    """
    The list of message headers that will be added to the email message.
    """
    Attachments: "builtins.list[Attachment] | None" = Field(default_factory=list)
    """
    The List of attachments to include in your email.

    All recipients will receive the same attachments.
    """

class SESV2EmailContent(Boto3Model):
    """
    An object that defines the entire content of the email, including the message
    headers, body content, and attachments.

    For a simple email message, you specify the subject and provide both text and HTML
    versions of the message body. You can also add attachments to simple and templated
    messages. For a raw message, you provide a complete MIME-formatted message, which
    can include custom headers and attachments.
    """

    Simple: "SESV2Message | None" = None
    """
    The simple email message.

    The message consists of a subject, message body and attachments list.
    """

    Raw: "RawMessage | None" = None
    """
    The raw email message.

    The message has to meet the following criteria:
    """

    Template: "SESV2Template | None" = None
    """
    The template to use for the email message.
    """

class SESV2MessageTag(Boto3Model):
    """
    Contains the name and value of a tag that you apply to an email.

    You can use message tags when you publish email sending events.
    """

    Name: str
    """
    The name of the message tag.

    The message tag name has to meet the following criteria:
    """

    Value: str
    """
    The value of the message tag.

    The message tag value has to meet the following criteria:
    """


class SESV2ListManagementOptions(Boto3Model):
    """
    An object used to specify a list or topic to which an email belongs, which will be
    used when a contact chooses to unsubscribe.
    """

    ContactListName: str
    """
    The name of the contact list.
    """

    TopicName: "str | None" = None
    """
    The name of the topic.
    """

class SESV2BulkEmailContent(Boto3Model):
    """
    An object that contains the body of the message.

    You can specify a template message.
    """

    Template: "SESV2Template | None" = None
    """
    The template to use for the bulk email message.
    """

class SESV2ReplacementTemplate(Boto3Model):
    """
    An object which contains ``ReplacementTemplateData`` to be used for a specific
    ``BulkEmailEntry``.
    """

    ReplacementTemplateData: "str | None" = None
    """
    A list of replacement values to apply to the template.

    This parameter is a JSON object, typically consisting of key- value pairs in which
    the keys correspond to replacement tags in the email template.
    """

class SESV2ReplacementEmailContent(Boto3Model):
    """
    The ``ReplaceEmailContent`` object to be used for a specific ``BulkEmailEntry``.

    The ``ReplacementTemplate`` can be specified within this object.
    """

    ReplacementTemplate: "SESV2ReplacementTemplate | None" = None
    """
    The ``ReplacementTemplate`` associated with ``ReplacementEmailContent``.
    """

class SESV2BulkEmailEntry(Boto3Model):
    Destination: SESV2Destination
    """
    Represents the destination of the message, consisting of To:, CC:, and BCC: fields.
    """

    ReplacementTags: "builtins.list[SESV2MessageTag] | None" = Field(
        default_factory=list
    )
    """
    A list of tags, in the form of name/value pairs, to apply to an email that you send
    using the ``SendBulkTemplatedEmail`` operation.

    Tags correspond to characteristics of the email that you define, so that you can
    publish email sending events.
    """

    ReplacementEmailContent: "SESV2ReplacementEmailContent | None" = None
    """
    The ``ReplacementEmailContent`` associated with a ``BulkEmailEntry``.
    """

    ReplacementHeaders: "builtins.list[MessageHeader] | None" = Field(
        default_factory=list
    )
    """
    The list of message headers associated with the ``BulkEmailEntry`` data type.
    """

class EmailIdentity(TagsDictMixin, PrimaryBoto3Model):
    """
    Information about an email identity.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = list[SESV2Tag] | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = EmailIdentityManager

    EmailIdentity: str = Field(default=None, alias="IdentityName")
    """
    The address or domain of the identity.
    """

    SendingEnabled: "bool | None" = None
    """
    Indicates whether or not you can send email from the identity.
    """

    IdentityType: Literal["EMAIL_ADDRESS", "DOMAIN", "MANAGED_DOMAIN"] = Field(
        default=None, frozen=True
    )
    """
    The email identity type.

    Note: the ``MANAGED_DOMAIN`` type is not supported for email identity types.
    """

    VerificationStatus: Literal[
        "PENDING", "SUCCESS", "FAILED", "TEMPORARY_FAILURE", "NOT_STARTED"
    ] = Field(default=None, frozen=True)
    """
    The verification status of the identity.

    The status can be one of the following:
    """

    Tags: list["SESV2Tag"] | None = None
    """
    Tags associated with the email identity.
    """

    FeedbackForwardingStatus: bool = Field(default=None, frozen=True)
    """
    The feedback forwarding configuration for the identity.
    """

    VerifiedForSendingStatus: bool = Field(default=None, frozen=True)
    """
    Specifies whether or not the identity is verified.

    You can only send email from verified email addresses or domains. For more
    information about verifying identities, see the
    `Amazon Pinpoint User Guide <https://docs.aws.amazon.com/pinpoint/latest/userguide/channels-email-manage-verify.html>`_.
    """

    DkimAttributes: SESV2DkimAttributes = Field(default=None, frozen=True)
    """
    An object that contains information about the DKIM attributes for the identity.
    """

    MailFromAttributes: SESV2MailFromAttributes = Field(default=None, frozen=True)
    """
    An object that contains information about the Mail-From attributes for the email
    identity.
    """

    Policies: dict[str, str] = Field(default_factory=dict, frozen=True)
    """
    A map of policy names to policies.
    """

    ConfigurationSetName: str = Field(default=None, frozen=True)
    """
    The configuration set used by default when sending from this identity.
    """

    VerificationInfo: SESV2VerificationInfo = Field(default=None, frozen=True)
    """
    An object that contains additional information about the verification status for the
    identity.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`EmailIdentity` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.EmailIdentity

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`EmailIdentity` attribute.

        Returns:
            The name of the model instance.
        """
        return self.EmailIdentity

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`EmailIdentity` attribute.
        """
        return hash(self.EmailIdentity)

    def send(
        self,
        Content: SESV2EmailContent,
        Destination: SESV2Destination | None = None,
        ReplyToAddresses: builtins.list[str] | None = None,
        FeedbackForwardingEmailAddress: str | None = None,
        FeedbackForwardingEmailAddressIdentityArn: str | None = None,
        FromEmailAddressIdentityArn: str | None = None,
        EmailTags: builtins.list[SESV2MessageTag] | None = None,
        ConfigurationSetName: str | None = None,
        ListManagementOptions: SESV2ListManagementOptions | None = None,
        EndpointId: str | None = None,
        TenantName: str | None = None,
    ) -> "str":
        """
        Send an email from this identity.

        Args:
            Content: An object that contains the body of the message. You can send
                either a Simple message, Raw message, or a
                Templated message.

        Keyword Args:
            Destination: An object that contains the recipients of the email message.
            ReplyToAddresses: The "Reply-to" email addresses for the message. When the
                recipient replies to the message, each
                Reply-to address receives the reply.
            FeedbackForwardingEmailAddress: The address that you want bounce and
                complaint notifications to be sent to.
            FeedbackForwardingEmailAddressIdentityArn: This parameter is used only for
                sending authorization. It is the ARN of
                the identity that is associated with the sending authorization policy
                that permits you to use the email address
                specified in the ``FeedbackForwardingEmailAddress`` parameter.
            FromEmailAddressIdentityArn: This parameter is used only for sending
                authorization. It is the ARN of the identity
                that is associated with the sending authorization policy that permits
                you to use the email address specified in the
                ``FromEmailAddress`` parameter.
            EmailTags: A list of tags, in the form of name/value pairs, to apply to an
                email that you send using the
                ``SendEmail`` operation. Tags correspond to characteristics of the email
                that you define, so that you can publish
                email sending events.
            ConfigurationSetName: The name of the configuration set to use when sending
                the email.
            ListManagementOptions: An object used to specify a list or topic to which an
                email belongs, which will be used when
                a contact chooses to unsubscribe.
            EndpointId: The ID of the multi-region endpoint (global-endpoint).
            TenantName: The name of the tenant through which this email will be sent.
        """
        return (
            cast("EmailIdentityManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .send(
                cast("str", self.EmailIdentity),
                Content,
                Destination=Destination,
                ReplyToAddresses=ReplyToAddresses,
                FeedbackForwardingEmailAddress=FeedbackForwardingEmailAddress,
                FeedbackForwardingEmailAddressIdentityArn=FeedbackForwardingEmailAddressIdentityArn,
                FromEmailAddressIdentityArn=FromEmailAddressIdentityArn,
                EmailTags=EmailTags,
                ConfigurationSetName=ConfigurationSetName,
                ListManagementOptions=ListManagementOptions,
                EndpointId=EndpointId,
                TenantName=TenantName,
            )
        )

    def send_bulk(
        self,
        DefaultContent: SESV2BulkEmailContent,
        BulkEmailEntries: builtins.list[SESV2BulkEmailEntry],
        ReplyToAddresses: builtins.list[str] | None = None,
        FeedbackForwardingEmailAddress: str | None = None,
        FeedbackForwardingEmailAddressIdentityArn: str | None = None,
        FromEmailAddressIdentityArn: str | None = None,
        DefaultEmailTags: builtins.list[SESV2MessageTag] | None = None,
        ConfigurationSetName: str | None = None,
        EndpointId: str | None = None,
        TenantName: str | None = None,
    ) -> "builtins.list[BulkEmailEntryResult]":
        """
        Send a bulk email from this identity.

        Args:
            DefaultContent: An object that contains the body of the message. You can
                specify a template message.
            BulkEmailEntries: The list of bulk email entry objects.

        Keyword Args:
            ReplyToAddresses: The "Reply-to" email addresses for the message. When the
                recipient replies to the message, each
                Reply-to address receives the reply.
            FeedbackForwardingEmailAddress: The address that you want bounce and
                complaint notifications to be sent to.
            FeedbackForwardingEmailAddressIdentityArn: This parameter is used only for
                sending authorization. It is the ARN of
                the identity that is associated with the sending authorization policy
                that permits you to use the email address
                specified in the ``FeedbackForwardingEmailAddress`` parameter.
            FromEmailAddressIdentityArn: This parameter is used only for sending
                authorization. It is the ARN of the identity
                that is associated with the sending authorization policy that permits
                you to use the email address specified in the
                ``FromEmailAddress`` parameter.
            DefaultEmailTags: A list of tags, in the form of name/value pairs, to apply
                to an email that you send using the
                ``SendEmail`` operation. Tags correspond to characteristics of the email
                that you define, so that you can publish
                email sending events.
            ConfigurationSetName: The name of the configuration set to use when sending
                the email.
            EndpointId: The ID of the multi-region endpoint (global-endpoint).
            TenantName: The name of the tenant through which this bulk email will be
                sent.
        """
        return (
            cast("EmailIdentityManager", self.objects)  # type: ignore[attr-defined]
            .using(self.session)
            .send_bulk(
                cast("str", self.EmailIdentity),
                DefaultContent,
                BulkEmailEntries,
                ReplyToAddresses=ReplyToAddresses,
                FeedbackForwardingEmailAddress=FeedbackForwardingEmailAddress,
                FeedbackForwardingEmailAddressIdentityArn=FeedbackForwardingEmailAddressIdentityArn,
                FromEmailAddressIdentityArn=FromEmailAddressIdentityArn,
                DefaultEmailTags=DefaultEmailTags,
                ConfigurationSetName=ConfigurationSetName,
                EndpointId=EndpointId,
                TenantName=TenantName,
            )
        )
class SESV2Topic(Boto3Model):
    """
    An interest group, theme, or label within a list.

    Lists can have multiple topics.
    """

    TopicName: str
    """
    The name of the topic.
    """

    DisplayName: str
    """
    The name of the topic the contact will see.
    """

    Description: "str | None" = None
    """
    A description of what the topic is about, which the contact will see.
    """

    DefaultSubscriptionStatus: Literal["OPT_IN", "OPT_OUT"]
    """
    The default subscription status to be applied to a contact if the contact has not
    noted their preference for subscribing to a topic.
    """

class ContactList(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = SESV2Tag
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = ContactListManager
    ContactListName: "str | None" = None
    """
    The name of the contact list.
    """

    Topics: "builtins.list[SESV2Topic] | None" = Field(default_factory=list)
    """
    An interest group, theme, or label within a list.

    A contact list can have multiple topics.
    """

    Description: "str | None" = None
    """
    A description of what the contact list is about.
    """

    CreatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    A timestamp noting when the contact list was created.
    """

    LastUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    A timestamp noting the last time the contact list was updated.
    """

    Tags: "builtins.list[SESV2Tag] | None" = Field(default_factory=list)
    """
    The tags associated with a contact list.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`ContactListName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.ContactListName

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`ContactListName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.ContactListName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`ContactListName` attribute.
        """
        return hash(self.ContactListName)


class EmailTemplate(TagsDictMixin, PrimaryBoto3Model):
    """
    The following element is returned by the service.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = SESV2Tag
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = EmailTemplateManager

    TemplateContent: "EmailTemplateContent | None" = None
    """
    The content of the email template, composed of a subject line, an HTML part, and a
    text-only part.
    """

    TemplateName: str
    """
    The name of the template.
    """

    Tags: "builtins.list[SESV2Tag] | None" = Field(default_factory=list)
    """
    An array of objects that define the tags (keys and values) that are associated with
    the email template.
    """

    CreatedTimestamp: datetime | None = None
    """
    Creation time returned by list operations.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`TemplateName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.TemplateName

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`TemplateName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.TemplateName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`TemplateName` attribute.
        """
        return hash(self.TemplateName)


class SuppressedDestinationAttributes(Boto3Model):
    """
    An object that contains additional attributes that are related an email address that
    is on the suppression list for your account.
    """

    MessageId: "str | None" = None
    """
    The unique identifier of the email message that caused the email address to be added
    to the suppression list for your account.
    """

    FeedbackId: "str | None" = None
    """
    A unique identifier that's generated when an email address is added to the
    suppression list for your account.
    """

class SuppressedDestination(PrimaryBoto3Model):
    """
    An object that contains information about an email address that is on the
    suppression list for your account.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = SuppressedDestinationManager

    EmailAddress: str
    """
    The email address that is on the suppression list for your account.
    """

    Reason: Literal["BOUNCE", "COMPLAINT"]
    """
    The reason that the address was added to the suppression list for your account.
    """

    LastUpdateTime: datetime = Field(frozen=True)
    """
    The date and time when the suppressed destination was last updated, shown in Unix
    time format.
    """

    Attributes: SuppressedDestinationAttributes = Field(default=None, frozen=True)
    """
    An optional value that can contain additional information about the reasons that the
    address was added to the suppression list for your account.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`EmailAddress` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.EmailAddress

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`EmailAddress` attribute.

        Returns:
            The name of the model instance.
        """
        return self.EmailAddress

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`EmailAddress` attribute.
        """
        return hash(self.EmailAddress)


class DeliverabilityTestReport(TagsDictMixin, PrimaryBoto3Model):
    """
    An object that contains metadata related to a predictive inbox placement test.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = list[SESV2Tag] | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DeliverabilityTestReportManager
    )

    ReportId: str = Field(default=None, frozen=True)
    """
    A unique string that identifies the predictive inbox placement test.
    """

    ReportName: "str | None" = None
    """
    A name that helps you identify a predictive inbox placement test report.
    """

    Subject: str = Field(default=None, frozen=True)
    """
    The subject line for an email that you submitted in a predictive inbox placement
    test.
    """
    FromEmailAddress: "str | None" = None
    """
    The sender address that you specified for the predictive inbox placement test.
    """

    CreateDate: datetime = Field(default=None, frozen=True)
    """
    The date and time when the predictive inbox placement test was created.
    """

    DeliverabilityTestStatus: Literal["IN_PROGRESS", "COMPLETED"] = Field(
        default=None, frozen=True
    )
    """
    The status of the predictive inbox placement test.

    If the status is ``IN_PROGRESS``, then the predictive inbox placement test is
    currently running. Predictive inbox placement tests are usually complete within 24
    hours of creating the test. If the status is ``COMPLETE``, then the test is
    finished, and you can use the ``GetDeliverabilityTestReport`` to view the results of
    the test.
    """

    Content: Any | None = None
    """
    Email content payload used when creating deliverability tests.
    """

    OverallPlacement: "SESV2PlacementStatistics | None" = None
    """
    Aggregate mailbox placement statistics from get operations.
    """

    IspPlacements: list["SESV2IspPlacement"] | None = None
    """
    Per-ISP placement statistics from get operations.
    """
    Message: str | None = None
    """
    Additional service message returned by get operations.
    """

    Tags: list["SESV2Tag"] | None = None
    """
    Tags associated with the report.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`ReportId` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.ReportId

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`ReportName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.ReportName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`ReportId` attribute.
        """
        return hash(self.ReportId)


class MultiRegionEndpoint(PrimaryBoto3Model):
    """
    An object that contains multi-region endpoint (global-endpoint) properties.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = MultiRegionEndpointManager

    EndpointName: "str | None" = None
    """
    The name of the multi-region endpoint (global-endpoint).
    """

    Status: Literal["CREATING", "READY", "FAILED", "DELETING"] = Field(
        default=None, frozen=True
    )
    """
    The status of the multi-region endpoint (global-endpoint).
    """
    EndpointId: str = Field(default=None, frozen=True)
    """
    The ID of the multi-region endpoint (global-endpoint).
    """

    Regions: "builtins.list[str]" = Field(default_factory=list, frozen=True)
    """
    Primary and secondary regions between which multi-region endpoint splits sending
    traffic.
    """

    CreatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The time stamp of when the multi-region endpoint (global-endpoint) was created.
    """

    LastUpdatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The time stamp of when the multi-region endpoint (global-endpoint) was last updated.
    """

    Details: Any | None = None
    """
    Route detail payload used during endpoint creation.
    """
    Routes: list["SESV2MultiRegionEndpointRoute"] | None = None
    """
    Route details returned by get operations.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`EndpointName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.EndpointName

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`EndpointName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.EndpointName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`EndpointName` attribute.
        """
        return hash(self.EndpointName)


class Tenant(TagsDictMixin, PrimaryBoto3Model):
    """
    A structure that contains details about a tenant.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = SESV2Tag
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = TenantManager

    TenantName: "str | None" = None
    """
    The name of the tenant.
    """

    TenantId: str = Field(default=None, frozen=True)
    """
    A unique identifier for the tenant.
    """

    TenantArn: str = Field(default=None, frozen=True)
    """
    The Amazon Resource Name (ARN) of the tenant.
    """

    CreatedTimestamp: datetime = Field(default=None, frozen=True)
    """
    The date and time when the tenant was created.
    """

    Tags: "builtins.list[SESV2Tag] | None" = Field(default_factory=list)
    """
    An array of objects that define the tags (keys and values) associated with the
    tenant.
    """

    SendingStatus: Literal["ENABLED", "REINSTATED", "DISABLED"] = Field(
        default=None, frozen=True
    )
    """
    The status of sending capability for the tenant.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`TenantName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.TenantName

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`TenantArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.TenantArn
    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`TenantName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.TenantName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`TenantName` attribute.
        """
        return hash(self.TenantName)


class DedicatedIpPool(PrimaryBoto3Model):
    """
    Contains information about a dedicated IP pool.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DedicatedIpPoolManager
    PoolName: str
    """
    The name of the dedicated IP pool.
    """

    ScalingMode: Literal["STANDARD", "MANAGED"]
    """
    The type of the dedicated IP pool.
    """
    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`PoolName` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.PoolName
    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`PoolName` attribute.

        Returns:
            The name of the model instance.
        """
        return self.PoolName

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`PoolName` attribute.
        """
        return hash(self.PoolName)


class DedicatedIp(ReadonlyPrimaryBoto3Model):
    """
    Contains information about a dedicated IP address that is associated with your
    Amazon SES account.

    To learn more about requesting dedicated IP addresses, see
    `Requesting and Relinquishing Dedicated IP Addresses <https://docs.aws.amazon.com/ses/latest/DeveloperGuide/dedicated-ip-case.html>`_
    in the *Amazon SES Developer
    Guide*.
    """

    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DedicatedIpManager

    Ip: str
    """
    An IPv4 address.
    """

    WarmupStatus: Literal["IN_PROGRESS", "DONE", "NOT_APPLICABLE"]
    """
    The warm-up status of a dedicated IP address.

    The status can have one of the following values:
    """

    WarmupPercentage: int
    """
Indicates the progress of your dedicated IP warm-up:
    """

    PoolName: "str | None" = None
    """
    The name of the dedicated IP pool that the IP address is associated with.
    """
    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`Ip` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.Ip
    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`Ip` attribute.

        Returns:
            The name of the model instance.
        """
        return self.Ip
    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`Ip` attribute.
        """
        return hash(self.Ip)
class BulkEmailEntryResult(Boto3Model):
    """
    The result of the ``SendBulkEmail`` operation of each specified ``BulkEmailEntry``.
    """

    Status: "Literal['SUCCESS', 'MESSAGE_REJECTED', 'MAIL_FROM_DOMAIN_NOT_VERIFIED', 'CONFIGURATION_SET_NOT_FOUND', 'TEMPLATE_NOT_FOUND', 'ACCOUNT_SUSPENDED', 'ACCOUNT_THROTTLED', 'ACCOUNT_DAILY_QUOTA_EXCEEDED', 'INVALID_SENDING_POOL_NAME', 'ACCOUNT_SENDING_PAUSED', 'CONFIGURATION_SET_SENDING_PAUSED', 'INVALID_PARAMETER', 'TRANSIENT_FAILURE', 'FAILED'] | None" = None
    """
    The status of a message sent using the ``SendBulkTemplatedEmail`` operation.
    """
    Error: "str | None" = None
    """
    A description of an error that prevented a message being sent using the
    ``SendBulkTemplatedEmail`` operation.
    """
    MessageId: "str | None" = None
    """
    The unique message identifier returned from the ``SendBulkTemplatedEmail``
    operation.
    """

class SESV2PlacementStatistics(Boto3Model):
    """
    An object that contains inbox placement data for an email provider.
    """

    InboxPercentage: "float | None" = None
    """
    The percentage of emails that arrived in recipients' inboxes during the predictive
    inbox placement test.
    """

    SpamPercentage: "float | None" = None
    """
    The percentage of emails that arrived in recipients' spam or junk mail folders
    during the predictive inbox placement test.
    """

    MissingPercentage: "float | None" = None
    """
    The percentage of emails that didn't arrive in recipients' inboxes at all during the
    predictive inbox placement test.
    """

    SpfPercentage: "float | None" = None
    """
    The percentage of emails that were authenticated by using Sender Policy Framework
    (SPF) during the predictive inbox placement test.
    """

    DkimPercentage: "float | None" = None
    """
    The percentage of emails that were authenticated by using DomainKeys Identified Mail
    (DKIM) during the predictive inbox placement test.
    """

class SESV2IspPlacement(Boto3Model):
    """
    An object that describes how email sent during the predictive inbox placement test
    was handled by a certain email provider.
    """

    IspName: "str | None" = None
    """
    The name of the email provider that the inbox placement data applies to.
    """
    PlacementStatistics: "SESV2PlacementStatistics | None" = None
    """
    An object that contains inbox placement metrics for a specific email provider.
    """
# =======================
# Request/Response Models
# =======================


class ListConfigurationSetsResponse(Boto3Model):
    """
    A list of configuration sets in your Amazon SES account in the current Amazon Web
    Services Region.
    """

    ConfigurationSets: "builtins.list[str] | None" = Field(default_factory=list)
    """
    An array that contains all of the configuration sets in your Amazon SES account in
    the current Amazon Web Services Region.
    """
    NextToken: "str | None" = None
    """
    A token that indicates that there are additional configuration sets to list.

    To view additional configuration sets, issue another request to
    ``ListConfigurationSets``, and pass this token in the ``NextToken`` parameter.
    """
class DeleteConfigurationSetResponse(Boto3Model):
    """
    An HTTP 200 response if the request succeeds, or an error message if the request
    fails.
    """


class SendEmailResponse(Boto3Model):
    """
    A unique message ID that you receive when an email is accepted for sending.
    """

    MessageId: "str | None" = None
    """
    A unique identifier for the message that is generated when the message is accepted.
    """
class SendBulkEmailResponse(Boto3Model):
    """
    The following data is returned in JSON format by the service.
    """

    BulkEmailEntryResults: "builtins.list[BulkEmailEntryResult]"
    """
    One object per intended recipient.

    Check each response object and retry any messages with a failure status.
    """


class ListContactListsResponse(Boto3Model):
    ContactLists: "builtins.list[ContactList] | None" = Field(default_factory=list)
    """
    The available contact lists.
    """

    NextToken: "str | None" = None
    """
    A string token indicating that there might be additional contact lists available to
    be listed.

    Copy this token to a subsequent call to ``ListContactLists`` with the same
    parameters to retrieve the next page of contact lists.
    """

class DeleteContactListResponse(Boto3Model):
    pass


class EmailTemplateMetadata(Boto3Model):
    """
    Contains information about an email template.
    """

    TemplateName: "str | None" = None
    """
    The name of the template.
    """

    CreatedTimestamp: "datetime | None" = None
    """
    The time and date the template was created.
    """

class ListEmailTemplatesResponse(Boto3Model):
    """
    The following elements are returned by the service.
    """

    TemplatesMetadata: "builtins.list[EmailTemplateMetadata] | None" = Field(
        default_factory=list
    )
    """
    An array the contains the name and creation time stamp for each template in your
    Amazon SES account.
    """
    NextToken: "str | None" = None
    """
    A token indicating that there are additional email templates available to be listed.

    Pass this token to a subsequent
    ``ListEmailTemplates`` call to retrieve the next 10 email templates.
    """

class DeleteEmailTemplateResponse(Boto3Model):
    """
    If the action is successful, the service sends back an HTTP 200 response with an
    empty HTTP body.
    """


class GetSuppressedDestinationResponse(Boto3Model):
    """
    Information about the suppressed email address.
    """

    SuppressedDestinationInstance: SuppressedDestination = Field(
        default=None, alias="SuppressedDestination"
    )
    """
    An object containing information about the suppressed email address.
    """

class SuppressedDestinationSummary(Boto3Model):
    """
    A summary that describes the suppressed email address.
    """

    EmailAddress: str
    """
    The email address that's on the suppression list for your account.
    """

    Reason: Literal["BOUNCE", "COMPLAINT"]
    """
    The reason that the address was added to the suppression list for your account.
    """
    LastUpdateTime: datetime
    """
    The date and time when the suppressed destination was last updated, shown in Unix
    time format.
    """


class ListSuppressedDestinationsResponse(Boto3Model):
    """
    A list of suppressed email addresses.
    """

    SuppressedDestinationSummaries: "builtins.list[SuppressedDestinationSummary] | None" = Field(
        default_factory=list
    )
    """
    A list of summaries, each containing a summary for a suppressed email destination.
    """

    NextToken: "str | None" = None
    """
    A token that indicates that there are additional email addresses on the suppression
    list for your account.

    To view additional suppressed addresses, issue another request to
    ``ListSuppressedDestinations``, and pass this token in the ``NextToken`` parameter.
    """

class DeleteSuppressedDestinationResponse(Boto3Model):
    """
    An HTTP 200 response if the request succeeds, or an error message if the request
    fails.
    """


class ListDeliverabilityTestReportsResponse(Boto3Model):
    """
    A list of the predictive inbox placement test reports that are available for your
    account, regardless of whether or not those tests are complete.
    """

    DeliverabilityTestReports: "builtins.list[DeliverabilityTestReport]"
    """
    An object that contains a lists of predictive inbox placement tests that you've
    performed.
    """

    NextToken: "str | None" = None
    """
    A token that indicates that there are additional predictive inbox placement tests to
    list.

    To view additional predictive inbox placement tests, issue another request to
    ``ListDeliverabilityTestReports``, and pass this token in the ``NextToken``
    parameter.
    """

class RouteDetails(Boto3Model):
    """
    An object that contains route configuration.

    Includes secondary region name.
    """

    Region: str
    """
    The name of an AWS-Region to be a secondary region for the multi-region endpoint
    (global-endpoint).
    """
class Details(Boto3Model):
    """
    An object that contains configuration details of multi-region endpoint (global-
    endpoint).
    """

    RoutesDetails: "builtins.list[RouteDetails]"
    """
    A list of route configuration details.

    Must contain exactly one route configuration.
    """


class SESV2MultiRegionEndpointRoute(Boto3Model):
    """
    An object which contains an AWS-Region and routing status.
    """

    Region: str
    """
    The name of an AWS-Region.
    """


class GetMultiRegionEndpointResponse(Boto3Model):
    """
    An HTTP 200 response if the request succeeds, or an error message if the request
    fails.
    """

    EndpointName: "str | None" = None
    """
    The name of the multi-region endpoint (global-endpoint).
    """
    EndpointId: "str | None" = None
    """
    The ID of the multi-region endpoint (global-endpoint).
    """

    Routes: "builtins.list[SESV2MultiRegionEndpointRoute] | None" = Field(
        default_factory=list
    )
    """
    Contains routes information for the multi-region endpoint (global-endpoint).
    """

    Status: "Literal['CREATING', 'READY', 'FAILED', 'DELETING'] | None" = None
    """
    The status of the multi-region endpoint (global-endpoint).
    """

    CreatedTimestamp: "datetime | None" = None
    """
    The time stamp of when the multi-region endpoint (global-endpoint) was created.
    """

    LastUpdatedTimestamp: "datetime | None" = None
    """
    The time stamp of when the multi-region endpoint (global-endpoint) was last updated.
    """

class ListMultiRegionEndpointsResponse(Boto3Model):
    """
    The following elements are returned by the service.
    """

    MultiRegionEndpoints: "builtins.list[MultiRegionEndpoint] | None" = Field(
        default_factory=list
    )
    """
    An array that contains key multi-region endpoint (global-endpoint) properties.
    """

    NextToken: "str | None" = None
    """
    A token indicating that there are additional multi-region endpoints (global-
    endpoints) available to be listed.

    Pass this token to a subsequent ``ListMultiRegionEndpoints`` call to retrieve the
    next page.
    """

class DeleteMultiRegionEndpointResponse(Boto3Model):
    """
    An HTTP 200 response if the request succeeds, or an error message if the request
    fails.
    """

    Status: "Literal['CREATING', 'READY', 'FAILED', 'DELETING'] | None" = None
    """
    A status of the multi-region endpoint (global-endpoint) right after the delete
    request.
    """

class CreateTenantResponse(TagsDictMixin, Boto3Model):
    """
    Information about a newly created tenant.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = SESV2Tag
    TenantName: "str | None" = None
    """
    The name of the tenant.
    """

    TenantId: "str | None" = None
    """
    A unique identifier for the tenant.
    """

    TenantArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the tenant.
    """
    CreatedTimestamp: "datetime | None" = None
    """
    The date and time when the tenant was created.
    """
    Tags: "builtins.list[SESV2Tag] | None" = Field(default_factory=list)
    """
    An array of objects that define the tags (keys and values) associated with the
    tenant.
    """

    SendingStatus: "Literal['ENABLED', 'REINSTATED', 'DISABLED'] | None" = None
    """
    The status of email sending capability for the tenant.
    """

class GetTenantResponse(Boto3Model):
    """
    Information about a specific tenant.
    """

    TenantInstance: Tenant = Field(default=None, alias="Tenant")
    """
    A structure that contains details about the tenant.
    """

class TenantInfo(Boto3Model):
    """
    A structure that contains basic information about a tenant.
    """

    TenantName: "str | None" = None
    """
    The name of the tenant.
    """

    TenantId: "str | None" = None
    """
    A unique identifier for the tenant.
    """

    TenantArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the tenant.
    """

    CreatedTimestamp: "datetime | None" = None
    """
    The date and time when the tenant was created.
    """

class ListTenantsResponse(Boto3Model):
    """
    Information about tenants associated with your account.
    """

    Tenants: "builtins.list[TenantInfo] | None" = Field(default_factory=list)
    """
    An array that contains basic information about each tenant.
    """

    NextToken: "str | None" = None
    """
    A token that indicates that there are additional tenants to list.

    To view additional tenants, issue another request to ``ListTenants``, and pass this
    token in the ``NextToken`` parameter.
    """

class DeleteTenantResponse(Boto3Model):
    """
    If the action is successful, the service sends back an HTTP 200 response with an
    empty HTTP body.
    """


class GetDedicatedIpPoolResponse(Boto3Model):
    """
    The following element is returned by the service.
    """

    DedicatedIpPoolInstance: DedicatedIpPool = Field(
        default=None, alias="DedicatedIpPool"
    )
    """
    An object that contains information about a dedicated IP pool.
    """

class ListDedicatedIpPoolsResponse(Boto3Model):
    """
    A list of dedicated IP pools.
    """

    DedicatedIpPools: "builtins.list[str] | None" = Field(default_factory=list)
    """
    A list of all of the dedicated IP pools that are associated with your Amazon Web
    Services account in the current Region.
    """

    NextToken: "str | None" = None
    """
    A token that indicates that there are additional IP pools to list.

    To view additional IP pools, issue another request to ``ListDedicatedIpPools``,
    passing this token in the ``NextToken`` parameter.
    """

class DeleteDedicatedIpPoolResponse(Boto3Model):
    """
    An HTTP 200 response if the request succeeds, or an error message if the request
    fails.
    """


class GetDedicatedIpResponse(Boto3Model):
    """
    Information about a dedicated IP address.
    """

    DedicatedIpInstance: DedicatedIp = Field(default=None, alias="DedicatedIp")
    """
    An object that contains information about a dedicated IP address.
    """

class GetDedicatedIpsResponse(Boto3Model):
    """
    Information about the dedicated IP addresses that are associated with your Amazon
    Web Services account.
    """

    DedicatedIps: "builtins.list[DedicatedIp] | None" = Field(default_factory=list)
    """
    A list of dedicated IP addresses that are associated with your Amazon Web Services
    account.
    """

    NextToken: "str | None" = None
    """
    A token that indicates that there are additional dedicated IP addresses to list.

    To view additional addresses, issue another request to ``GetDedicatedIps``, passing
    this token in the ``NextToken`` parameter.
    """
