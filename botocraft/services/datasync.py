# This file is automatically generated by botocraft.  Do not edit directly.
# mypy: disable-error-code="index, override, assignment, union-attr, misc"
from pydantic import Field
from botocraft.mixins.datasync import datasync_list_fsx_lustre_locations
from botocraft.mixins.datasync import datasync_task_execution_from_start
from botocraft.mixins.datasync import datasync_refresh_after_create
from botocraft.mixins.datasync import datasync_add_tags_to_queryset
from botocraft.mixins.datasync import datasync_list_fsx_ontap_locations
from botocraft.mixins.datasync import datasync_list_azure_blob_locations
import builtins
from botocraft.services.common import Tag
from botocraft.mixins.datasync import datasync_add_tags
from .abstract import (
    Boto3Model,
    ReadonlyBoto3Model,
    PrimaryBoto3Model,
    ReadonlyPrimaryBoto3Model,
    Boto3ModelManager,
    ReadonlyBoto3ModelManager,
)
from botocraft.mixins.datasync import datasync_list_fsx_openzfs_locations
from botocraft.mixins.datasync import datasync_list_efs_locations
from botocraft.mixins.datasync import datasync_list_hdfs_locations
from botocraft.mixins.datasync import datasync_list_object_storage_locations
from botocraft.mixins.datasync import datasync_list_fsx_windows_locations
from botocraft.mixins.tags import TagsDictMixin
from .abstract import PrimaryBoto3ModelQuerySet
from botocraft.mixins.datasync import datasync_list_s3_locations
from botocraft.mixins.datasync import datasync_refresh_after_update
from botocraft.mixins.datasync import datasync_list_smb_locations
from typing import ClassVar, Literal, Any, Type as ModelType, cast
from botocraft.mixins.datasync import datasync_list_nfs_locations
from datetime import datetime

# ===============
# Managers
# ===============


class DataSyncAgentManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncAgent") -> "DataSyncAgent | None":
        """
        Activates an DataSync agent that you deploy in your storage environment.

        The activation process associates the agent with your Amazon Web Services
        account.

        Args:
            model: The :py:class:`DescribeAgentResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ActivationKey=data.get("ActivationKey"),
            AgentName=data.get("Name"),
            Tags=data.get("Tags"),
            VpcEndpointId=data.get("VpcEndpointId"),
            SubnetArns=data.get("SubnetArns"),
            SecurityGroupArns=data.get("SecurityGroupArns"),
        )
        _response = self.client.create_agent(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateAgentResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncAgent | None", response)

    @datasync_add_tags
    def get(self, AgentArn: str) -> "DataSyncAgent | None":
        """
        Returns information about an DataSync agent, such as its name, service endpoint
        type, and status.

        Args:
            AgentArn: Specifies the Amazon Resource Name (ARN) of the DataSync agent
                that you want information about.
        """
        args: dict[str, Any] = dict(AgentArn=self.serialize(AgentArn))
        _response = self.client.describe_agent(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncAgent(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_add_tags_to_queryset
    def list(
        self,
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of DataSync agents that belong to an Amazon Web Services account
        in the Amazon Web Services Region specified in the request.
        """
        paginator = self.client.get_paginator("list_agents")
        args: dict[str, Any] = dict()
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListAgentsResponse(**_response)
            if response.Agents:
                results.extend(response.Agents)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncAgent") -> "DataSyncAgent | None":
        """
        Updates the name of an DataSync agent.

        Args:
            model: The :py:class:`DescribeAgentResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(AgentArn=data.get("AgentArn"), Name=data.get("Name"))
        _response = self.client.update_agent(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateAgentResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncAgent | None", response)

    def delete(self, AgentArn: str) -> None:
        """
        Removes an DataSync agent resource from your Amazon Web Services account.

        Args:
            AgentArn: The Amazon Resource Name (ARN) of the agent to delete. Use the
                ``ListAgents`` operation to return a list
                of agents for your account and Amazon Web Services Region.
        """
        args: dict[str, Any] = dict(AgentArn=self.serialize(AgentArn))
        self.client.delete_agent(**{k: v for k, v in args.items() if v is not None})


class DataSyncTaskManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncTask") -> "DataSyncTask | None":
        """
        Configures a *task*, which defines where and how DataSync transfers your data.

        Args:
            model: The :py:class:`DescribeTaskResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            SourceLocationArn=data.get("SourceLocationArn"),
            DestinationLocationArn=data.get("DestinationLocationArn"),
            CloudWatchLogGroupArn=data.get("CloudWatchLogGroupArn"),
            Name=data.get("Name"),
            Options=data.get("Options"),
            Excludes=data.get("Excludes"),
            Schedule=data.get("Schedule"),
            Tags=data.get("Tags"),
            Includes=data.get("Includes"),
            ManifestConfig=data.get("ManifestConfig"),
            TaskReportConfig=data.get("TaskReportConfig"),
            TaskMode=data.get("TaskMode"),
        )
        _response = self.client.create_task(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateTaskResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncTask | None", response)

    @datasync_add_tags
    def get(self, TaskArn: str) -> "DataSyncTask | None":
        """
        Provides information about a *task*, which defines where and how DataSync
        transfers your data.

        Args:
            TaskArn: Specifies the Amazon Resource Name (ARN) of the transfer task that
                you want information about.
        """
        args: dict[str, Any] = dict(TaskArn=self.serialize(TaskArn))
        _response = self.client.describe_task(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncTask(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_add_tags_to_queryset
    def list(
        self, *, Filters: "builtins.list[TaskFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of the DataSync tasks you created.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListTasks``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListTasks`` with filter name ``LocationId`` and
                ``Operator Equals`` with the ARN for the location.
        """
        paginator = self.client.get_paginator("list_tasks")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = DataSyncListTasksResponse(**_response)
            if response.Tasks:
                results.extend(response.Tasks)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncTask") -> "DataSyncTask | None":
        """
        Updates the configuration of a *task*, which defines where and how DataSync
        transfers your data.

        Args:
            model: The :py:class:`DescribeTaskResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            TaskArn=data.get("TaskArn"),
            Options=data.get("Options"),
            Excludes=data.get("Excludes"),
            Schedule=data.get("Schedule"),
            Name=data.get("Name"),
            CloudWatchLogGroupArn=data.get("CloudWatchLogGroupArn"),
            Includes=data.get("Includes"),
            ManifestConfig=data.get("ManifestConfig"),
            TaskReportConfig=data.get("TaskReportConfig"),
        )
        _response = self.client.update_task(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateTaskResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncTask | None", response)

    def delete(self, TaskArn: str) -> None:
        """
        Deletes a transfer task resource from DataSync.

        Args:
            TaskArn: Specifies the Amazon Resource Name (ARN) of the task that you want
                to delete.
        """
        args: dict[str, Any] = dict(TaskArn=self.serialize(TaskArn))
        self.client.delete_task(**{k: v for k, v in args.items() if v is not None})

    @datasync_task_execution_from_start
    def start_task_execution(
        self,
        TaskArn: str,
        *,
        OverrideOptions: "DataSyncOptions | None" = None,
        Includes: "builtins.list[DataSyncFilterRule] | None" = None,
        Excludes: "builtins.list[DataSyncFilterRule] | None" = None,
        ManifestConfig: "DataSyncManifestConfig | None" = None,
        TaskReportConfig: "DataSyncTaskReportConfig | None" = None,
        Tags: "builtins.list[TagListEntry] | None" = None,
    ) -> "DataSyncTaskExecution | None":
        """
        Starts an DataSync transfer task.

        For each task, you can only run one task execution at a time.
                Args:
                    TaskArn: Specifies the Amazon Resource Name (ARN) of the task that
                        you want to start.

                Keyword Args:
                    OverrideOptions: Indicates how your transfer task is configured.
                        These options include how DataSync handles files,
                        objects, and their associated metadata during your transfer. You
                        also can specify how to verify data integrity, set
                        bandwidth limits for your task, among other options.
                    Includes: Specifies a list of filter rules that determines which
                        files to include when running a task. The pattern
                        should contain a single filter string that consists of the
                        patterns to include. The patterns are delimited by "|"
                        (that is, a pipe), for example, ``"/folder1|/folder2"``.
                    Excludes: Specifies a list of filter rules that determines which
                        files to exclude from a task. The list contains a
                        single filter string that consists of the patterns to exclude.
                        The patterns are delimited by "|" (that is, a pipe),
                        for example, ``"/folder1|/folder2"``.
                    ManifestConfig: Configures a manifest, which is a list of files or
                        objects that you want DataSync to transfer. For
                        more information and configuration examples, see `Specifying
                        what DataSync transfers by using a manifest
                        <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html>`_.
                    TaskReportConfig: Specifies how you want to configure a task report,
                        which provides detailed information about your
                        DataSync transfer. For more information, see `Monitoring your
                        DataSync transfers with task reports
                        <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
                    Tags: Specifies the tags that you want to apply to the Amazon
                        Resource Name (ARN) representing the task execution.
        """
        args: dict[str, Any] = dict(
            TaskArn=self.serialize(TaskArn),
            OverrideOptions=self.serialize(OverrideOptions),
            Includes=self.serialize(Includes),
            Excludes=self.serialize(Excludes),
            ManifestConfig=self.serialize(ManifestConfig),
            TaskReportConfig=self.serialize(TaskReportConfig),
            Tags=self.serialize(Tags),
        )
        _response = self.client.start_task_execution(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = StartTaskExecutionResponse(**_response)

        results: "DataSyncTaskExecution | None" = None
        if response is not None:
            results = response

        self.sessionize(results)
        return cast("DataSyncTaskExecution | None", results)


class DataSyncTaskExecutionManager(Boto3ModelManager):
    service_name: str = "datasync"

    def get(self, TaskExecutionArn: str) -> "DataSyncTaskExecution | None":
        """
        Provides information about an execution of your DataSync task.

        You can use this operation to help monitor the progress of an ongoing data
        transfer or check the results of the transfer.

        Args:
            TaskExecutionArn: Specifies the Amazon Resource Name (ARN) of the task
                execution that you want information about.
        """
        args: dict[str, Any] = dict(TaskExecutionArn=self.serialize(TaskExecutionArn))
        _response = self.client.describe_task_execution(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncTaskExecution(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    def list(self, *, TaskArn: "str | None" = None) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of executions for an DataSync transfer task.

        Keyword Args:
            TaskArn: Specifies the Amazon Resource Name (ARN) of the task that you want
                execution information about.
        """
        paginator = self.client.get_paginator("list_task_executions")
        args: dict[str, Any] = dict(TaskArn=self.serialize(TaskArn))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListTaskExecutionsResponse(**_response)
            if response.TaskExecutions:
                results.extend(response.TaskExecutions)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncTaskExecution") -> "DataSyncTaskExecution | None":
        """
        Updates the configuration of a running DataSync task execution.

        Args:
            model: The :py:class:`DescribeTaskExecutionResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            TaskExecutionArn=data.get("TaskExecutionArn"), Options=data.get("Options")
        )
        _response = self.client.update_task_execution(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateTaskExecutionResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncTaskExecution | None", response)

    def cancel(self, TaskExecutionArn: str) -> None:
        """
        Stops an DataSync task execution that's in progress.

        The transfer of some files are abruptly interrupted. File contents that're
        transferred to the destination might be incomplete or inconsistent with the
        source files.

        Args:
            TaskExecutionArn: The Amazon Resource Name (ARN) of the task execution to
                stop.
        """
        args: dict[str, Any] = dict(TaskExecutionArn=self.serialize(TaskExecutionArn))
        self.client.cancel_task_execution(
            **{k: v for k, v in args.items() if v is not None}
        )


class DataSyncLocationAzureBlobManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationAzureBlob"
    ) -> "DataSyncLocationAzureBlob | None":
        """
        Creates a transfer *location* for a Microsoft Azure Blob Storage container.

        DataSync can use this location as a transfer source or destination. You can make
        transfers with or without a `DataSync
        agent <https://docs.aws.amazon.com/datasync/latest/userguide/creating-azure-blob-location.html#azure-blob-creating-
        agent>`_ that connects to your container.

        Args:
            model: The :py:class:`DescribeLocationAzureBlobResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ContainerUrl=data.get("ContainerUrl"),
            AuthenticationType=data.get("AuthenticationType"),
            SasConfiguration=data.get("SasConfiguration"),
            BlobType=data.get("BlobType"),
            AccessTier=data.get("AccessTier"),
            Subdirectory=data.get("Subdirectory"),
            AgentArns=data.get("AgentArns"),
            Tags=data.get("Tags"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.create_location_azure_blob(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationAzureBlobResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationAzureBlob | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationAzureBlob | None":
        """
        Provides details about how an DataSync transfer location for Microsoft Azure
        Blob Storage is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of your Azure Blob
                Storage transfer location.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_azure_blob(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationAzureBlob(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_azure_blob_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationAzureBlob"
    ) -> "DataSyncLocationAzureBlob | None":
        """
        Modifies the following configurations of the Microsoft Azure Blob Storage
        transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationAzureBlobResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            AuthenticationType=data.get("AuthenticationType"),
            SasConfiguration=data.get("SasConfiguration"),
            BlobType=data.get("BlobType"),
            AccessTier=data.get("AccessTier"),
            AgentArns=data.get("AgentArns"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.update_location_azure_blob(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationAzureBlobResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationAzureBlob | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationEfsManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncLocationEfs") -> "DataSyncLocationEfs | None":
        """
        Creates a transfer *location* for an Amazon EFS file system.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationEfsResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            EfsFilesystemArn=data.get("EfsFilesystemArn"),
            Ec2Config=data.get("Ec2Config"),
            Subdirectory=data.get("Subdirectory"),
            Tags=data.get("Tags"),
            AccessPointArn=data.get("AccessPointArn"),
            FileSystemAccessRoleArn=data.get("FileSystemAccessRoleArn"),
            InTransitEncryption=data.get("InTransitEncryption"),
        )
        _response = self.client.create_location_efs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationEfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationEfs | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationEfs | None":
        """
        Provides details about how an DataSync transfer location for an Amazon EFS file
        system is configured.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the Amazon EFS file system
                location that you want information about.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_efs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationEfs(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_efs_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncLocationEfs") -> "DataSyncLocationEfs | None":
        """
        Modifies the following configuration parameters of the Amazon EFS transfer
        location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationEfsResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            AccessPointArn=data.get("AccessPointArn"),
            FileSystemAccessRoleArn=data.get("FileSystemAccessRoleArn"),
            InTransitEncryption=data.get("InTransitEncryption"),
        )
        _response = self.client.update_location_efs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationEfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationEfs | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationFsxLustreManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationFsxLustre"
    ) -> "DataSyncLocationFsxLustre | None":
        """
        Creates a transfer *location* for an Amazon FSx for Lustre file system.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationFsxLustreResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            FsxFilesystemArn=data.get("FsxFilesystemArn"),
            SecurityGroupArns=data.get("SecurityGroupArns"),
            Subdirectory=data.get("Subdirectory"),
            Tags=data.get("Tags"),
        )
        _response = self.client.create_location_fsx_lustre(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationFsxLustreResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxLustre | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationFsxLustre | None":
        """
        Provides details about how an DataSync transfer location for an Amazon FSx for
        Lustre file system is configured.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the FSx for Lustre location
                to describe.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_fsx_lustre(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationFsxLustre(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_fsx_lustre_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationFsxLustre"
    ) -> "DataSyncLocationFsxLustre | None":
        """
        Modifies the following configuration parameters of the Amazon FSx for Lustre
        transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationFsxLustreResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"), Subdirectory=data.get("Subdirectory")
        )
        _response = self.client.update_location_fsx_lustre(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationFsxLustreResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxLustre | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationFsxOntapManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationFsxOntap"
    ) -> "DataSyncLocationFsxOntap | None":
        """
        Creates a transfer *location* for an Amazon FSx for NetApp ONTAP file system.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationFsxOntapResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Protocol=data.get("Protocol"),
            SecurityGroupArns=data.get("SecurityGroupArns"),
            StorageVirtualMachineArn=data.get("StorageVirtualMachineArn"),
            Subdirectory=data.get("Subdirectory"),
            Tags=data.get("Tags"),
        )
        _response = self.client.create_location_fsx_ontap(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationFsxOntapResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxOntap | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationFsxOntap | None":
        """
        Provides details about how an DataSync transfer location for an Amazon FSx for
        NetApp ONTAP file system is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the FSx for ONTAP
                file system location that you want
                information about.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_fsx_ontap(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationFsxOntap(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_fsx_ontap_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationFsxOntap"
    ) -> "DataSyncLocationFsxOntap | None":
        """
        Modifies the following configuration parameters of the Amazon FSx for NetApp
        ONTAP transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationFsxOntapResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Protocol=data.get("Protocol"),
            Subdirectory=data.get("Subdirectory"),
        )
        _response = self.client.update_location_fsx_ontap(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationFsxOntapResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxOntap | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationFsxOpenZfsManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationFsxOpenZfs"
    ) -> "DataSyncLocationFsxOpenZfs | None":
        """
        Creates a transfer *location* for an Amazon FSx for OpenZFS file system.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationFsxOpenZfsResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            FsxFilesystemArn=data.get("FsxFilesystemArn"),
            Protocol=data.get("Protocol"),
            SecurityGroupArns=data.get("SecurityGroupArns"),
            Subdirectory=data.get("Subdirectory"),
            Tags=data.get("Tags"),
        )
        _response = self.client.create_location_fsx_open_zfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationFsxOpenZfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxOpenZfs | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationFsxOpenZfs | None":
        """
        Provides details about how an DataSync transfer location for an Amazon FSx for
        OpenZFS file system is configured.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the FSx for OpenZFS location
                to describe.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_fsx_open_zfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationFsxOpenZfs(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_fsx_openzfs_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationFsxOpenZfs"
    ) -> "DataSyncLocationFsxOpenZfs | None":
        """
        Modifies the following configuration parameters of the Amazon FSx for OpenZFS
        transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationFsxOpenZfsResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Protocol=data.get("Protocol"),
            Subdirectory=data.get("Subdirectory"),
        )
        _response = self.client.update_location_fsx_open_zfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationFsxOpenZfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxOpenZfs | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationFsxWindowsManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationFsxWindows"
    ) -> "DataSyncLocationFsxWindows | None":
        """
        Creates a transfer *location* for an Amazon FSx for Windows File Server file
        system.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationFsxWindowsResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            FsxFilesystemArn=data.get("FsxFilesystemArn"),
            SecurityGroupArns=data.get("SecurityGroupArns"),
            User=data.get("User"),
            Subdirectory=data.get("Subdirectory"),
            Tags=data.get("Tags"),
            Domain=data.get("Domain"),
            Password=data.get("Password"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.create_location_fsx_windows(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationFsxWindowsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxWindows | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationFsxWindows | None":
        """
        Provides details about how an DataSync transfer location for an Amazon FSx for
        Windows File Server file system is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the FSx for Windows
                File Server location.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_fsx_windows(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationFsxWindows(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_fsx_windows_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationFsxWindows"
    ) -> "DataSyncLocationFsxWindows | None":
        """
        Modifies the following configuration parameters of the Amazon FSx for Windows
        File Server transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationFsxWindowsResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            Domain=data.get("Domain"),
            User=data.get("User"),
            Password=data.get("Password"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.update_location_fsx_windows(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationFsxWindowsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationFsxWindows | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationHdfsManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncLocationHdfs") -> "DataSyncLocationHdfs | None":
        """
        Creates a transfer *location* for a Hadoop Distributed File System (HDFS).

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationHdfsResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            NameNodes=data.get("NameNodes"),
            AuthenticationType=data.get("AuthenticationType"),
            AgentArns=data.get("AgentArns"),
            Subdirectory=data.get("Subdirectory"),
            BlockSize=data.get("BlockSize"),
            ReplicationFactor=data.get("ReplicationFactor"),
            KmsKeyProviderUri=data.get("KmsKeyProviderUri"),
            QopConfiguration=data.get("QopConfiguration"),
            SimpleUser=data.get("SimpleUser"),
            KerberosPrincipal=data.get("KerberosPrincipal"),
            KerberosKeytab=data.get("KerberosKeytab"),
            KerberosKrb5Conf=data.get("KerberosKrb5Conf"),
            Tags=data.get("Tags"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.create_location_hdfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationHdfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationHdfs | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationHdfs | None":
        """
        Provides details about how an DataSync transfer location for a Hadoop
        Distributed File System (HDFS) is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the HDFS location.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_hdfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationHdfs(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_hdfs_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncLocationHdfs") -> "DataSyncLocationHdfs | None":
        """
        Modifies the following configuration parameters of the Hadoop Distributed File
        System (HDFS) transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationHdfsResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            NameNodes=data.get("NameNodes"),
            BlockSize=data.get("BlockSize"),
            ReplicationFactor=data.get("ReplicationFactor"),
            KmsKeyProviderUri=data.get("KmsKeyProviderUri"),
            QopConfiguration=data.get("QopConfiguration"),
            AuthenticationType=data.get("AuthenticationType"),
            SimpleUser=data.get("SimpleUser"),
            KerberosPrincipal=data.get("KerberosPrincipal"),
            KerberosKeytab=data.get("KerberosKeytab"),
            KerberosKrb5Conf=data.get("KerberosKrb5Conf"),
            AgentArns=data.get("AgentArns"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.update_location_hdfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationHdfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationHdfs | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationNfsManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncLocationNfs") -> "DataSyncLocationNfs | None":
        """
        Creates a transfer *location* for a Network File System (NFS) file server.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationNfsResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Subdirectory=data.get("Subdirectory"),
            ServerHostname=data.get("ServerHostname"),
            OnPremConfig=data.get("OnPremConfig"),
            MountOptions=data.get("MountOptions"),
            Tags=data.get("Tags"),
        )
        _response = self.client.create_location_nfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationNfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationNfs | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationNfs | None":
        """
        Provides details about how an DataSync transfer location for a Network File
        System (NFS) file server is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the NFS location
                that you want information about.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_nfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationNfs(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_nfs_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncLocationNfs") -> "DataSyncLocationNfs | None":
        """
        Modifies the following configuration parameters of the Network File System (NFS)
        transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationNfsResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            ServerHostname=data.get("ServerHostname"),
            OnPremConfig=data.get("OnPremConfig"),
            MountOptions=data.get("MountOptions"),
        )
        _response = self.client.update_location_nfs(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationNfsResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationNfs | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationObjectStorageManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(
        self, model: "DataSyncLocationObjectStorage"
    ) -> "DataSyncLocationObjectStorage | None":
        """
        Creates a transfer *location* for an object storage system.

        DataSync can use this location as a source or destination for transferring data.
        You can make transfers with or without a `DataSync
        agent <https://docs.aws.amazon.com/datasync/latest/userguide/do-i-need-datasync-agent.html#when-agent-required>`_.

        Args:
            model: The :py:class:`DescribeLocationObjectStorageResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            ServerHostname=data.get("ServerHostname"),
            BucketName=data.get("BucketName"),
            ServerPort=data.get("ServerPort"),
            ServerProtocol=data.get("ServerProtocol"),
            Subdirectory=data.get("Subdirectory"),
            AccessKey=data.get("AccessKey"),
            SecretKey=data.get("SecretKey"),
            AgentArns=data.get("AgentArns"),
            Tags=data.get("Tags"),
            ServerCertificate=data.get("ServerCertificate"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.create_location_object_storage(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationObjectStorageResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationObjectStorage | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationObjectStorage | None":
        """
        Provides details about how an DataSync transfer location for an object storage
        system is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the object storage
                system location.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_object_storage(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationObjectStorage(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_object_storage_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(
        self, model: "DataSyncLocationObjectStorage"
    ) -> "DataSyncLocationObjectStorage | None":
        """
        Modifies the following configuration parameters of the object storage transfer
        location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationObjectStorageResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            ServerPort=data.get("ServerPort"),
            ServerProtocol=data.get("ServerProtocol"),
            Subdirectory=data.get("Subdirectory"),
            ServerHostname=data.get("ServerHostname"),
            AccessKey=data.get("AccessKey"),
            SecretKey=data.get("SecretKey"),
            AgentArns=data.get("AgentArns"),
            ServerCertificate=data.get("ServerCertificate"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
        )
        _response = self.client.update_location_object_storage(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationObjectStorageResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationObjectStorage | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationS3Manager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncLocationS3") -> "DataSyncLocationS3 | None":
        """
        Creates a transfer *location* for an Amazon S3 bucket.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationS3Response` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            S3BucketArn=data.get("S3BucketArn"),
            S3Config=data.get("S3Config"),
            Subdirectory=data.get("Subdirectory"),
            S3StorageClass=data.get("S3StorageClass"),
            AgentArns=data.get("AgentArns"),
            Tags=data.get("Tags"),
        )
        _response = self.client.create_location_s3(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationS3Response(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationS3 | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationS3 | None":
        """
        Provides details about how an DataSync transfer location for an S3 bucket is
        configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the Amazon S3
                location.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_s3(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationS3(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_s3_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncLocationS3") -> "DataSyncLocationS3 | None":
        """
        Modifies the following configuration parameters of the Amazon S3 transfer
        location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationS3Response` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            S3StorageClass=data.get("S3StorageClass"),
            S3Config=data.get("S3Config"),
        )
        _response = self.client.update_location_s3(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationS3Response(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationS3 | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


class DataSyncLocationSmbManager(Boto3ModelManager):
    service_name: str = "datasync"

    @datasync_refresh_after_create
    def create(self, model: "DataSyncLocationSmb") -> "DataSyncLocationSmb | None":
        """
        Creates a transfer *location* for a Server Message Block (SMB) file server.

        DataSync can use this location as a source or destination for transferring data.

        Args:
            model: The :py:class:`DescribeLocationSmbResponse` to create.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            Subdirectory=data.get("Subdirectory"),
            ServerHostname=data.get("ServerHostname"),
            AgentArns=data.get("AgentArns"),
            User=data.get("User"),
            Domain=data.get("Domain"),
            Password=data.get("Password"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
            MountOptions=data.get("MountOptions"),
            Tags=data.get("Tags"),
            AuthenticationType=data.get("AuthenticationType"),
            DnsIpAddresses=data.get("DnsIpAddresses"),
            KerberosPrincipal=data.get("KerberosPrincipal"),
            KerberosKeytab=data.get("KerberosKeytab"),
            KerberosKrb5Conf=data.get("KerberosKrb5Conf"),
        )
        _response = self.client.create_location_smb(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = CreateLocationSmbResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationSmb | None", response)

    @datasync_add_tags
    def get(self, LocationArn: str) -> "DataSyncLocationSmb | None":
        """
        Provides details about how an DataSync transfer location for a Server Message
        Block (SMB) file server is configured.

        Args:
            LocationArn: Specifies the Amazon Resource Name (ARN) of the SMB location
                that you want information about.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        _response = self.client.describe_location_smb(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = DataSyncLocationSmb(**_response)

        if response:
            self.sessionize(response)
            return response
        return None

    @datasync_list_smb_locations
    def list(
        self, *, Filters: "builtins.list[LocationFilter] | None" = None
    ) -> PrimaryBoto3ModelQuerySet:
        """
        Returns a list of source and destination locations.

        Keyword Args:
            Filters: You can use API filters to narrow down the list of resources
                returned by ``ListLocations``. For example, to
                retrieve all tasks on a specific source location, you can use
                ``ListLocations`` with filter name ``LocationType S3``
                and ``Operator Equals``.
        """
        paginator = self.client.get_paginator("list_locations")
        args: dict[str, Any] = dict(Filters=self.serialize(Filters))
        response_iterator = paginator.paginate(
            **{k: v for k, v in args.items() if v is not None}
        )
        results = []
        for _response in response_iterator:
            if list(_response.keys()) == ["ResponseMetadata"]:
                break
            if "ResponseMetadata" in _response:
                del _response["ResponseMetadata"]
            response = ListLocationsResponse(**_response)
            if response.Locations:
                results.extend(response.Locations)
            else:
                if getattr(response, "NextToken", None):
                    continue
                break
        self.sessionize(results)
        if results and isinstance(results[0], Boto3Model):
            return PrimaryBoto3ModelQuerySet(results)
        return results

    @datasync_refresh_after_update
    def update(self, model: "DataSyncLocationSmb") -> "DataSyncLocationSmb | None":
        """
        Modifies the following configuration parameters of the Server Message Block
        (SMB) transfer location that you're using with DataSync.

        Args:
            model: The :py:class:`DescribeLocationSmbResponse` to update.
        """
        data = model.model_dump(exclude_none=True, by_alias=True)
        args = dict(
            LocationArn=data.get("LocationArn"),
            Subdirectory=data.get("Subdirectory"),
            ServerHostname=data.get("ServerHostname"),
            User=data.get("User"),
            Domain=data.get("Domain"),
            Password=data.get("Password"),
            CmkSecretConfig=data.get("CmkSecretConfig"),
            CustomSecretConfig=data.get("CustomSecretConfig"),
            AgentArns=data.get("AgentArns"),
            MountOptions=data.get("MountOptions"),
            AuthenticationType=data.get("AuthenticationType"),
            DnsIpAddresses=data.get("DnsIpAddresses"),
            KerberosPrincipal=data.get("KerberosPrincipal"),
            KerberosKeytab=data.get("KerberosKeytab"),
            KerberosKrb5Conf=data.get("KerberosKrb5Conf"),
        )
        _response = self.client.update_location_smb(
            **{k: v for k, v in args.items() if v is not None}
        )
        response = UpdateLocationSmbResponse(**_response)

        self.sessionize(response)
        return cast("DataSyncLocationSmb | None", response)

    def delete(self, LocationArn: str) -> None:
        """
        Deletes a transfer location resource from DataSync.

        Args:
            LocationArn: The Amazon Resource Name (ARN) of the location to delete.
        """
        args: dict[str, Any] = dict(LocationArn=self.serialize(LocationArn))
        self.client.delete_location(**{k: v for k, v in args.items() if v is not None})


# ==============
# Service Models
# ==============


class DataSyncPrivateLinkConfig(Boto3Model):
    """Specifies how your DataSync agent connects to Amazon Web Services using a `virtual private cloud (VPC) service
    endpoint <https://docs.aws.amazon.com/datasync/latest/userguide/choose-service-endpoint.html#choose-service-endpoint-
    vpc>`_. An agent that uses a VPC endpoint isn't accessible over the public internet.
    """

    VpcEndpointId: "str | None" = None
    """
    Specifies the ID of the VPC endpoint that your agent connects to.
    """

    PrivateLinkEndpoint: "str | None" = None
    """
Specifies the VPC endpoint provided by `Amazon Web Services
PrivateLink <https://docs.aws.amazon.com/vpc/latest/privatelink/privatelink-share-your-services.html>`_ that your agent
connects to.
    """

    SubnetArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    Specifies the ARN of the subnet where your VPC endpoint is located.

    You can only specify one ARN.
    """

    SecurityGroupArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    Specifies the Amazon Resource Names (ARN) of the security group that provides
    DataSync access to your VPC endpoint.

    You can only specify one ARN.
    """

class DataSyncPlatform(Boto3Model):
    """
    The platform-related details about the DataSync agent, such as the version number.
    """

    Version: "str | None" = None
    """
    The version of the DataSync agent.
    """

class DataSyncAgent(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeAgentResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncAgentManager

    AgentArn: "str | None" = None
    """
    The ARN of the agent.
    """

    Name: "str | None" = None
    """
    The name of the agent.
    """

    Status: Literal["ONLINE", "OFFLINE"] = Field(default=None, frozen=True)
    """
    The status of the agent.
    """

    LastConnectionTime: datetime = Field(default=None, frozen=True)
    """
    The last time that the agent was communicating with the DataSync service.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
The time that the agent was `activated <https://docs.aws.amazon.com/datasync/latest/userguide/activate-agent.html>`_.
    """

    EndpointType: Literal["PUBLIC", "PRIVATE_LINK", "FIPS", "FIPS_PRIVATE_LINK"] = (
        Field(default=None, frozen=True)
    )
    """
The type of `service endpoint <https://docs.aws.amazon.com/datasync/latest/userguide/choose-service-endpoint.html>`_ that
your agent is connected to.
    """

    PrivateLinkConfig: DataSyncPrivateLinkConfig = Field(default=None, frozen=True)
    """
The network configuration that the agent uses when connecting to a `VPC service
endpoint <https://docs.aws.amazon.com/datasync/latest/userguide/choose-service-endpoint.html#choose-service-endpoint-
vpc>`_.
    """

    Platform: DataSyncPlatform = Field(default=None, frozen=True)
    """
    The platform-related details about the agent, such as the version number.
    """

    ActivationKey: str | None = None
    SecurityGroupArns: builtins.list[str] | None = Field(default_factory=list)
    SubnetArns: builtins.list[str] | None = Field(default_factory=list)
    Tags: builtins.list[Tag] | None = Field(default_factory=list)
    VpcEndpointId: str | None = None

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`AgentArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.AgentArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`AgentArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.AgentArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`Name` attribute.

        Returns:
            The name of the model instance.
        """
        return self.Name

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`AgentArn` attribute.
        """
        return hash(self.AgentArn)
class DataSyncOptions(Boto3Model):
    """
    Indicates how your transfer task is configured.

    These options include how DataSync handles files, objects, and their associated
    metadata during your transfer. You also can specify how to verify data integrity,
    set bandwidth limits for your task, among other options.

    Each option has a default value. Unless you need to, you don't have to configure any
    option before calling
    `StartTaskExecution <https://docs.aws.amazon.com/datasync/latest/userguide/API_StartTaskExecution.html>`_.

    You also can override your task options for each task execution. For example, you
    might want to adjust the ``LogLevel``
    for an individual execution.
    """

    VerifyMode: "Literal['POINT_IN_TIME_CONSISTENT', 'ONLY_FILES_TRANSFERRED', 'NONE'] | None" = None
    """
    Specifies if and how DataSync checks the integrity of your data at the end of your
    transfer.
    """

    OverwriteMode: "Literal['ALWAYS', 'NEVER'] | None" = None
    """
    Specifies whether DataSync should modify or preserve data at the destination
    location.
    """

    Atime: "Literal['NONE', 'BEST_EFFORT'] | None" = None
    """
    Specifies whether to preserve metadata indicating the last time a file was read or
    written to.
    """

    Mtime: "Literal['NONE', 'PRESERVE'] | None" = None
    """
    Specifies whether to preserve metadata indicating the last time that a file was
    written to before the ``PREPARING`` step of your task execution.

    This option is required when you need to run the a task more than once.
    """
    Uid: "Literal['NONE', 'INT_VALUE', 'NAME', 'BOTH'] | None" = None
    """
    Specifies the POSIX user ID (UID) of the file's owner.
    """
    Gid: "Literal['NONE', 'INT_VALUE', 'NAME', 'BOTH'] | None" = None
    """
    Specifies the POSIX group ID (GID) of the file's owners.
    """

    PreserveDeletedFiles: "Literal['PRESERVE', 'REMOVE'] | None" = None
    """
    Specifies whether files in the destination location that don't exist in the source
    should be preserved.

    This option can affect your Amazon S3 storage cost. If your task deletes objects,
    you might incur minimum storage duration charges for certain storage classes. For
    detailed information, see
    `Considerations when working with Amazon S3 storage classes in DataSync <https://docs.aws.amazon.com/datasync/latest/userguide/create-s3-location.html#using-storage-classes>`_.
    """

    PreserveDevices: "Literal['NONE', 'PRESERVE'] | None" = None
    """
    Specifies whether DataSync should preserve the metadata of block and character
    devices in the source location and recreate the files with that device name and
    metadata on the destination.

    DataSync copies only the name and metadata of such devices.
    """

    PosixPermissions: "Literal['NONE', 'PRESERVE'] | None" = None
    """
    Specifies which users or groups can access a file for a specific purpose such as
    reading, writing, or execution of the file.
    """

    BytesPerSecond: "int | None" = None
    """
    Limits the bandwidth used by a DataSync task.

    For example, if you want DataSync to use a maximum of 1 MB, set this value to
    ``1048576`` (``=1024*1024``).
    """

    TaskQueueing: "Literal['ENABLED', 'DISABLED'] | None" = None
    """
Specifies whether your transfer tasks should be put into a queue during certain
scenarios when `running multiple
tasks <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#running-multiple-tasks>`_. This is ``ENABLED``
by default.
    """

    LogLevel: "Literal['OFF', 'BASIC', 'TRANSFER'] | None" = None
    """
    Specifies the type of logs that DataSync publishes to a Amazon CloudWatch Logs log
    group.

    To specify the log group, see
    `CloudWatchLogGroupArn <https://docs.aws.amazon.com/datasync/latest/userguide/API_CreateTask.html#DataSync-CreateTask-
    request-CloudWatchLogGroupArn>`_.
    """
    TransferMode: "Literal['CHANGED', 'ALL'] | None" = None
    """
    Specifies whether DataSync transfers only the data (including metadata) that differs
    between locations following an initial copy or transfers all data every time you run
    the task.

    If you're planning on recurring transfers, you might only want to transfer what's
    changed since your previous task execution.
    """

    SecurityDescriptorCopyFlags: "Literal['NONE', 'OWNER_DACL', 'OWNER_DACL_SACL'] | None" = None
    """
    Specifies which components of the SMB security descriptor are copied from source to
    destination objects.
    """

    ObjectTags: "Literal['PRESERVE', 'NONE'] | None" = None
    """
    Specifies whether you want DataSync to ``PRESERVE`` object tags (default behavior)
    when transferring between object storage systems.

    If you want your DataSync task to ignore object tags, specify the ``NONE`` value.
    """

class DataSyncFilterRule(Boto3Model):
    """
    Specifies which files, folders, and objects to include or exclude when transferring
    files from source to destination.
    """

    FilterType: "Literal['SIMPLE_PATTERN'] | None" = None
    """
    The type of filter rule to apply.

    DataSync only supports the SIMPLE_PATTERN rule type.
    """

    Value: "str | None" = None
    """
    A single filter string that consists of the patterns to include or exclude.

    The patterns are delimited by "|" (that is, a pipe), for example:
    ``/folder1|/folder2``
    """

class DataSyncTaskSchedule(Boto3Model):
    """Configures your DataSync task to run on a `schedule <https://docs.aws.amazon.com/datasync/latest/userguide/task-
    scheduling.html>`_ (at a minimum interval of 1 hour).
    """

    ScheduleExpression: str
    """
    Specifies your task schedule by using a cron or rate expression.
    """

    Status: "Literal['ENABLED', 'DISABLED'] | None" = None
    """
    Specifies whether to enable or disable your task schedule.

    Your schedule is enabled by default, but there can be situations where you need to
    disable it. For example, you might need to pause a recurring transfer to fix an
    issue with your task or perform maintenance on your storage system.
    """
class S3ManifestConfig(Boto3Model):
    """
    Specifies the S3 bucket where you're hosting the manifest that you want DataSync to
    use.

    For more information and configuration examples, see
    `Specifying what DataSync transfers by using a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html>`_.
    """

    ManifestObjectPath: str
    """
    Specifies the Amazon S3 object key of your manifest.

    This can include a prefix (for example, ``prefix/my- manifest.csv``).
    """

    BucketAccessRoleArn: str
    """
    Specifies the Identity and Access Management (IAM) role that allows DataSync to
    access your manifest.

    For more information, see
    `Providing DataSync access to your manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html#transferring-with-
    manifest-access>`_.
    """
    S3BucketArn: str
    """
    Specifies the Amazon Resource Name (ARN) of the S3 bucket where you're hosting your
    manifest.
    """
    ManifestObjectVersionId: "str | None" = None
    """
    Specifies the object version ID of the manifest that you want DataSync to use.

    If you don't set this, DataSync uses the latest version of the object.
    """

class SourceManifestConfig(Boto3Model):
    """
    Specifies the manifest that you want DataSync to use and where it's hosted.

    For more information and configuration examples, see
    `Specifying what DataSync transfers by using a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html>`_.
    """

    S3: S3ManifestConfig
    """
    Specifies the S3 bucket where you're hosting your manifest.
    """


class DataSyncManifestConfig(Boto3Model):
    """
    Configures a manifest, which is a list of files or objects that you want DataSync to
    transfer.

    For more information and configuration examples, see
    `Specifying what DataSync transfers by using a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html>`_.
    """

    Action: "Literal['TRANSFER'] | None" = None
    """
    Specifies what DataSync uses the manifest for.
    """

    Format: "Literal['CSV'] | None" = None
    """
    Specifies the file format of your manifest.

    For more information, see
    `Creating a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html#transferring-with-
    manifest-create>`_.
    """

    Source: "SourceManifestConfig | None" = None
    """
    Specifies the manifest that you want DataSync to use and where it's hosted.
    """

class ReportDestinationS3(Boto3Model):
    """Specifies the Amazon S3 bucket where DataSync uploads your `task
    report <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    Subdirectory: "str | None" = None
    """
    Specifies a bucket prefix for your report.
    """

    S3BucketArn: str
    """
    Specifies the ARN of the S3 bucket where DataSync uploads your report.
    """

    BucketAccessRoleArn: str
    """
    Specifies the Amazon Resource Name (ARN) of the IAM policy that allows DataSync to
    upload a task report to your S3 bucket.

    For more information, see
    `Allowing DataSync to upload a task report to an Amazon S3 bucket <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

class ReportDestination(Boto3Model):
    """Specifies where DataSync uploads your `task report <https://docs.aws.amazon.com/datasync/latest/userguide/task-
    reports.html>`_.
    """

    S3: "ReportDestinationS3 | None" = None
    """
    Specifies the Amazon S3 bucket where DataSync uploads your task report.
    """

class ReportOverride(Boto3Model):
    """Specifies the level of detail for a particular aspect of your DataSync `task
    report <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    ReportLevel: "Literal['ERRORS_ONLY', 'SUCCESSES_AND_ERRORS'] | None" = None
    """
    Specifies whether your task report includes errors only or successes and errors.
    """

class ReportOverrides(Boto3Model):
    """The level of detail included in each aspect of your DataSync `task
    report <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    Transferred: "ReportOverride | None" = None
    """
    Specifies the level of reporting for the files, objects, and directories that
    DataSync attempted to transfer.
    """

    Verified: "ReportOverride | None" = None
    """
    Specifies the level of reporting for the files, objects, and directories that
    DataSync attempted to verify at the end of your transfer.
    """
    Deleted: "ReportOverride | None" = None
    """
    Specifies the level of reporting for the files, objects, and directories that
    DataSync attempted to delete in your destination location.

    This only applies if you
    `configure your task <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html>`_
    to delete data in the destination
    that isn't in the source.
    """
    Skipped: "ReportOverride | None" = None
    """
    Specifies the level of reporting for the files, objects, and directories that
    DataSync attempted to skip during your transfer.
    """
class DataSyncTaskReportConfig(Boto3Model):
    """
    Specifies how you want to configure a task report, which provides detailed
    information about for your DataSync transfer.

    For more information, see
    `Task reports <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    Destination: "ReportDestination | None" = None
    """
    Specifies the Amazon S3 bucket where DataSync uploads your task report.

    For more information, see
    `Task reports <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html#task-report-access>`_.
    """

    OutputType: "Literal['SUMMARY_ONLY', 'STANDARD'] | None" = None
    """
Specifies the type of task report that you want:
    """

    ReportLevel: "Literal['ERRORS_ONLY', 'SUCCESSES_AND_ERRORS'] | None" = None
    """
    Specifies whether you want your task report to include only what went wrong with
    your transfer or a list of what succeeded and didn't.
    """

    ObjectVersionIds: "Literal['INCLUDE', 'NONE'] | None" = None
    """
    Specifies whether your task report includes the new version of each object
    transferred into an S3 bucket.

    This only applies if you
    `enable versioning on your bucket <https://docs.aws.amazon.com/AmazonS3/latest/userguide/manage-
    versioning-examples.html>`_. Keep in mind that setting this to ``INCLUDE`` can
    increase the duration of your task
    execution.
    """

    Overrides: "ReportOverrides | None" = None
    """
    Customizes the reporting level for aspects of your task report.

    For example, your report might generally only include errors, but you could specify
    that you want a list of successes and errors just for the files that DataSync
    attempted to delete in your destination location.
    """

class DataSyncTaskScheduleDetails(Boto3Model):
    """Provides information about your DataSync `task schedule <https://docs.aws.amazon.com/datasync/latest/userguide/task-
    scheduling.html>`_.
    """

    StatusUpdateTime: "datetime | None" = None
    """
    Indicates the last time the status of your task schedule changed.

    For example, if DataSync automatically disables your schedule because of a repeated
    error, you can see when the schedule was disabled.
    """

    DisabledReason: "str | None" = None
    """
    Provides a reason if the task schedule is disabled.
    """

    DisabledBy: "Literal['USER', 'SERVICE'] | None" = None
    """
    Indicates how your task schedule was disabled.
    """

class DataSyncTask(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeTaskResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncTaskManager

    TaskArn: "str | None" = None
    """
    The ARN of your task.
    """

    Status: Literal["AVAILABLE", "CREATING", "QUEUED", "RUNNING", "UNAVAILABLE"] = (
        Field(default=None, frozen=True)
    )
    """
    The status of your task.

    For information about what each status means, see
    `Task statuses <https://docs.aws.amazon.com/datasync/latest/userguide/create-task-how-to.html#understand-task-creation-
    statuses>`_.
    """

    Name: "str | None" = None
    """
    The name of your task.
    """

    CurrentTaskExecutionArn: str = Field(default=None, frozen=True)
    """
    The ARN of the most recent task execution.
    """

    SourceLocationArn: "str | None" = None
    """
    The ARN of your transfer's source location.
    """

    DestinationLocationArn: "str | None" = None
    """
    The ARN of your transfer's destination location.
    """

    CloudWatchLogGroupArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of an Amazon CloudWatch log group for monitoring your
    task.
    """

    SourceNetworkInterfaceArns: "builtins.list[str]" = Field(
        default_factory=list, frozen=True
    )
    """
The ARNs of the `network interfaces <https://docs.aws.amazon.com/datasync/latest/userguide/datasync-
network.html#required-network-interfaces>`_ that DataSync created for your source
location.
    """
    DestinationNetworkInterfaceArns: "builtins.list[str]" = Field(
        default_factory=list, frozen=True
    )
    """
The ARNs of the `network interfaces <https://docs.aws.amazon.com/datasync/latest/userguide/datasync-
network.html#required-network-interfaces>`_ that DataSync created for your destination
location.
    """

    Options: "DataSyncOptions | None" = None
    """
    The task's settings.

    For example, what file metadata gets preserved, how data integrity gets verified at
    the end of your transfer, bandwidth limits, among other options.
    """

    Excludes: "builtins.list[DataSyncFilterRule] | None" = Field(default_factory=list)
    """
    The exclude filters that define the files, objects, and folders in your source
    location that you don't want DataSync to transfer.

    For more information and examples, see
    `Specifying what DataSync transfers by using filters <https://docs.aws.amazon.com/datasync/latest/userguide/filtering.html>`_.
    """

    Schedule: "DataSyncTaskSchedule | None" = None
    """
    The schedule for when you want your task to run.

    For more information, see
    `Scheduling your task <https://docs.aws.amazon.com/datasync/latest/userguide/task-scheduling.html>`_.
    """

    ErrorCode: str = Field(default=None, frozen=True)
    """
    If there's an issue with your task, you can use the error code to help you
    troubleshoot the problem.

    For more information, see
    `Troubleshooting issues with DataSync transfers <https://docs.aws.amazon.com/datasync/latest/userguide/troubleshooting-datasync-locations-tasks.html>`_.
    """

    ErrorDetail: str = Field(default=None, frozen=True)
    """
    If there's an issue with your task, you can use the error details to help you
    troubleshoot the problem.

    For more information, see
    `Troubleshooting issues with DataSync transfers <https://docs.aws.amazon.com/datasync/latest/userguide/troubleshooting-datasync-locations-tasks.html>`_.
    """
    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the task was created.
    """

    Includes: "builtins.list[DataSyncFilterRule] | None" = Field(default_factory=list)
    """
    The include filters that define the files, objects, and folders in your source
    location that you want DataSync to transfer.

    For more information and examples, see
    `Specifying what DataSync transfers by using filters <https://docs.aws.amazon.com/datasync/latest/userguide/filtering.html>`_.
    """

    ManifestConfig: "DataSyncManifestConfig | None" = None
    """
    The configuration of the manifest that lists the files or objects that you want
    DataSync to transfer.

    For more information, see
    `Specifying what DataSync transfers by using a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-manifest.html>`_.
    """

    TaskReportConfig: "DataSyncTaskReportConfig | None" = None
    """
    The configuration of your task report, which provides detailed information about
    your DataSync transfer.

    For more information, see
    `Monitoring your DataSync transfers with task reports <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    ScheduleDetails: DataSyncTaskScheduleDetails = Field(default=None, frozen=True)
    """
The details about your `task schedule <https://docs.aws.amazon.com/datasync/latest/userguide/task-scheduling.html>`_.
    """

    TaskMode: "Literal['BASIC', 'ENHANCED'] | None" = None
    """
    The task mode that you're using.

    For more information, see
    `Choosing a task mode for your data transfer <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`TaskArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.TaskArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`TaskArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.TaskArn

    @property
    def name(self) -> str | None:
        """
        Return the name of the model.

        This is the value of the :py:attr:`Name` attribute.

        Returns:
            The name of the model instance.
        """
        return self.Name
    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`TaskArn` attribute.
        """
        return hash(self.TaskArn)
class DataSyncTaskExecutionResultDetail(Boto3Model):
    """
    Provides detailed information about the result of your DataSync task execution.
    """

    PrepareDuration: "int | None" = None
    """
    The time in milliseconds that your task execution was in the ``PREPARING`` step.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-
    statuses>`_.
    """

    PrepareStatus: "Literal['PENDING', 'SUCCESS', 'ERROR'] | None" = None
    """
    The status of the ``PREPARING`` step for your task execution.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-statuses>`_.
    """

    TotalDuration: "int | None" = None
    """
    The time in milliseconds that your task execution ran.
    """

    TransferDuration: "int | None" = None
    """
    The time in milliseconds that your task execution was in the ``TRANSFERRING`` step.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-
    statuses>`_.
    """

    TransferStatus: "Literal['PENDING', 'SUCCESS', 'ERROR'] | None" = None
    """
    The status of the ``TRANSFERRING`` step for your task execution.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-statuses>`_.
    """

    VerifyDuration: "int | None" = None
    """
    The time in milliseconds that your task execution was in the ``VERIFYING`` step.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-
    statuses>`_.
    """

    VerifyStatus: "Literal['PENDING', 'SUCCESS', 'ERROR'] | None" = None
    """
    The status of the ``VERIFYING`` step for your task execution.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-statuses>`_.
    """

    ErrorCode: "str | None" = None
    """
    An error that DataSync encountered during your task execution.

    You can use this information to help
    `troubleshoot issues <https://docs.aws.amazon.com/datasync/latest/userguide/troubleshooting-datasync-locations-tasks.html>`_.
    """

    ErrorDetail: "str | None" = None
    """
    The detailed description of an error that DataSync encountered during your task
    execution.

    You can use this information to help
    `troubleshoot issues <https://docs.aws.amazon.com/datasync/latest/userguide/troubleshooting-datasync-locations-
    tasks.html>`_.
    """

class DataSyncReportResult(Boto3Model):
    """Indicates whether DataSync created a complete `task report <https://docs.aws.amazon.com/datasync/latest/userguide/task-
    reports.html>`_ for your transfer.
    """

    Status: "Literal['PENDING', 'SUCCESS', 'ERROR'] | None" = None
    """
    Indicates whether DataSync is still working on your report, created a report, or
    can't create a complete report.
    """

    ErrorCode: "str | None" = None
    """
    Indicates the code associated with the error if DataSync can't create a complete
    report.
    """

    ErrorDetail: "str | None" = None
    """
    Provides details about issues creating a report.
    """

class TaskExecutionFilesListedDetail(Boto3Model):
    """
    The number of files or objects that DataSync finds at your locations.

    Applies only to
    `Enhanced mode tasks <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

    AtSource: "int | None" = None
    """
    The number of files or objects that DataSync finds at your source location.
    """

    AtDestinationForDelete: "int | None" = None
    """
    The number of files or objects that DataSync finds at your destination location.

    This counter is only applicable if you
    `configure your task <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html#task-option-file-
    object-handling>`_ to delete data in the destination that isn't in the source.
    """

class TaskExecutionFilesFailedDetail(Boto3Model):
    """
    The number of files or objects that DataSync fails to prepare, transfer, verify, and
    delete during your task execution.

    Applies only to
    `Enhanced mode tasks <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

    Prepare: "int | None" = None
    """
    The number of files or objects that DataSync fails to prepare during your task
    execution.
    """

    Transfer: "int | None" = None
    """
    The number of files or objects that DataSync fails to transfer during your task
    execution.
    """

    Verify: "int | None" = None
    """
    The number of files or objects that DataSync fails to verify during your task
    execution.
    """

    Delete: "int | None" = None
    """
    The number of files or objects that DataSync fails to delete during your task
    execution.
    """

class TaskExecutionFoldersListedDetail(Boto3Model):
    """
    The number of directories that DataSync finds at your locations.

    Applies only to
    `Enhanced mode tasks <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """
    AtSource: "int | None" = None
    """
    The number of directories that DataSync finds at your source location.
    """

    AtDestinationForDelete: "int | None" = None
    """
    The number of directories that DataSync finds at your destination location.

    This counter is only applicable if you
    `configure your task <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html#task-option-file-
    object-handling>`_ to delete data in the destination that isn't in the source.
    """

class TaskExecutionFoldersFailedDetail(Boto3Model):
    """
    The number of directories that DataSync fails to list, prepare, transfer, verify,
    and delete during your task execution.

    Applies only to
    `Enhanced mode tasks <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

    List: "int | None" = None
    """
    The number of directories that DataSync fails to list during your task execution.
    """

    Prepare: "int | None" = None
    """
    The number of directories that DataSync fails to prepare during your task execution.
    """

    Transfer: "int | None" = None
    """
    The number of directories that DataSync fails to transfer during your task
    execution.
    """

    Verify: "int | None" = None
    """
    The number of directories that DataSync fails to verify during your task execution.
    """

    Delete: "int | None" = None
    """
    The number of directories that DataSync fails to delete during your task execution.
    """

class DataSyncTaskExecution(PrimaryBoto3Model):
    """
    DescribeTaskExecutionResponse.
    """
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncTaskExecutionManager

    TaskExecutionArn: "str | None" = None
    """
    The ARN of the task execution that you wanted information about.

    ``TaskExecutionArn`` is hierarchical and includes ``TaskArn`` for the task that was
    executed.
    """

    Status: Literal[
        "QUEUED",
        "CANCELLING",
        "LAUNCHING",
        "PREPARING",
        "TRANSFERRING",
        "VERIFYING",
        "SUCCESS",
        "ERROR",
    ] = Field(default=None, frozen=True)
    """
    The status of the task execution.
    """

    Options: "DataSyncOptions | None" = None
    """
    Indicates how your transfer task is configured.

    These options include how DataSync handles files, objects, and their associated
    metadata during your transfer. You also can specify how to verify data integrity,
    set bandwidth limits for your task, among other options.
    """

    Excludes: "builtins.list[DataSyncFilterRule]" = Field(
        default_factory=list, frozen=True
    )
    """
    A list of filter rules that exclude specific data during your transfer.

    For more information and examples, see
    `Filtering data transferred by DataSync <https://docs.aws.amazon.com/datasync/latest/userguide/filtering.html>`_.
    """
    Includes: "builtins.list[DataSyncFilterRule]" = Field(
        default_factory=list, frozen=True
    )
    """
    A list of filter rules that include specific data during your transfer.

    For more information and examples, see
    `Filtering data transferred by DataSync <https://docs.aws.amazon.com/datasync/latest/userguide/filtering.html>`_.
    """

    ManifestConfig: DataSyncManifestConfig = Field(default=None, frozen=True)
    """
    The configuration of the manifest that lists the files or objects to transfer.

    For more information, see
    `Specifying what DataSync transfers by using a manifest <https://docs.aws.amazon.com/datasync/latest/userguide/transferring-with-
    manifest.html>`_.
    """

    StartTime: datetime = Field(default=None, frozen=True)
    """
    The time that DataSync sends the request to start the task execution.

    For non-queued tasks, ``LaunchTime`` and ``StartTime`` are typically the same. For
    queued tasks, ``LaunchTime`` is typically later than ``StartTime`` because
    previously queued tasks must finish running before newer tasks can begin.
    """

    EstimatedFilesToTransfer: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync expects to transfer over
    the network.

    This value is calculated while DataSync
    `prepares <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-
    task-execution-statuses>`_ the transfer.
    """

    EstimatedBytesToTransfer: int = Field(default=None, frozen=True)
    """
    The number of logical bytes that DataSync expects to write to the destination
    location.
    """

    FilesTransferred: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync actually transfers over
    the network.

    This value is updated periodically during your task execution when something is read
    from the source and sent over the network.
    """

    BytesWritten: int = Field(default=None, frozen=True)
    """
    The number of logical bytes that DataSync actually writes to the destination
    location.
    """

    BytesTransferred: int = Field(default=None, frozen=True)
    """
    The number of bytes that DataSync sends to the network before compression (if
    compression is possible).

    For the number of bytes transferred over the network, see
    `BytesCompressed <https://docs.aws.amazon.com/datasync/latest/userguide/API_DescribeTaskExecution.html#DataSync-
    DescribeTaskExecution-response-BytesCompressed>`_.
    """

    BytesCompressed: int = Field(default=None, frozen=True)
    """
    The number of physical bytes that DataSync transfers over the network after
    compression (if compression is possible).

    This number is typically less than
    `BytesTransferred <https://docs.aws.amazon.com/datasync/latest/userguide/API_DescribeTaskExecution.html#DataSync-
    DescribeTaskExecution-response-BytesTransferred>`_ unless the data isn't
    compressible.
    """

    Result: DataSyncTaskExecutionResultDetail = Field(default=None, frozen=True)
    """
    The result of the task execution.
    """

    TaskReportConfig: DataSyncTaskReportConfig = Field(default=None, frozen=True)
    """
    The configuration of your task report, which provides detailed information about for
    your DataSync transfer.

    For more information, see
    `Creating a task report <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_.
    """

    FilesDeleted: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync actually deletes in your
    destination location.

    If you don't configure your task to `delete data in the destination that isn't in
    the
    source <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html>`_, the value is always ``0``.
    """

    FilesSkipped: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync skips during your
    transfer.
    """

    FilesVerified: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync verifies during your
    transfer.
    """

    ReportResult: DataSyncReportResult = Field(default=None, frozen=True)
    """
Indicates whether DataSync generated a complete `task
report <https://docs.aws.amazon.com/datasync/latest/userguide/task-reports.html>`_ for your transfer.
    """

    EstimatedFilesToDelete: int = Field(default=None, frozen=True)
    """
    The number of files, objects, and directories that DataSync expects to delete in
    your destination location.

    If you don't configure your task to `delete data in the destination that isn't in
    the
    source <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html>`_, the value is always ``0``.
    """

    TaskMode: Literal["BASIC", "ENHANCED"] = Field(default=None, frozen=True)
    """
    The task mode that you're using.

    For more information, see
    `Choosing a task mode for your data transfer <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

    FilesPrepared: int = Field(default=None, frozen=True)
    """
    The number of files or objects that DataSync will attempt to transfer after
    comparing your source and destination locations.
    """

    FilesListed: TaskExecutionFilesListedDetail = Field(default=None, frozen=True)
    """
    The number of files or objects that DataSync finds at your locations.
    """

    FilesFailed: TaskExecutionFilesFailedDetail = Field(default=None, frozen=True)
    """
    The number of files or objects that DataSync fails to prepare, transfer, verify, and
    delete during your task execution.
    """

    EstimatedFoldersToDelete: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync expects to delete in your destination
    location.

    If you don't configure your task to `delete data in the destination that isn't in
    the
    source <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html>`_, the value is always ``0``.
    """

    EstimatedFoldersToTransfer: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync expects to transfer over the network.

    This value is calculated as DataSync
    `prepares <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-statuses>`_
    directories to transfer.
    """

    FoldersSkipped: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync skips during your transfer.
    """

    FoldersPrepared: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync will attempt to transfer after comparing
    your source and destination locations.
    """

    FoldersTransferred: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync actually transfers over the network.

    This value is updated periodically during your task execution when something is read
    from the source and sent over the network.
    """

    FoldersVerified: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync verifies during your transfer.
    """

    FoldersDeleted: int = Field(default=None, frozen=True)
    """
    The number of directories that DataSync actually deletes in your destination
    location.

    If you don't configure your task to `delete data in the destination that isn't in
    the
    source <https://docs.aws.amazon.com/datasync/latest/userguide/configure-metadata.html>`_, the value is always ``0``.
    """

    FoldersListed: TaskExecutionFoldersListedDetail = Field(default=None, frozen=True)
    """
    The number of directories that DataSync finds at your locations.
    """

    FoldersFailed: TaskExecutionFoldersFailedDetail = Field(default=None, frozen=True)
    """
    The number of directories that DataSync fails to list, prepare, transfer, verify,
    and delete during your task execution.
    """

    LaunchTime: datetime = Field(default=None, frozen=True)
    """
    The time that the task execution actually begins.

    For non-queued tasks, ``LaunchTime`` and ``StartTime`` are typically the same. For
    queued tasks, ``LaunchTime`` is typically later than ``StartTime`` because
    previously queued tasks must finish running before newer tasks can begin.
    """
    EndTime: datetime = Field(default=None, frozen=True)
    """
    The time that the transfer task ends.
    """

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`TaskExecutionArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.TaskExecutionArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`TaskExecutionArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.TaskExecutionArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`TaskExecutionArn` attribute.
        """
        return hash(self.TaskExecutionArn)


class DataSyncManagedSecretConfig(Boto3Model):
    """
    Specifies configuration information for a DataSync-managed secret, such as an
    authentication token or set of credentials that DataSync uses to access a specific
    transfer location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """

    SecretArn: "str | None" = None
    """
    Specifies the ARN for an Secrets Manager secret.
    """

class DataSyncCmkSecretConfig(Boto3Model):
    """
    Specifies configuration information for a DataSync-managed secret, such as an
    authentication token, secret key, password, or Kerberos keytab that DataSync uses to
    access a specific storage location, with a customer-managed KMS key.

    You can use either ``CmkSecretConfig`` or ``CustomSecretConfig`` to provide
    credentials for a ``CreateLocation`` request. Do not provide both parameters for the
    same request.
    """
    SecretArn: "str | None" = None
    """
    Specifies the ARN for the DataSync-managed Secrets Manager secret that that is used
    to access a specific storage location.

    This property is generated by DataSync and is read-only. DataSync encrypts this
    secret with the KMS key that you specify for ``KmsKeyArn``.
    """

    KmsKeyArn: "str | None" = None
    """
    Specifies the ARN for the customer-managed KMS key that DataSync uses to encrypt the
    DataSync-managed secret stored for ``SecretArn``.

    DataSync provides this key to Secrets Manager.
    """

class DataSyncCustomSecretConfig(Boto3Model):
    """
    Specifies configuration information for a customer-managed Secrets Manager secret
    where a storage location credentials is stored in Secrets Manager as plain text (for
    authentication token, secret key, or password) or as binary (for Kerberos keytab).
    This configuration includes the secret ARN, and the ARN for an IAM role that
    provides access to the secret.

    You can use either ``CmkSecretConfig`` or ``CustomSecretConfig`` to provide
    credentials for a ``CreateLocation`` request. Do not provide both parameters for the
    same request.
    """

    SecretArn: "str | None" = None
    """
    Specifies the ARN for an Secrets Manager secret.
    """

    SecretAccessRoleArn: "str | None" = None
    """
Specifies the ARN for the Identity and Access Management role that DataSync uses to
access the secret specified for
``SecretArn``.
    """

class DataSyncLocationAzureBlob(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationAzureBlobManager
    )

    LocationArn: "str | None" = None
    """
    The ARN of your Azure Blob Storage transfer location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URL of the Azure Blob Storage container involved in your transfer.
    """

    AuthenticationType: "Literal['SAS', 'NONE'] | None" = None
    """
    The authentication method DataSync uses to access your Azure Blob Storage.

    DataSync can access blob storage using a shared access signature (SAS).
    """

    BlobType: "Literal['BLOCK'] | None" = None
    """
    The type of blob that you want your objects or files to be when transferring them
    into Azure Blob Storage.

    Currently, DataSync only supports moving data into Azure Blob Storage as block
    blobs. For more information on blob types, see the
    `Azure Blob Storage documentation <https://learn.microsoft.com/en-us/rest/api/storageservices/understanding-block-blobs
    --append-blobs--and-page-blobs>`_.
    """

    AccessTier: "Literal['HOT', 'COOL', 'ARCHIVE'] | None" = None
    """
    The access tier that you want your objects or files transferred into.

    This only applies when using the location as a transfer destination. For more
    information, see
    `Access tiers <https://docs.aws.amazon.com/datasync/latest/userguide/creating-azure-blob-location.html#azure-blob-access-tiers>`_.
    """

    AgentArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the DataSync agents that can connect with your Azure Blob Storage
    container.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that your Azure Blob Storage transfer location was created.
    """

    ManagedSecretConfig: DataSyncManagedSecretConfig = Field(default=None, frozen=True)
    """
    Describes configuration information for a DataSync-managed secret, such as an
    authentication token that DataSync uses to access a specific storage location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """

    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as an
    authentication token that DataSync uses to access a specific storage location, with
    a customer-managed KMS key.
    """

    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Describes configuration information for a customer-managed secret, such as an
    authentication token that DataSync uses to access a specific storage location, with
    a customer-managed Identity and Access Management (IAM) role that provides access to
    the secret.
    """

    ContainerUrl: str | None = None
    SasConfiguration: dict[str, Any] | None = Field(default_factory=dict)
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)
    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncEc2Config(Boto3Model):
    """The subnet and security groups that DataSync uses to connect to one of your Amazon EFS file system's `mount
    targets <https://docs.aws.amazon.com/efs/latest/ug/accessing-fs.html>`_.
    """

    SubnetArn: str
    """
Specifies the ARN of a subnet where DataSync creates the `network
interfaces <https://docs.aws.amazon.com/datasync/latest/userguide/datasync-network.html#required-network-interfaces>`_ for
managing traffic during your transfer.
    """

    SecurityGroupArns: "builtins.list[str]"
    """
    Specifies the Amazon Resource Names (ARNs) of the security groups associated with an
    Amazon EFS file system's mount target.
    """


class DataSyncLocationEfs(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeLocationEfsResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncLocationEfsManager

    LocationArn: "str | None" = None
    """
    The ARN of the Amazon EFS file system location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URL of the Amazon EFS file system location.
    """

    Ec2Config: "DataSyncEc2Config | None" = None
    """
The subnet and security groups that DataSync uses to connect to one of your Amazon EFS
file system's `mount
targets <https://docs.aws.amazon.com/efs/latest/ug/accessing-fs.html>`_.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the location was created.
    """

    AccessPointArn: "str | None" = None
    """
    The ARN of the access point that DataSync uses to access the Amazon EFS file system.
    """

    FileSystemAccessRoleArn: "str | None" = None
    """
    The Identity and Access Management (IAM) role that allows DataSync to access your
    Amazon EFS file system.
    """

    InTransitEncryption: "Literal['NONE', 'TLS1_2'] | None" = None
    """
    Indicates whether DataSync uses Transport Layer Security (TLS) encryption when
    transferring data to or from the Amazon EFS file system.
    """

    EfsFilesystemArn: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncLocationFsxLustre(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationFsxLustreManager
    )

    LocationArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the FSx for Lustre location that was described.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URI of the FSx for Lustre location that was described.
    """

    SecurityGroupArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The Amazon Resource Names (ARNs) of the security groups that are configured for the
    FSx for Lustre file system.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the FSx for Lustre location was created.
    """

    FsxFilesystemArn: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncNfsMountOptions(Boto3Model):
    """
    Specifies how DataSync can access a location using the NFS protocol.
    """
    Version: "Literal['AUTOMATIC', 'NFS3', 'NFS4_0', 'NFS4_1'] | None" = None
    """
    Specifies the NFS version that you want DataSync to use when mounting your NFS
    share.

    If the server refuses to use the version specified, the task fails.
    """

class FsxProtocolNfs(Boto3Model):
    """
    Specifies the Network File System (NFS) protocol configuration that DataSync uses to
    access your FSx for OpenZFS file system or FSx for ONTAP file system's storage
    virtual machine (SVM).
    """
    MountOptions: "DataSyncNfsMountOptions | None" = None
    """
    Specifies how DataSync can access a location using the NFS protocol.
    """
class DataSyncSmbMountOptions(Boto3Model):
    """
    Specifies the version of the Server Message Block (SMB) protocol that DataSync uses
    to access an SMB file server.
    """

    Version: "Literal['AUTOMATIC', 'SMB2', 'SMB3', 'SMB1', 'SMB2_0'] | None" = None
    """
    By default, DataSync automatically chooses an SMB protocol version based on
    negotiation with your SMB file server.

    You also can configure DataSync to use a specific SMB version, but we recommend
    doing this only if DataSync has trouble negotiating with the SMB file server
    automatically.
    """

class FsxProtocolSmb(Boto3Model):
    """
    Specifies the Server Message Block (SMB) protocol configuration that DataSync uses
    to access your Amazon FSx for NetApp ONTAP file system's storage virtual machine
    (SVM).

    For more information, see
    `Providing DataSync access to FSx for ONTAP file systems <https://docs.aws.amazon.com/datasync/latest/userguide/create-ontap-location.html#create-ontap-location-
    access>`_.
    """

    Domain: "str | None" = None
    """
    Specifies the name of the Windows domain that your storage virtual machine (SVM)
    belongs to.
    """

    MountOptions: "DataSyncSmbMountOptions | None" = None
    """
    Specifies the version of the Server Message Block (SMB) protocol that DataSync uses
    to access an SMB file server.
    """

    Password: "str | None" = None
    """
    Specifies the password of a user who has permission to access your SVM.
    """
    User: str
    """
    Specifies a user that can mount and access the files, folders, and metadata in your
    SVM.
    """

    ManagedSecretConfig: "DataSyncManagedSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``Password`` that DataSync uses to access a specific storage location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """

    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Specifies configuration information for a DataSync-managed secret, which includes
    the password that DataSync uses to access a specific FSx for ONTAP storage location
    (using SMB), with a customer-managed KMS key.
    """
    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Specifies configuration information for a customer-managed Secrets Manager secret
    where the password for an FSx for ONTAP storage location (using SMB) is stored in
    plain text, in Secrets Manager.

    This configuration includes the secret ARN, and the ARN for an IAM role that
    provides access to the secret. For more information, see
    `Using a secret that you manage <https://docs.aws.amazon.com/datasync/latest/userguide/location-credentials.html#custom-secret-custom-key>`_.
    """
class DataSyncFsxProtocol(Boto3Model):
    """
    Specifies the data transfer protocol that DataSync uses to access your Amazon FSx
    file system.
    """
    NFS: "FsxProtocolNfs | None" = None
    """
    Specifies the Network File System (NFS) protocol configuration that DataSync uses to
    access your FSx for OpenZFS file system or FSx for ONTAP file system's storage
    virtual machine (SVM).
    """
    SMB: "FsxProtocolSmb | None" = None
    """
    Specifies the Server Message Block (SMB) protocol configuration that DataSync uses
    to access your FSx for ONTAP file system's SVM.
    """

class DataSyncLocationFsxOntap(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationFsxOntapManager
    )

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the location was created.
    """

    LocationArn: "str | None" = None
    """
    The ARN of the FSx for ONTAP file system location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The uniform resource identifier (URI) of the FSx for ONTAP file system location.
    """

    Protocol: "DataSyncFsxProtocol | None" = None
    """
    Specifies the data transfer protocol that DataSync uses to access your Amazon FSx
    file system.
    """

    SecurityGroupArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The security groups that DataSync uses to access your FSx for ONTAP file system.
    """

    StorageVirtualMachineArn: "str | None" = None
    """
    The ARN of the storage virtual machine (SVM) on your FSx for ONTAP file system where
    you're copying data to or from.
    """

    FsxFilesystemArn: str = Field(default=None, frozen=True)
    """
    The ARN of the FSx for ONTAP file system.
    """

    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn
    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncLocationFsxOpenZfs(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationFsxOpenZfsManager
    )

    LocationArn: "str | None" = None
    """
    The ARN of the FSx for OpenZFS location that was described.
    """
    LocationUri: str = Field(default=None, frozen=True)
    """
    The uniform resource identifier (URI) of the FSx for OpenZFS location that was
    described.
    """

    SecurityGroupArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the security groups that are configured for the FSx for OpenZFS file
    system.
    """

    Protocol: "DataSyncFsxProtocol | None" = None
    """
    The type of protocol that DataSync uses to access your file system.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the FSx for OpenZFS location was created.
    """

    FsxFilesystemArn: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncLocationFsxWindows(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationFsxWindowsManager
    )

    LocationArn: "str | None" = None
    """
    The ARN of the FSx for Windows File Server location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The uniform resource identifier (URI) of the FSx for Windows File Server location.
    """

    SecurityGroupArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the Amazon EC2 security groups that provide access to your file system's
    preferred subnet.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the FSx for Windows File Server location was created.
    """

    User: "str | None" = None
    """
    The user with the permissions to mount and access the FSx for Windows File Server
    file system.
    """

    Domain: "str | None" = None
    """
    The name of the Microsoft Active Directory domain that the FSx for Windows File
    Server file system belongs to.
    """

    ManagedSecretConfig: DataSyncManagedSecretConfig = Field(default=None, frozen=True)
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``Password`` that DataSync uses to access a specific storage location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """
    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``Password`` that DataSync uses to access a specific storage location, with a
    customer-managed KMS key.
    """

    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Describes configuration information for a customer-managed secret, such as a
    ``Password`` that DataSync uses to access a specific storage location, with a
    customer-managed Identity and Access Management (IAM) role that provides access to
    the secret.
    """

    FsxFilesystemArn: str | None = None
    Password: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn
    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class HdfsNameNode(Boto3Model):
    """
    The NameNode of the Hadoop Distributed File System (HDFS).

    The NameNode manages the file system's namespace. The NameNode performs operations
    such as opening, closing, and renaming files and directories. The NameNode contains
    the information to map blocks of data to the DataNodes.
    """
    Hostname: str
    """
    The hostname of the NameNode in the HDFS cluster.

    This value is the IP address or Domain Name Service (DNS) name of the NameNode. An
    agent that's installed on-premises uses this hostname to communicate with the
    NameNode in the network.
    """

    Port: int
    """
    The port that the NameNode uses to listen to client requests.
    """


class DataSyncQopConfiguration(Boto3Model):
    """
    The Quality of Protection (QOP) configuration specifies the Remote Procedure Call
    (RPC) and data transfer privacy settings configured on the Hadoop Distributed File
    System (HDFS) cluster.
    """

    RpcProtection: "Literal['DISABLED', 'AUTHENTICATION', 'INTEGRITY', 'PRIVACY'] | None" = None
    """
    The RPC protection setting configured on the HDFS cluster.

    This setting corresponds to your ``hadoop.rpc.protection`` setting in your ``core-
    site.xml`` file on your Hadoop cluster.
    """

    DataTransferProtection: "Literal['DISABLED', 'AUTHENTICATION', 'INTEGRITY', 'PRIVACY'] | None" = None
    """
    The data transfer protection setting configured on the HDFS cluster.

    This setting corresponds to your
    ``dfs.data.transfer.protection`` setting in the ``hdfs-site.xml`` file on your
    Hadoop cluster.
    """
class DataSyncLocationHdfs(TagsDictMixin, PrimaryBoto3Model):
    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncLocationHdfsManager

    LocationArn: "str | None" = None
    """
    The ARN of the HDFS location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URI of the HDFS location.
    """

    NameNodes: "builtins.list[HdfsNameNode] | None" = Field(default_factory=list)
    """
    The NameNode that manages the HDFS namespace.
    """

    BlockSize: "int | None" = None
    """
    The size of the data blocks to write into the HDFS cluster.
    """

    ReplicationFactor: "int | None" = None
    """
    The number of DataNodes to replicate the data to when writing to the HDFS cluster.
    """

    KmsKeyProviderUri: "str | None" = None
    """
    The URI of the HDFS cluster's Key Management Server (KMS).
    """

    QopConfiguration: "DataSyncQopConfiguration | None" = None
    """
    The Quality of Protection (QOP) configuration, which specifies the Remote Procedure
    Call (RPC) and data transfer protection settings configured on the HDFS cluster.
    """

    AuthenticationType: "Literal['SIMPLE', 'KERBEROS'] | None" = None
    """
    The type of authentication used to determine the identity of the user.
    """

    SimpleUser: "str | None" = None
    """
    The user name to identify the client on the host operating system.

    This parameter is used if the ``AuthenticationType`` is defined as ``SIMPLE``.
    """

    KerberosPrincipal: "str | None" = None
    """
    The Kerberos principal with access to the files and folders on the HDFS cluster.

    This parameter is used if the
    ``AuthenticationType`` is defined as ``KERBEROS``.
    """

    AgentArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the DataSync agents that can connect with your HDFS cluster.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the HDFS location was created.
    """

    ManagedSecretConfig: DataSyncManagedSecretConfig = Field(default=None, frozen=True)
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``KerberosKeytab`` that DataSync uses to access a specific storage location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """

    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``KerberosKeytab`` that DataSync uses to access a specific storage location, with a
    customer-managed KMS key.
    """

    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Describes configuration information for a customer-managed secret, such as a
    ``KerberosKeytab`` that DataSync uses to access a specific storage location, with a
    customer-managed Identity and Access Management (IAM) role that provides access to
    the secret.
    """

    KerberosKeytab: bytes | None = None
    KerberosKrb5Conf: bytes | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncOnPremConfig(Boto3Model):
    """
    The DataSync agents that can connect to your Network File System (NFS) file server.
    """
    AgentArns: "builtins.list[str]"
    """
    The Amazon Resource Names (ARNs) of the DataSync agents that can connect to your NFS
    file server.
    """


class DataSyncLocationNfs(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeLocationNfsResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncLocationNfsManager

    LocationArn: "str | None" = None
    """
    The ARN of the NFS location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URI of the NFS location.
    """

    OnPremConfig: "DataSyncOnPremConfig | None" = None
    """
    The DataSync agents that can connect to your Network File System (NFS) file server.
    """

    MountOptions: "DataSyncNfsMountOptions | None" = None
    """
    The mount options that DataSync uses to mount your NFS file server.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time when the NFS location was created.
    """

    ServerHostname: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)
    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)
class DataSyncLocationObjectStorage(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeLocationObjectStorageResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = (
        DataSyncLocationObjectStorageManager
    )

    LocationArn: "str | None" = None
    """
    The ARN of the object storage system location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URI of the object storage system location.
    """

    AccessKey: "str | None" = None
    """
    The access key (for example, a user name) required to authenticate with the object
    storage system.
    """

    ServerPort: "int | None" = None
    """
    The port that your object storage server accepts inbound network traffic on (for
    example, port 443).
    """

    ServerProtocol: "Literal['HTTPS', 'HTTP'] | None" = None
    """
    The protocol that your object storage system uses to communicate.
    """

    AgentArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the DataSync agents that can connect with your object storage system.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the location was created.
    """

    ServerCertificate: "bytes | None" = None
    """
    The certificate chain for DataSync to authenticate with your object storage system
    if the system uses a private or self- signed certificate authority (CA).
    """
    ManagedSecretConfig: DataSyncManagedSecretConfig = Field(default=None, frozen=True)
    """
    Describes configuration information for a DataSync-managed secret, such as an
    authentication token or set of credentials that DataSync uses to access a specific
    transfer location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """

    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as an
    authentication token or set of credentials that DataSync uses to access a specific
    transfer location, and a customer-managed KMS key.
    """

    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Describes configuration information for a customer-managed secret, such as an
    authentication token or set of credentials that DataSync uses to access a specific
    transfer location, and a customer-managed Identity and Access Management (IAM) role
    that provides access to the secret.
    """

    BucketName: str | None = None
    SecretKey: str | None = None
    ServerHostname: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)


class DataSyncS3Config(Boto3Model):
    """
    Specifies the Amazon Resource Name (ARN) of the Identity and Access Management (IAM)
    role that DataSync uses to access your S3 bucket.

    For more information, see
    `Providing DataSync access to S3 buckets <https://docs.aws.amazon.com/datasync/latest/userguide/create-s3-location.html#create-s3-location-access>`_.
    """

    BucketAccessRoleArn: str
    """
    Specifies the ARN of the IAM role that DataSync uses to access your S3 bucket.
    """


class DataSyncLocationS3(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeLocationS3Response.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncLocationS3Manager

    LocationArn: "str | None" = None
    """
    The ARN of the Amazon S3 location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URL of the Amazon S3 location that was described.
    """

    S3StorageClass: "Literal['STANDARD', 'STANDARD_IA', 'ONEZONE_IA', 'INTELLIGENT_TIERING', 'GLACIER', 'DEEP_ARCHIVE', 'OUTPOSTS', 'GLACIER_INSTANT_RETRIEVAL'] | None" = None
    """
    When Amazon S3 is a destination location, this is the storage class that you chose
    for your objects.
    """

    S3Config: "DataSyncS3Config | None" = None
    """
    Specifies the Amazon Resource Name (ARN) of the Identity and Access Management (IAM)
    role that DataSync uses to access your S3 bucket.
    """

    AgentArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the DataSync agents deployed on your Outpost when using working with
    Amazon S3 on Outposts.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the Amazon S3 location was created.
    """

    S3BucketArn: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn
    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)
class DataSyncLocationSmb(TagsDictMixin, PrimaryBoto3Model):
    """
    DescribeLocationSmbResponse.
    """

    tag_class: ClassVar[ModelType[Boto3Model]] = Tag | None
    manager_class: ClassVar[ModelType[Boto3ModelManager]] = DataSyncLocationSmbManager

    LocationArn: "str | None" = None
    """
    The ARN of the SMB location.
    """

    LocationUri: str = Field(default=None, frozen=True)
    """
    The URI of the SMB location.
    """

    AgentArns: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The ARNs of the DataSync agents that can connect with your SMB file server.
    """

    User: "str | None" = None
    """
    The user that can mount and access the files, folders, and file metadata in your SMB
    file server.

    This element applies only if ``AuthenticationType`` is set to ``NTLM``.
    """

    Domain: "str | None" = None
    """
    The name of the Windows domain that the SMB file server belongs to.

    This element applies only if ``AuthenticationType`` is set to ``NTLM``.
    """
    MountOptions: "DataSyncSmbMountOptions | None" = None
    """
    The SMB protocol version that DataSync uses to access your SMB file server.
    """

    CreationTime: datetime = Field(default=None, frozen=True)
    """
    The time that the SMB location was created.
    """

    DnsIpAddresses: "builtins.list[str] | None" = Field(default_factory=list)
    """
    The IPv4 or IPv6 addresses for the DNS servers that your SMB file server belongs to.

    This element applies only if
    ``AuthenticationType`` is set to ``KERBEROS``.
    """

    KerberosPrincipal: "str | None" = None
    """
    The Kerberos principal that has permission to access the files, folders, and file
    metadata in your SMB file server.
    """
    AuthenticationType: "Literal['NTLM', 'KERBEROS'] | None" = None
    """
    The authentication protocol that DataSync uses to connect to your SMB file server.
    """

    ManagedSecretConfig: DataSyncManagedSecretConfig = Field(default=None, frozen=True)
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``Password`` or ``KerberosKeytab`` that DataSync uses to access a specific storage
    location.

    DataSync uses the default Amazon Web Services-managed KMS key to encrypt this secret
    in Secrets Manager.
    """
    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Describes configuration information for a DataSync-managed secret, such as a
    ``Password`` or ``KerberosKeytab`` that DataSync uses to access a specific storage
    location, with a customer-managed KMS key.
    """
    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Describes configuration information for a customer-managed secret, such as a
    ``Password`` or ``KerberosKeytab`` that DataSync uses to access a specific storage
    location, with a customer-managed Identity and Access Management (IAM) role that
    provides access to the secret.
    """

    KerberosKeytab: bytes | None = None
    KerberosKrb5Conf: bytes | None = None
    Password: str | None = None
    ServerHostname: str | None = None
    Subdirectory: str | None = None
    Tags: builtins.list[Tag] | None = Field(default_factory=list)

    @property
    def pk(self) -> str | None:
        """
        Return the primary key of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The primary key of the model instance.
        """
        return self.LocationArn

    @property
    def arn(self) -> str | None:
        """
        Return the ARN of the model.

        This is the value of the :py:attr:`LocationArn` attribute.

        Returns:
            The ARN of the model instance.
        """
        return self.LocationArn

    def __hash__(self) -> int:
        """
        Return the hash of the model.

        This is the value of the
        :py:attr:`LocationArn` attribute.
        """
        return hash(self.LocationArn)
# =======================
# Request/Response Models
# =======================


class TagListEntry(Boto3Model):
    """
    A key-value pair representing a single tag that's been applied to an Amazon Web
    Services resource.
    """

    Key: str
    """
    The key for an Amazon Web Services resource tag.
    """

    Value: "str | None" = None
    """
    The value for an Amazon Web Services resource tag.
    """

class CreateAgentResponse(Boto3Model):
    """
    CreateAgentResponse.
    """

    AgentArn: "str | None" = None
    """
    The ARN of the agent that you just activated.

    Use the
    `ListAgents <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListAgents.html>`_
    operation to return a list of
    agents in your Amazon Web Services account and Amazon Web Services Region.
    """

class AgentListEntry(Boto3Model):
    """Represents a single entry in a list (or array) of DataSync agents when you call the
    `ListAgents <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListAgents.html>`_ operation.
    """

    AgentArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of a DataSync agent.
    """

    Name: "str | None" = None
    """
    The name of an agent.
    """

    Status: "Literal['ONLINE', 'OFFLINE'] | None" = None
    """
    The status of an agent.
    """

    Platform: "DataSyncPlatform | None" = None
    """
    The platform-related details about the agent, such as the version number.
    """

class ListAgentsResponse(Boto3Model):
    """
    ListAgentsResponse.
    """

    Agents: "builtins.list[AgentListEntry] | None" = Field(default_factory=list)
    """
    A list of DataSync agents in your Amazon Web Services account in the Amazon Web
    Services Region specified in the request.

    The list is ordered by the agents' Amazon Resource Names (ARNs).
    """

    NextToken: "str | None" = None
    """
    The opaque string that indicates the position to begin the next list of results in
    the response.
    """

class UpdateAgentResponse(Boto3Model):
    pass


class DeleteAgentResponse(Boto3Model):
    pass


class CreateTaskResponse(Boto3Model):
    """
    CreateTaskResponse.
    """

    TaskArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the task.
    """

class TaskFilter(Boto3Model):
    """
    You can use API filters to narrow down the list of resources returned by
    ``ListTasks``.

    For example, to retrieve all tasks on a source location, you can use ``ListTasks``
    with filter name ``LocationId`` and ``Operator Equals`` with the ARN for the
    location.

    For more information, see
    `filtering DataSync resources <https://docs.aws.amazon.com/datasync/latest/userguide/query-
    resources.html>`_.
    """

    Name: Literal["LocationId", "CreationTime"]
    """
    The name of the filter being used.

    Each API call supports a list of filters that are available for it. For example,
    ``LocationId`` for ``ListTasks``.
    """
    Values: "builtins.list[str]"
    """
    The values that you want to filter for.

    For example, you might want to display only tasks for a specific destination
    location.
    """

    Operator: Literal[
        "Equals",
        "NotEquals",
        "In",
        "LessThanOrEqual",
        "LessThan",
        "GreaterThanOrEqual",
        "GreaterThan",
        "Contains",
        "NotContains",
        "BeginsWith",
    ]
    """
    The operator that is used to compare filter values (for example, ``Equals`` or
    ``Contains``).
    """


class TaskListEntry(Boto3Model):
    """
    Represents a single entry in a list of tasks.

    ``TaskListEntry`` returns an array that contains a list of tasks when the
    `ListTasks <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListTasks.html>`_
    operation is called. A task
    includes the source and destination file systems to sync and the options to use for
    the tasks.
    """

    TaskArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the task.
    """

    Status: "Literal['AVAILABLE', 'CREATING', 'QUEUED', 'RUNNING', 'UNAVAILABLE'] | None" = None
    """
    The status of the task.
    """

    Name: "str | None" = None
    """
    The name of the task.
    """

    TaskMode: "Literal['BASIC', 'ENHANCED'] | None" = None
    """
    The task mode that you're using.

    For more information, see
    `Choosing a task mode for your data transfer <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

class DataSyncListTasksResponse(Boto3Model):
    """
    ListTasksResponse.
    """

    Tasks: "builtins.list[TaskListEntry] | None" = Field(default_factory=list)
    """
    A list of all the tasks that are returned.
    """

    NextToken: "str | None" = None
    """
    An opaque string that indicates the position at which to begin returning the next
    list of tasks.
    """
class UpdateTaskResponse(Boto3Model):
    pass


class DeleteTaskResponse(Boto3Model):
    pass


class StartTaskExecutionResponse(Boto3Model):
    """
    StartTaskExecutionResponse.
    """

    TaskExecutionArn: "str | None" = None
    """
    The ARN of the running task execution.
    """

class TaskExecutionListEntry(Boto3Model):
    """Represents a single entry in a list of DataSync task executions that's returned with the
    `ListTaskExecutions <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListTaskExecutions.html>`_ operation.
    """
    TaskExecutionArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of a task execution.
    """

    Status: "Literal['QUEUED', 'CANCELLING', 'LAUNCHING', 'PREPARING', 'TRANSFERRING', 'VERIFYING', 'SUCCESS', 'ERROR'] | None" = None
    """
    The status of a task execution.

    For more information, see
    `Task execution statuses <https://docs.aws.amazon.com/datasync/latest/userguide/run-task.html#understand-task-execution-statuses>`_.
    """

    TaskMode: "Literal['BASIC', 'ENHANCED'] | None" = None
    """
    The task mode that you're using.

    For more information, see
    `Choosing a task mode for your data transfer <https://docs.aws.amazon.com/datasync/latest/userguide/choosing-task-mode.html>`_.
    """

class ListTaskExecutionsResponse(Boto3Model):
    """
    ListTaskExecutionsResponse.
    """

    TaskExecutions: "builtins.list[TaskExecutionListEntry] | None" = Field(
        default_factory=list
    )
    """
    A list of the task's executions.
    """

    NextToken: "str | None" = None
    """
    The opaque string that indicates the position to begin the next list of results in
    the response.
    """

class UpdateTaskExecutionResponse(Boto3Model):
    pass


class CancelTaskExecutionResponse(Boto3Model):
    pass


class AzureBlobSasConfiguration(Boto3Model):
    """
    The shared access signature (SAS) configuration that allows DataSync to access your
    Microsoft Azure Blob Storage.

    For more information, see
    `SAS tokens <https://docs.aws.amazon.com/datasync/latest/userguide/creating-azure-blob-
    location.html#azure-blob-sas-tokens>`_ for accessing your Azure Blob Storage.
    """

    Token: str
    """
    Specifies a SAS token that provides permissions to access your Azure Blob Storage.
    """


class CreateLocationAzureBlobResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    The ARN of the Azure Blob Storage transfer location that you created.
    """

class LocationFilter(Boto3Model):
    """
    Narrow down the list of resources returned by ``ListLocations``.

    For example, to see all your Amazon S3 locations, create a filter using ``"Name":
    "LocationType"``, ``"Operator": "Equals"``, and ``"Values": "S3"``.

    For more information, see
    `filtering resources <https://docs.aws.amazon.com/datasync/latest/userguide/query-
    resources.html>`_.
    """

    Name: Literal["LocationUri", "LocationType", "CreationTime"]
    """
    The name of the filter being used.

    Each API call supports a list of filters that are available for it (for example,
    ``LocationType`` for ``ListLocations``).
    """

    Values: "builtins.list[str]"
    """
    The values that you want to filter for.

    For example, you might want to display only Amazon S3 locations.
    """

    Operator: Literal[
        "Equals",
        "NotEquals",
        "In",
        "LessThanOrEqual",
        "LessThan",
        "GreaterThanOrEqual",
        "GreaterThan",
        "Contains",
        "NotContains",
        "BeginsWith",
    ]
    """
    The operator that is used to compare filter values (for example, ``Equals`` or
    ``Contains``).
    """


class LocationListEntry(Boto3Model):
    """
    Represents a single entry in a list of locations.

    ``LocationListEntry`` returns an array that contains a list of locations when the
    `ListLocations <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListLocations.html>`_
    operation is called.
    """

    LocationArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the location.

    For Network File System (NFS) or Amazon EFS, the location is the export path. For
    Amazon S3, the location is the prefix path that you want to mount and use as the
    root of the location.
    """
    LocationUri: "str | None" = None
    """
    Represents a list of URIs of a location.

    ``LocationUri`` returns an array that contains a list of locations when the
    `ListLocations <https://docs.aws.amazon.com/datasync/latest/userguide/API_ListLocations.html>`_
    operation is called.
    """
class ListLocationsResponse(Boto3Model):
    """
    ListLocationsResponse.
    """

    Locations: "builtins.list[LocationListEntry] | None" = Field(default_factory=list)
    """
    An array that contains a list of locations.
    """

    NextToken: "str | None" = None
    """
    An opaque string that indicates the position at which to begin returning the next
    list of locations.
    """

class UpdateLocationAzureBlobResponse(Boto3Model):
    pass


class DeleteLocationResponse(Boto3Model):
    pass


class CreateLocationEfsResponse(Boto3Model):
    """
    CreateLocationEfs.
    """

    LocationArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the Amazon EFS file system location that you
    create.
    """

class UpdateLocationEfsResponse(Boto3Model):
    pass


class CreateLocationFsxLustreResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    The Amazon Resource Name (ARN) of the FSx for Lustre file system location that you
    created.
    """

class UpdateLocationFsxLustreResponse(Boto3Model):
    pass


class CreateLocationFsxOntapResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    Specifies the ARN of the FSx for ONTAP file system location that you create.
    """

class FsxUpdateProtocolSmb(Boto3Model):
    """
    Specifies the Server Message Block (SMB) protocol configuration that DataSync uses
    to access your Amazon FSx for NetApp ONTAP file system's storage virtual machine
    (SVM).

    For more information, see
    `Providing DataSync access to FSx for ONTAP file systems <https://docs.aws.amazon.com/datasync/latest/userguide/create-ontap-location.html#create-ontap-location-
    access>`_.
    """

    Domain: "str | None" = None
    """
    Specifies the name of the Windows domain that your storage virtual machine (SVM)
    belongs to.
    """

    MountOptions: "DataSyncSmbMountOptions | None" = None
    """
    Specifies the version of the Server Message Block (SMB) protocol that DataSync uses
    to access an SMB file server.
    """

    Password: "str | None" = None
    """
    Specifies the password of a user who has permission to access your SVM.
    """

    User: "str | None" = None
    """
    Specifies a user that can mount and access the files, folders, and metadata in your
    SVM.
    """

    CmkSecretConfig: "DataSyncCmkSecretConfig | None" = None
    """
    Specifies configuration information for a DataSync-managed secret, such as a
    ``Password`` or set of credentials that DataSync uses to access a specific transfer
    location, and a customer-managed KMS key.
    """

    CustomSecretConfig: "DataSyncCustomSecretConfig | None" = None
    """
    Specifies configuration information for a customer-managed secret, such as a
    ``Password`` or set of credentials that DataSync uses to access a specific transfer
    location.

    This configuration includes the secret ARN, and the ARN for an IAM role that
    provides access to the secret.
    """

class FsxUpdateProtocol(Boto3Model):
    """
    Specifies the data transfer protocol that DataSync uses to access your Amazon FSx
    file system.

    You can't update the Network File System (NFS) protocol configuration for FSx for
    ONTAP locations. DataSync currently only supports NFS version 3 with this location
    type.
    """

    NFS: "FsxProtocolNfs | None" = None
    """
    Specifies the Network File System (NFS) protocol configuration that DataSync uses to
    access your FSx for OpenZFS file system or FSx for ONTAP file system's storage
    virtual machine (SVM).
    """

    SMB: "FsxUpdateProtocolSmb | None" = None
    """
    Specifies the Server Message Block (SMB) protocol configuration that DataSync uses
    to access your FSx for ONTAP file system's storage virtual machine (SVM).
    """

class UpdateLocationFsxOntapResponse(Boto3Model):
    pass


class CreateLocationFsxOpenZfsResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    The ARN of the FSx for OpenZFS file system location that you created.
    """

class UpdateLocationFsxOpenZfsResponse(Boto3Model):
    pass


class CreateLocationFsxWindowsResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    The ARN of the FSx for Windows File Server file system location you created.
    """

class UpdateLocationFsxWindowsResponse(Boto3Model):
    pass


class CreateLocationHdfsResponse(Boto3Model):
    LocationArn: "str | None" = None
    """
    The ARN of the source HDFS cluster location that you create.
    """

class UpdateLocationHdfsResponse(Boto3Model):
    pass


class CreateLocationNfsResponse(Boto3Model):
    """
    CreateLocationNfsResponse.
    """

    LocationArn: "str | None" = None
    """
    The ARN of the transfer location that you created for your NFS file server.
    """

class UpdateLocationNfsResponse(Boto3Model):
    pass


class CreateLocationObjectStorageResponse(Boto3Model):
    """
    CreateLocationObjectStorageResponse.
    """

    LocationArn: "str | None" = None
    """
    Specifies the ARN of the object storage system location that you create.
    """

class UpdateLocationObjectStorageResponse(Boto3Model):
    pass


class CreateLocationS3Response(Boto3Model):
    """
    CreateLocationS3Response.
    """

    LocationArn: "str | None" = None
    """
    The ARN of the S3 location that you created.
    """

class UpdateLocationS3Response(Boto3Model):
    pass


class CreateLocationSmbResponse(Boto3Model):
    """
    CreateLocationSmbResponse.
    """

    LocationArn: "str | None" = None
    """
    The ARN of the SMB location that you created.
    """

class UpdateLocationSmbResponse(Boto3Model):
    pass
